# 开发日志

## 使用规则

- 每次开始开发前先阅读本文件，再检查仓库状态。
- 每完成一个独立、可验证的子任务，即更新本文件并创建一次 Git commit。
- 每条记录说明完成内容、验证方式、相关 commit 和下一步。
- 所有新增或修改的业务代码，均须以易读注释说明每个逻辑区块的职责、输入输出和重要业务原因；避免只重复代码字面含义的无效注释。

## 2026-08-09｜项目基线

### 已完成

- 已确认并归档中文产品需求文档（PRD v1.0）。
- 已创建开发分支 `codex/timetabling-mvp`。
- 已建立本开发日志和基础 `.gitignore`。

### 当前状态

- 尚未初始化应用工程。
- 尚未选定前端、后端、数据库与认证技术栈。
- 现有 `Teaching Members` Excel 的导入格式已在 PRD 中定义。

### 本次验证

- 已检查 Git 工作区和分支状态。

### 下一步

1. 评估并确定适合多人实时协作排课系统的技术栈与基础工程结构。
2. 初始化应用工程，并在完成后独立提交。

## 2026-08-09｜GitHub 远程接入

### 已完成

- 已将 `origin` 配置为 `https://github.com/wanxin1996/npicttimetabling.git`。
- 已完成远程仓库的只读连接检查；当前远程没有分支。
- 已将本地开发分支首次推送至 GitHub。

### 当前状态

- 本地开发分支：`codex/timetabling-mvp`。
- 本地分支已跟踪 `origin/codex/timetabling-mvp`。

### 本次验证

- `git ls-remote --heads origin` 成功完成且未返回远程分支。
- `git push -u origin codex/timetabling-mvp` 成功完成。

### 下一步

1. 评估技术栈并初始化应用工程。

## 2026-08-09｜Web 应用基础工程

### 已完成

- 已在 `web/` 初始化 Next.js 16、React 19、TypeScript、Tailwind CSS 与 ESLint。
- 已移除默认 Google Fonts 依赖，避免构建时依赖外部字体服务。
- 已将生产构建固定为 `next build --webpack`；当前环境的 Turbopack 无法创建其内部所需进程/端口，但 Webpack 构建可稳定通过。

### 技术选择

- 前端与服务层：Next.js App Router + TypeScript。
- UI 样式：Tailwind CSS。
- 质量检查：ESLint 与生产构建。
- 数据库、认证与实时协作将在下一子任务中确定并接入。

### 本次验证

- `npm run lint` 通过。
- `npx next build --webpack` 通过，首页可静态构建。

### 下一步

1. 确定数据库、认证和实时协作方案，并建立数据模型。
2. 实现基础数据维护页面。

## 2026-08-09｜数据模型与本地数据层

### 已完成

- 已引入 Prisma ORM 与 SQLite 本地驱动。
- 已建立课程、课程班次、每次课程安排、教师、学生班级、教室、教学分配、不可上课时段、规则设置与恢复备份的数据模型。
- 已为 `Teaching Members` 的课程—教师—班次数量分配建立 `TeachingAllocation` 模型。
- 已增加 `db:generate` 与 `db:check` 命令，并新增技术架构说明。

### 当前限制

- Prisma 的 schema-engine 在当前环境无法实际执行 SQLite 迁移，错误信息为空；但 schema 校验、从空数据库生成完整 SQLite SQL、Prisma Client 生成、lint 及生产构建均通过。
- 线上 PostgreSQL、认证与实时协作服务尚未创建，因此本子任务只完成可迁移的数据模型和本地数据访问层。

### 本次验证

- `npx prisma validate` 通过。
- `npx prisma migrate diff --from-empty --to-schema prisma/schema.prisma --script` 成功生成完整建表 SQL。
- `npx prisma generate` 通过。
- `npm run lint` 与 `npm run build` 通过。

### 下一步

1. 实现教师、学生班级和教室的基础资料维护界面与输入校验。
2. 选择并接入线上 PostgreSQL、认证与实时协作服务。

## 2026-08-09｜基础资料维护工作台

### 已完成

- 已实现英文的资料维护工作台，包含教师、学生班级和教室三个视图。
- 已实现未排资料的搜索、视图切换、新增教师/班级/教室，以及教师和教室的启用/停用交互。
- 已在界面中显示 PT 教师优先标记、学生班级年级/专业和教室容量/设施。

### 当前限制

- 本子任务使用浏览器会话内示例数据，操作不会持久化到数据库；数据库迁移引擎问题解决并接通数据访问层后，将替换为真实数据。

### 本次验证

- `npm run lint` 与 `npm run build` 通过。
- 已在浏览器中验证资料视图切换和新增学生班级流程；统计与表格即时更新，刷新后恢复默认示例数据。

### 下一步

1. 接入基础资料的持久化 API，并解决/替换本地数据库迁移执行方式。
2. 实现 `Teaching Members` Excel 导入与课程班次生成。

## 2026-08-09｜基础资料持久化 API

### 已完成

- 已使用 `better-sqlite3` 建立本地 SQLite 数据访问层，替代当前环境中无法执行的 Prisma schema-engine 迁移。
- 已实现教师、学生班级和教室的读取、新增及教师/教室启用状态 API。
- 已将资料维护界面从浏览器会话示例数据接入本地 API；刷新页面后新增和状态变更均会保留。
- Smart Classroom 创建时会自动同时设置 Multi Projector，教室 Block 由地址第一段自动解析。

### 本次验证

- `npm run lint` 与 `npm run build` 通过。
- API 测试：成功新增、读取并清除临时学生班级。
- 浏览器测试：成功通过界面新增临时学生班级，刷新后确认仍存在；测试记录已清除。

### 下一步

1. 实现 `Teaching Members` Excel 导入、课程资料维护及课程班次生成。
2. 接入线上 PostgreSQL、正式认证与实时协作服务。

## 2026-08-09｜Teaching Members Excel 导入与班次预生成

### 已完成

- 已新增本地 `Teaching Members` Excel 导入：读取 `Mod`、`Catalog`、`Lecturer`、`Staff Type` 与 `# of grps teaching`。
- 对每个正数分配自动创建/更新课程、教师和教学分配，并按数量预生成带教师的课程班次；0 班分配的行会忽略，不会错误创建班次。
- 已在资料维护页增加 Courses 视图和导入入口，展示每门课的预分配及生成班次数量；PT 教师会继续标记为优先。
- 已加入本地数据库表的幂等初始化，确保升级现有开发数据库后可自动补齐课程相关表。

### 本次验证

- `npm run lint` 与 `npm run build` 通过。
- 使用用户提供的 `Teaching Team 2026S1 (20-2-2026).xlsx` 验证导入：成功得到 52 门课程、86 位有正向分配的教师、184 条分配、374 个预生成班次，忽略 29 条零分配行。
- `LEAD` 已验证为 18 个预生成班次；浏览器中已确认教师计数、PT 优先数量和课程班次数量显示正确。

### 下一步

1. 增加课程配置（时长、每周次数、年级、教室容量/设施要求）及人工调整教师分配。
2. 实现年级总表的拖拽排课和实时冲突检查。

## 2026-08-09｜代码可读性注释规范

### 已完成

- 已为现有页面、SQLite 数据访问层、Excel 导入流程、所有本地 API、Prisma 数据模型及应用布局补充面向业务的区块注释。
- 已将“每次修改业务代码必须同步更新易读注释”加入本开发日志的长期工作规则。

### 本次验证

- `npm run lint` 与 `npm run build` 通过。

### 下一步

1. 增加课程配置（时长、每周次数、年级、教室容量/设施要求）及人工调整教师分配，并遵守上述注释规范。
2. 实现年级总表的拖拽排课和实时冲突检查。

## 2026-08-09｜课程排课要求配置

### 已完成

- 已在 Courses 视图中为每门课程增加 Configure 操作，可手工设置课程时长、每周上课次数、主年级、最低教室容量，以及 Lab／Multi Projector／Smart Classroom 要求。
- 已新增课程配置 API，并为现有本地 SQLite 数据库提供安全的字段升级逻辑。
- 课程配置存储在课程层级，因此所有预生成班次共享同一组要求；重新导入 Teaching Members 时会刷新教师分配和班次，但不会覆盖这些手工设置。

### 本次验证

- `npm run lint` 与 `npm run build` 通过。
- API 验证：已将 LEAD 设置为 3 小时、每周一次、Year 1、最低容量 20、需要 Multi Projector；重新导入真实 Teaching Members 后，该配置仍完整保留，且 LEAD 仍为 18 个班次。

### 下一步

1. 增加课程班次层级的人工调整：为每个 `MOD_01` 等班次调整教师与关联的学生班级。
2. 实现年级总表的拖拽排课和实时冲突检查。

## 2026-08-09｜课程班次人工分配

### 已完成

- 已在 Courses 视图增加 Sections 操作，可打开每门课的 `MOD_01` 等班次清单。
- 每个班次可独立选择一位有效教师，并勾选一个或多个学生班级，因此支持跨年级课程。
- 已建立班次—学生班级关联表和本地 API；这组关联将作为后续教师、学生班级冲突检查的输入。

### 本次验证

- `npm run lint` 与 `npm run build` 通过。
- API 验证：已将 `LEAD_01` 设置为 WAN XIN 教师和 AAA_01 学生班，重新读取后设置正确保留。

### 下一步

1. 实现年级总表的拖拽排课，显示尚未安排的班次。
2. 在放置或编辑课程时实时检查教师、教室和学生班级冲突。

## 2026-08-10｜年级总表基础网格与排课记录

### 已完成

- 已增加 Monday–Friday、08:00–18:00 的年级总表基础网格，可在 Year 1、Year 2、Year 3 间切换。
- 已建立排课记录表和 `GET`/`POST /api/schedule/lessons` API；课程放置时按课程时长检查工作日与时间范围。
- 教师冲突和教室冲突会以 warnings 返回，同时允许保存，符合“可以排入总表但必须看到警告”的需求。
- 排课记录会按课程主年级显示，跨年级学生班级仍保留在班次关联中，供后续冲突检查使用。

### 本次验证

- `npm run lint` 与 `npm run build` 通过。
- API 验证：已将 `LEAD_01` 放置到 Year 1 的周一 09:00，保存 3 小时课程并能从 Year 1 总表读取。

### 下一步

1. 将未排班次拖拽到总表，并在网格内编辑教师、教室和时间。
2. 完善学生班级冲突、不可用时段和软规则的实时警告明细。

## 2026-08-10｜未排班次拖拽放置

### 已完成

- 已在年级总表左侧增加未排班次清单，只显示属于当前主年级、已填写课程时长且尚未安排的班次。
- 班次卡片会显示编号、时长、教师和学生班级，可拖入 Monday–Friday 的任意整点格子。
- 放置成功后自动刷新总表和未排清单；服务器仍负责校验课程时长、工作日和 08:00–18:00 范围，并返回冲突警告。
- 已修正总表网格为“时间 + 五个工作日”六列，并在总表模式隐藏无关的资料管理表格。

### 本次验证

- `npm run lint` 与 `npm run build` 通过。
- 未排班次 API 验证：Year 1 当前返回 17 个待排班次；已经安排的 `LEAD_01` 不再出现在清单中。
- 本机开发服务已重新启动并确认 `http://localhost:3000/` 返回 HTTP 200，页面已在应用内打开供用户查看。

### 下一步

1. 点击已排课程编辑教室、教师、时间，并支持在总表内重新拖动。
2. 完善学生班级冲突、不可用时段和午餐／连续课时等软规则警告。

## 2026-08-10｜已排课程编辑与重新安排

### 已完成

- 已排课程卡片可直接拖到新的工作日／整点时段，服务器会重新校验并保存位置。
- 点击课程卡片可打开编辑面板，同时修改日期、开始时间、授课教师和教室。
- 可将课程退回未排清单；该操作只删除排课记录，不删除课程班次或其教师／学生班级资料。
- 冲突警告会保存在排课记录中，刷新年级总表后仍然显示。

### 本次验证

- `npm run lint` 与 `npm run build` 通过。
- API 验证：已把 `LEAD_01` 调整到 Tuesday 09:00，教师为 WAN XIN、教室为 31-05-10；重新读取 Year 1 总表后全部设置正确保留。

### 下一步

1. 完善学生班级冲突、教室容量／设施不符和课程不可用时段警告。
2. 增加午餐、连续课时、每日总时数及不同 Block 连堂等软规则。

## 2026-08-10｜教师、学生班级与教室实时警告

### 已完成

- 已将拖拽放置和编辑保存统一接入同一个警告引擎，避免不同操作得到不同判断结果。
- 已增加教师重叠、教室重叠及学生班级重叠检查；跨年级课程通过班次关联的学生班级一并检查。
- 已增加教室最低容量、Lab、Multi Projector 和 Smart Classroom 要求检查。
- 缺少教师、教室或学生班级时会显示资料不完整警告；所有警告允许保存，并持久化到总表。

### 本次验证

- `npm run lint` 与 `npm run build` 通过。
- 已建立测试重叠：`LEAD_02` 与 `LEAD_01` 在 Monday 10:00 重叠，系统同时返回并保存 `Teacher conflict`、`Student group conflict (AAA_01)`、`Multi Projector required`。

### 下一步

1. 增加午餐、连续课时、教师／学生每日总时数和 08:00 开课软警告。
2. 增加不同 Block 连堂与“同一门少数特殊课程不能同日”的规则。

## 2026-08-10｜午餐、连续课时与每日时数软规则

### 已完成

- 已增加 08:00 开课不推荐提醒，但仍允许排入总表。
- 已分别对教师和每个学生班级检查 12:00–14:00 是否至少保留一小时午餐时间。
- 已检查教师和学生班级是否出现超过 4 小时的连续课程。
- 已检查学生班级每日是否超过 6 小时、教师每日是否超过 7 小时。
- 已根据教室编号解析的 Block，提醒教师或学生班级在不同 Block 连堂。

### 本次验证

- `npm run lint` 与 `npm run build` 通过。
- 测试序列 `LEAD_01`／`LEAD_02`／`LEAD_03` 成功同时触发教师与 AAA_01 的午餐、连续课时、每日总时数和跨 Block 连堂共 8 项警告。

### 下一步

1. 实现教师和年级／学生班级不可上课时段维护，并接入实时警告。
2. 增加少数课程“同一门不同班次不能安排在同一天”的可配置规则。

## 2026-08-10｜教师与年级不可上课时段

### 已完成

- 已启用 Rules & issues 页面，可维护教师个人不可上课时段和 Year 1–3 的禁排时段。
- 每条规则可选择 Monday–Friday、整点开始和结束时间，并可随时删除。
- 排课警告引擎会检查教师不可用时段；课程关联的任一学生班级年级或课程主年级被禁排时，也会显示对应 Year 警告。

### 本次验证

- `npm run lint` 与 `npm run build` 通过。
- 已添加 WAN XIN Monday 08:00–18:00 和 Year 1 Monday 08:00–18:00 测试规则；重新保存 `LEAD_01` 后同时返回 `Teacher is unavailable at this time` 与 `Year 1 is unavailable at this time`。

### 下一步

1. 增加少数课程“同一门不同班次不能安排在同一天”的可配置规则。
2. 建立 Rules & issues 的统一问题清单，按教师、学生班级和规则类型汇总所有警告。

## 2026-08-10｜同一课程班次分散日期规则

### 已完成

- 课程 Configure 表单新增 `Keep sections on different days` 开关，仅对少数需要该限制的课程启用。
- 启用后，若同一课程已有其他班次安排在相同工作日，系统显示警告但仍允许保存。
- 规则属于课程配置，重新导入 Teaching Members 时不会被覆盖。

### 本次验证

- `npm run lint` 与 `npm run build` 通过。
- 已为 LEAD 启用该规则；重新保存 Monday 的 `LEAD_02` 后成功返回 `LEAD sections should not be scheduled on the same day`。

### 下一步

1. 建立 Rules & issues 统一问题清单，集中查看所有已排课程警告。
2. 加入候选时段功能，仅列出完全没有冲突、警告或不推荐项的时段。

## 2026-08-10｜统一排课问题清单

### 已完成

- Rules & issues 页面新增统一问题清单，集中显示所有已排课程的冲突、禁排、资料缺失、教室要求、课时负担、跨 Block 和偏好提醒。
- 每条问题显示严重程度、规则类别、课程班次、年级、教师、学生班级、教室和上课时间。
- 页面按 High、Warning、Advisory 汇总数量；这些标签仅帮助检查优先级，不会阻止保存或导出。
- 打开问题页面时会重新运行统一警告引擎，并同步更新排课记录中的警告，确保新增／删除禁排规则或调整相邻课程后不会继续显示旧结果。

### 本次验证

- `npm run lint` 与 `npm run build` 通过。
- `GET /api/issues` 已在真实开发数据验证：成功重新计算 LEAD 测试排课，并按 Conflict、Availability、Room、Workload、Travel、Course rule 和 Assignment 分类返回问题。

### 下一步

1. 加入候选时段功能，仅列出完全没有冲突、警告或不推荐项的时段及合适教室。
2. 增加按教师和按学生班级查看的个人时间表。

## 2026-08-10｜完全无警告候选时段

### 已完成

- 每张未排班次卡片新增 Find clear options，可按需检查 Monday–Friday 的所有整点时段和有效教室。
- 候选计算复用统一警告引擎，只有教师、学生班级、教室、禁排时段、午餐、连续课时、每日时数、跨 Block、课程规则和 09:00 偏好全部通过的组合才会显示。
- 08:00 因属于不推荐时段不会进入“完全无警告”候选；资料未补齐的班次也不会给出误导性建议。
- 候选面板显示时间、教室容量和设施；点击一个候选可再次进行服务器校验并直接排入总表。

### 本次验证

- `npm run lint` 与 `npm run build` 通过。
- API 反向验证：未关联学生班级的 `LEAD_04` 返回 0 个候选。
- API 正向验证：补齐教师和 CICTP_02 后，`LEAD_04` 返回 40 个完全无警告的时间／教室组合；Monday 禁排、08:00、不合午餐要求和无 Multi Projector 的教室均未出现。
- 浏览器验证：Year 1 未排清单可打开 `LEAD_04` 候选面板，显示 40 个选项及教室设施，页面控制台无错误。

### 下一步

1. 增加按教师和按学生班级查看的个人时间表。
2. 支持每周两次课程生成和安排两个独立上课记录。

## 2026-08-10｜教师与学生班级个人时间表

### 已完成

- 工作区新增 Personal timetables，可切换 Teacher 和 Student group 两种只读周课表。
- 教师课表跨 Year 1–3 汇总该教师的全部已排课程，避免教师同时参与不同年级排课时被遗漏。
- 学生班级课表依据班次—学生班级关联汇总，因此 cross-level 课程会出现在每个实际参加班级的课表中。
- 个人课表直接读取三个年级总表的同一批排课记录，不创建副本；显示课程班次、时长、教师／教室和警告数量。

### 本次验证

- `npm run lint` 与 `npm run build` 通过。
- 教师 API 验证：WAN XIN 个人课表正确返回 `LEAD_01`、`LEAD_02`、`LEAD_03` 三条已排课程及保存的警告。
- 学生班级 API 验证：AAA_01 个人课表通过班次关联返回相同三条参与课程，没有依赖课程主年级进行错误过滤。

### 下一步

1. 支持每周两次课程生成和安排两个独立上课记录。
2. 实现第 1–4 周／第 5–8 周换课的周段显示与冲突检查。

## 2026-08-10｜每周两次课程的独立课次

### 已完成

- 课程配置为 Sessions/week = 2 时，每个课程班次会在未排清单生成 Session 1 和 Session 2 两张独立卡片；已有课次不会重复出现。
- 两个课次可分别拖拽、寻找完全无警告候选、编辑和退回未排清单，并各自保留日期、时间、教室和警告。
- 同一班次的两个每周课次安排在同一天时会给出分日警告，但仍遵守系统“允许保存并提示”的总体规则。
- 年级总表、教师个人表、学生班级个人表和问题清单均显示 Session 编号，避免两条同名记录难以区分。
- 总表同一格现在会显示所有同时开始的课程，而不是只显示第一条，确保已允许保存的冲突不会被界面隐藏。
- 若第二课次已经排好，系统会拒绝直接把课程从每周两次降为一次，要求先把多余课次退回清单，避免静默丢失数据。

### 本次验证

- `npm run lint` 与 `npm run build` 通过。
- 将 LEAD 配置为每周两次后，未排 API 为 `LEAD_01`–`LEAD_18` 正确生成缺少的独立课次；已有 Session 1 的班次只出现 Session 2。
- 将 `LEAD_01 · Session 2` 临时排在周一，成功触发 `LEAD weekly sessions should be scheduled on different days`；删除测试记录后改排周二 09:00、31-05-10，保存结果为零警告。
- Year 1 总表 API 同时保留 `LEAD_01 · Session 1`（周一）和 `Session 2`（周二）。
- 已验证存在第二课次时降为每周一次会返回保护性错误，LEAD 保持每周两次配置。

### 下一步

1. 实现第 1–4 周／第 5–8 周换课的周段显示与冲突检查。
2. 设计并接入账号登录和多人协作所需的线上数据库。

## 2026-08-10｜第 1–4 周／第 5–8 周换课

### 已完成

- 课程配置新增 Teaching weeks，可选择 All weeks、Weeks 1–4 或 Weeks 5–8；重新导入 Teaching Members 不会覆盖该手工设置。
- 未排课次、年级总表、教师／学生班级个人表、候选面板和问题清单均显示 `W1–4`／`W5–8` 标签。
- 教师、教室、学生班级重叠，午餐、连续课时、每日总时数和跨 Block 检查现在只合并实际重叠的教学周段。
- All weeks 与两个半段都会重叠；Weeks 1–4 和 Weeks 5–8 互不重叠，因此换课课程可共用相同教师、学生班级、时间和教室。
- SQLite 自动升级逻辑与 Prisma 可迁移模型均加入周段字段。

### 本次验证

- `npm run lint`、`npm run build` 与 `npx prisma validate` 通过。
- 将 `BED_01 · W1–4` 和 `3DP_01 · W5–8` 设为 WAN XIN、AAA_01、Tuesday 13:00、31-05-10，两条排课均为零警告。
- 对照测试：临时把 3DP 改成 W1–4 后重新校验，正确触发 Teacher conflict、Room conflict、Student group conflict 和每日时数警告。
- 恢复 3DP 为 W5–8 并再次校验后恢复零警告；该正确配置保留在本地开发数据中供检查。

### 下一步

1. 设计并接入账号登录和多人协作所需的线上数据库。
2. 增加规则启用／停用设置，使以后新增或减少规则不需要修改页面代码。

## 2026-08-10｜可维护的政策规则开关

### 已完成

- Rules & issues 新增 Policy rule settings，可启用／停用 09:00 开课偏好、午餐一小时、连续最多 4 小时、学生每天最多 6 小时、教师每天最多 7 小时、不同 Block 连堂和每周两次分日。
- 每次切换会立即重新计算统一问题清单；候选时段也使用当前启用的规则，例如关闭 09:00 偏好后 08:00 可成为零警告候选。
- 教师、教室和学生班级重叠，禁排时段及教室硬性要求不提供关闭开关，避免误操作隐藏核心冲突。
- 规则设置持久化在 SQLite；以后新增同类政策规则可通过增加一条注册信息和判断快速扩展，并保留现有账号的选择。

### 本次验证

- `npm run lint` 与 `npm run build` 通过。
- `GET /api/rule-settings` 正确返回 7 条默认启用的政策规则。
- 关闭 Teacher maximum 7 hours/day 并重新计算后，保存警告中 `Teacher exceeds 7 teaching hours` 数量由 4 变为 0。
- 重新启用并计算后数量恢复为 4；本地规则配置已恢复为全部启用。

### 下一步

1. 实现本地管理员首次设置、账号创建和独立登录。
2. 评估线上 PostgreSQL 与实时协作部署方案，将本地原型迁移为多人共享系统。

## 2026-08-10｜本地管理员与独立账号登录

### 已完成

- 首次打开系统时显示 Create the administrator，由实际使用者设置真实管理员用户名和密码；系统不会内置默认密码。
- 密码使用 Node scrypt 加随机 salt 哈希，数据库不保存明文；登录使用随机 HttpOnly、SameSite 会话 cookie，并在 12 小时后过期。
- 管理员可在 Accounts 页面创建普通 scheduler 账号；普通账号可使用全部排课功能，但不能查看或创建账号。
- Sign out 同时删除服务器会话并清除浏览器 cookie。
- Next.js 16 `proxy.ts` 统一保护所有非认证业务 API；未登录请求无法绕过页面直接读取或修改教师、课程和排课数据。
- SQLite 自动初始化与 Prisma 模型均加入用户和会话表，业务代码继续包含面向维护者的区块注释。

### 本次验证

- `npm run lint`、`npm run build` 与 `npx prisma validate` 通过；生产构建识别 `Proxy (Middleware)` 和 5 个认证 API。
- 未登录访问 `/api/teachers` 返回 401；临时管理员建立会话后可访问业务 API并创建普通账号。
- 普通账号可访问 `/api/courses`（200），但访问账号管理返回 403。
- 普通账号退出后旧 cookie 再访问业务 API 返回 401，证明服务器会话已失效。
- 测试管理员、普通账号、会话和临时 cookie 已全部清除；`/api/auth/status` 恢复为 `setupRequired: true`，保留给用户自行设置真实管理员。

### 下一步

1. 增加账号停用和密码修改／重置能力。
2. 评估并接入线上 PostgreSQL 与多人实时同步，替代单机 SQLite。

## 2026-08-10｜多人页面自动同步

### 已完成

- 年级总表、教师／学生班级个人表和 Rules & issues 在打开期间每 5 秒从服务器读取当前视图的最新数据，无需手动刷新。
- 只轮询当前可见的数据投影，避免每个账号持续下载无关的全部课程和资料。
- 页头显示精确到秒的最后同步时间；会话过期时自动返回登录页，不继续显示可能过期的受保护数据。
- 现阶段采用适合小团队和单服务器 SQLite MVP 的短轮询；迁移线上数据库后可替换为数据库事件／WebSocket，而不改变排课数据结构。

### 本次验证

- `npm run lint` 与 `npm run build` 通过。
- 浏览器与外部 API 建立两个独立登录会话：浏览器 Year 1 初始显示 `37 ready to place`。
- 第二会话排入 `LEAD_02 · Session 2` 后，浏览器未刷新并在约 5 秒后自动显示 `36 ready to place`，页头同步时间同时更新。
- 临时排课已删除，临时管理员、会话和 cookie 已全部清除，首次管理员设置状态得到保留。

### 下一步

1. 增加并发编辑版本检查，避免两位老师几乎同时编辑同一课次时后保存者静默覆盖前者。
2. 评估并接入线上 PostgreSQL 与公网部署，支持真正的不同地点共享访问。

## 2026-08-10｜排课并发版本保护

### 已完成

- 每条排课记录新增持久化 revision；新记录从 1 开始，每次移动或编辑成功后加 1。
- 浏览器移动、编辑和退回未排清单时必须提交自己读取到的 revision。
- 若另一位老师已先保存，旧版本编辑会返回 409 并提示重新查看最新总表，不会静默覆盖对方的日期、教师或教室。
- SQLite 自动升级和 Prisma 模型均加入 revision，旧的开发排课记录自动从版本 1 开始。

### 本次验证

- `npm run lint`、`npm run build` 与 `npx prisma validate` 通过。
- 使用 `LEAD_01 · Session 1` revision 1 保存成功，响应与数据库版本升为 2。
- 再使用旧 revision 1 尝试改到另一时间，系统拒绝并返回 `This lesson was changed by another scheduler`；最新排课未被覆盖。
- 临时测试管理员、会话和 cookie 已全部清除；现有排课内容保持原时间，仅 revision 正常升为 2。

### 下一步

1. 增加账号停用和密码修改／重置能力。
2. 完成部署前审计：环境配置、PostgreSQL 迁移、备份和公网访问方案。

## 2026-08-10｜账号停用与密码维护

### 已完成

- 所有登录用户可点击页头用户名进入 My account，验证当前密码后设置新密码。
- 本人修改密码会删除该账号全部服务器会话并返回登录页，避免遗失的浏览器继续访问。
- 管理员 Accounts 页面可停用／启用普通 scheduler 账号；停用立即撤销现有会话。
- 管理员可为普通账号设置临时新密码；重置同样撤销全部现有会话。管理员账号不能在该页面被误停用或重置。

### 本次验证

- `npm run lint` 与 `npm run build` 通过。
- 普通测试账号登录后由管理员停用，旧会话访问课程 API 立即返回 401。
- 重新启用并重置密码后，原密码登录返回 401，新密码登录成功。
- 普通账号自行修改密码后旧 cookie 返回 401，自行设置的新密码登录成功。
- 临时管理员、普通账号、会话和 cookie 已全部清除，系统保留首次管理员设置状态。

### 下一步

1. 完成部署前需求与代码审计，列出仍缺少的核心功能和部署阻塞。
2. 准备 PostgreSQL 数据层和环境配置，等待确定实际托管平台后接入。

## 2026-08-10｜按教室查看个人课表

### 已完成

- Personal timetables 现在除教师和学生班级外，也可选择任一有效教室查看其跨三个年级总表的每周占用。
- 教室课表直接读取同一份排课记录，不建立另一份副本；课程块显示班次、教师、时长和当前问题数量。
- 个人课表 API 明确只接受 Teacher、StudentGroup 或 Room 三种查看维度，并拒绝无效参数。

### 本次验证

- `npm run lint`、`npm run build` 与 `npx prisma validate` 通过。
- 通过真实受保护 API 查询 `31-05-10`，正确返回 `LEAD_01` 两个课次以及教学周互不重叠的 `BED_01`、`3DP_01` 共 4 条安排。
- 无效查看维度返回 HTTP 400；临时管理员、会话和 cookie 已清除，系统恢复首次管理员设置状态。

### 下一步

1. 增加现有教室地址、容量和设施的编辑功能。
2. 增加 Excel 遗漏后的课程与班次数量人工修正。

## 2026-08-10｜教室资料编辑

### 已完成

- Rooms 清单新增 Edit，可修改现有教室的 Block-Level-Room 地址、容量、Lab、Multi Projector 和 Smart Classroom 标记。
- 地址修改后自动重新解析 Block，确保不同 Block 连堂提醒使用最新建筑位置。
- Smart Classroom 仍会自动包含 Multi Projector；重复地址返回明确冲突，错误地址格式和非正整数容量会被拒绝。
- 编辑和启用／停用共用同一路由，但以明确请求类型分支处理，既保留旧排课引用，也不会混淆资料修改与状态修改。

### 本次验证

- `npm run lint` 与 `npm run build` 通过。
- 通过真实受保护 API 将 `room-3` 临时修改为 `28-06-01`、Block 28、32 人、Lab + Smart，读取结果自动包含 Multi Projector。
- 修改为已有地址返回 HTTP 409；修改为非 Block-Level-Room 地址返回 HTTP 400。
- `room-3` 已恢复为原始 `27-03-08`、Block 27、20 人、Multi Projector；临时管理员、会话和 cookie 已清除。

### 下一步

1. 增加 Excel 遗漏后的课程与班次数量人工修正。
2. 增加未排班次搜索和教师类型／学生班级／专业筛选。

## 2026-08-10｜课程遗漏与班次数量人工修正

### 已完成

- Courses 导入面板新增 Add a missing course manually，可填写课程编号、可选 Catalog 和班次数量；新班次按 `MOD_01` 连续生成且教师留空，不伪造 Teaching Members 分配数量。
- 每门课程的 Sections 面板可修改总班数；增加时追加连续编号，减少时只移除最高编号。
- 减少班数前页面要求确认；服务器拒绝移除已排课或仍关联学生班级的班次，避免静默丢失实际排课工作。
- Teaching Members 重导只重建文件中出现的课程，保留 Excel 遗漏后人工新增的课程；如相关导入课程已经开始排课，则拒绝重导并指引使用人工修正或开启新一轮。
- 将认证 Proxy 的请求体上限由默认 10 MB 明确配置为 25 MB，使用户提供的约 16 MB Teaching Members 文件能够完整到达导入 API。

### 本次验证

- `npm run lint`、`npm run build` 与 `npx prisma validate` 通过。
- 真实 API 创建 `CODEXTEST` 3 班后扩至 5 班，正确得到 `CODEXTEST_01` 至 `_05`，且 allocation 为 0。
- 给 `_05` 关联 `AAA_01` 后缩回 3 班返回 HTTP 409；清空关联后缩减成功，只保留 `_01` 至 `_03`。
- 上传真实 `Teaching Team 2026S1 (20-2-2026).xlsx`：调整上限前复现空 HTTP 500；调整后文件完整解析，并因已有正式排课返回明确 HTTP 400 安全提示。
- 临时课程、学生班级关联、管理员、会话和 cookie 已全部清除；系统恢复首次管理员设置，正式课程仍为 52 门。

### 下一步

1. 增加未排班次搜索和教师类型／学生班级／专业筛选。
2. 增加实际教师班数与 Teaching Members 分配不一致提醒。

## 2026-08-10｜未排班次搜索、筛选与 PT 置顶

### 已完成

- 未排班次 API 现在返回 FT/PT，并在数据库查询层把 PT 教师的班次排在 FT 和未分配教师之前。
- PT 班次使用琥珀色卡片和 `PT priority` 标记，避免只依赖排序位置判断。
- 年级总表左侧新增即时关键字搜索，可匹配课程班次、教师、教师类型、学生班级和专业。
- 新增 FT/PT、学生班级和专业精确筛选，可组合使用，并显示“筛选结果数 / 当前年级总数”和 Clear filters。
- 所有筛选都在当前年级已加载的数据上即时运行，不会增加服务器请求或改变课表。

### 本次验证

- `npm run lint` 与 `npm run build` 通过。
- 真实 API 临时建立 `TRAYTEST_01`（PT + AAA_01）和 `TRAYTEST_02`（FT + CICTP_02）；Year 1 返回 39 项，PT 项位于索引 0，首个非 PT 位于索引 1，证明 PT 全部置顶。
- 响应正确包含 `staffType`、教师姓名和学生班级，满足客户端课程／教师／类型／班级／专业组合筛选的输入要求。
- 临时课程、班次分配、管理员、会话和 cookie 已全部清除。

### 下一步

1. 增加实际教师班数与 Teaching Members 分配不一致提醒。
2. 实现带自动备份和恢复能力的安全“新一轮排课”。

## 2026-08-10｜Teaching Members 教师分配偏差提醒

### 已完成

- 系统按课程和教师比较 Teaching Members 的预期班数与当前课程班次的实际教师数量。
- Courses 清单直接显示每门课程有多少项 allocation mismatch；打开 Sections 后列出具体教师的 expected / currently 数量。
- 修改任一班次教师后，保存响应立即返回最新偏差并在页面状态栏提示；偏差只提醒、不阻止保存。
- 实际使用未出现在 Excel 分配中的替代教师也会纳入偏差；没有 Teaching Members 基线的人工课程不会被误报。
- 分配详情使用独立只读 API，保持现有 Sections API 格式稳定，便于以后单独刷新或扩展。

### 本次验证

- `npm run lint`、`npm run build` 与 `npx prisma validate` 通过。
- LEAD 基线正确识别 4 项偏差，包括 WAN XIN expected 1 / currently 4，以及三位 expected 1 / currently 0 的教师。
- 临时把 `LEAD_04` 从 ONG HOON JIN 改给 WAN XIN 后，保存响应立即变为 5 项偏差，且准确显示 ONG 1/0、WAN XIN 1/5。
- 恢复 `LEAD_04` 后偏差回到原有 4 项；ONG HOON JIN 实际班数恢复为 1，WAN XIN 恢复为 4，临时管理员、会话和 cookie 已清除。

### 下一步

1. 实现带多次确认、自动备份和恢复能力的安全“新一轮排课”。
2. 完成警告刷新一致性和全量 MVP 回归审计。

## 2026-08-10｜安全新一轮排课与应急恢复

### 已完成

- 所有登录账号均可进入 New cycle & recovery；页面明确显示当前课程、班次和排课数量。
- 开始新一轮前必须完成两个确认勾选并准确输入 `START NEW CYCLE`；服务器也独立校验固定短语，无法通过绕过页面误触清空。
- 清空前在同一 SQLite 事务内保存唯一一份应急快照，再清除课程、Teaching Members 分配、生成班次、班次学生关联和排课记录；任一步失败则全部回滚。
- 教师、教室、学生班级、教师／年级禁排、规则设置、账号和登录会话不进入清空范围并完整保留。
- 可查看最新应急备份的时间与数量，并在确认替换当前循环后输入 `RESTORE LAST BACKUP` 恢复；恢复按外键父子顺序在单一事务内执行。
- 只保留最新应急快照，不提供日常版本历史，符合首版范围。

### 本次验证

- `npm run lint`、`npm run build` 与 `npx prisma validate` 通过；生产构建包含受保护的 `/api/cycle`。
- 真实数据库 API 状态返回 52 门课程、374 班次、8 条排课、无备份；错误确认短语返回 HTTP 400，且四项计数完全不变。
- 因真实清空属于高风险破坏性操作，成功清空／恢复在 SQLite 在线备份的隔离副本验证：52/374/8 清为 0/0/0，同时备份记录 52/374/8；恢复后回到 52/374/8。
- 隔离副本恢复前后五张循环表 SHA-256 均为 `277a3e215ca592d61c4a4fb8ef0e2272558358ffa21a65f6f1671268ac0e9cf7`；教师 86、教室 3、学生班级 4、教师禁排 1、年级禁排 1、规则 7 均保持，`foreign_key_check` 无错误。
- 安全基线审计同时发现并清除了先前测试命令遗留的 5 个孤立临时班次；正式数据未受影响，清理后为 374 个有效班次、6 条学生班级关联、8 条排课且无孤立外键。
- 所有隔离数据库、额外安全备份、临时管理员、会话和 cookie 已清除；真实数据库未执行新一轮清空，仍为 52/374/8，系统恢复首次管理员设置状态。

### 下一步

1. 统一刷新年级总表和个人课表中的现有警告，避免相邻课程或规则变化后卡片显示旧结果。
2. 完成中文 MVP 验收矩阵和全量回归验证。

## 2026-08-10｜排课警告统一刷新与课程时长同步

### 已完成

- 新增统一警告刷新器；任何课程放置、移动、删除，班次教师／学生班级修改，课程要求修改，教室资料／状态修改，禁排时段增删或政策规则开关后，都会重算所有已排课程的保存警告。
- 年级总表和教师／学生班级／教室个人课表继续读取轻量的保存结果，但现在所有受影响卡片会在一次修改后同时更新，不必先打开集中问题页。
- 集中问题清单复用同一刷新器，不再维护另一套重算循环。
- 修改课程共享时长会同步更新其所有已排课次的 duration，并增加 revision；已打开的旧编辑器因此会收到 409，不会用旧时长静默覆盖。
- 若增加时长会使任一现有课次超过 18:00，课程设置保存会明确拒绝并提示先移动晚课。
- 从应急备份恢复后，会按当前保留的教室、禁排和规则重新计算警告，而不是盲目沿用备份时的旧提示。

### 本次验证

- `npm run lint`、`npm run build` 与 `npx prisma validate` 通过。
- Year 1 基线包含 4 条 `Teacher exceeds 7 teaching hours`；关闭对应规则后直接读取年级总表变为 0，重新启用后恢复为 4，证明无需打开问题页即可统一刷新。
- 临时 `DURATIONTEST_01` 以 2 小时排在 Monday 16:00；增至 3 小时返回 HTTP 400 和“先移动晚课”提示。
- 把临时课移至 14:00 后增至 3 小时成功；年级总表同时显示 duration 3、revision 3，使用变更前 revision 2 保存返回 HTTP 409。
- 临时课程和排课已在启用 foreign keys 的清理事务中完整级联删除；正式数据恢复为 52 门课程、374 班次、8 条排课、0 孤儿，规则全部恢复，临时管理员与会话已清除。

### 下一步

1. 建立中文 MVP 验收矩阵，逐条绑定功能、API、界面和验证证据。
2. 执行最终全量回归，确认本地 MVP 是否只剩公网托管与共享数据库决策。

## 2026-08-10｜教师与学生班级资料修正

### 已完成

- 教师清单新增 Edit 操作，可在保留原有教师编号的情况下修正姓名和 FT／PT 类型；既有 Teaching Members 分配、禁排时段和排课关联不会丢失。
- 学生班级清单新增 Edit 操作，可修正班级编号、所属年级和专业；既有班次学生关联继续使用同一班级记录。
- 新增教师和修改教师使用同一套大写规范及重名保护；新增学生班级和修改班级也使用相同的年级范围与班级编号唯一性校验。
- 修改已关联的学生班级资料后，系统立即重算总表中保存的冲突与工作量警告。
- 三类基础资料表单均清楚区分 New 与 Edit 状态，保存或切换页面后会清除编辑对象，避免下一次新增误覆盖旧记录。

### 本次验证

- `npm run lint`、`npm run build` 与 `npx prisma validate` 通过；生产构建包含新的 `/api/student-groups/[id]` 路由。
- 真实 API 新增临时教师后把姓名改为 `CODEX EDIT TEACHER UPDATED`、类型改为 PT，返回 HTTP 200 且教师清单正确读回。
- 真实 API 新增临时学生班级后把编号改为 `CODEX_EDIT_02`、年级改为 3、专业改为 `UPDATED`，返回 HTTP 200 且班级清单正确读回。
- 把临时教师改成既有教师 `WAN XIN` 返回 HTTP 409；把临时班级年级改为 4 返回 HTTP 400。
- 临时教师、学生班级、管理员、登录会话和 cookie 已完整清除；系统回到首次管理员设置状态，`foreign_key_check` 为 0。

### 下一步

1. 在集中问题清单增加一键定位到对应年级和课次的操作。
2. 把课程时长输入严格收窄为已确认的 2–4 小时，并加入边界回归。

## 2026-08-10｜集中问题一键定位课次

### 已完成

- Current timetable issues 的每一条问题新增 `Open lesson` 操作。
- 点击后会重新读取问题所属年级的最新总表与未排课清单，以最新 revision 找到对应课次，避免打开问题清单中的旧资料。
- 系统自动切换到正确的年级总表、打开原有课次编辑器，并滚动到可视区域；排课老师可直接调整星期、整点开始时间、教师和教室，或把课程退回未排区。
- 若其他账号已经移除了该课次，系统不会打开错误课程，而是提示刷新问题清单移除过期项目。
- 课次编辑器新增稳定的页面定位点，仅用于界面导航，不改变原有排课保存或 revision 冲突保护。

### 本次验证

- `npm run lint`、`npm run build` 与 `npx prisma validate` 通过。
- 在真实浏览器界面载入现有 46 条问题，确认每条均显示 `Open lesson`。
- 点击第一条问题后，界面从 Rules & issues 自动切换到 Year 1，打开 `LEAD_01 · Session 1`，并正确带入 Monday 09:00、WAN XIN 与 31-05-10。
- 浏览器确认课次编辑器可见且位于当前视口顶部 16px，无需再在总表中手动寻找课程。
- 临时管理员和登录会话已清除；正式数据保持 52 门课程、374 班次、8 条排课，`foreign_key_check` 为 0，浏览器恢复首次管理员设置页面并保留给用户查看。

### 下一步

1. 把课程时长输入严格收窄为已确认的 2–4 小时，并加入边界回归。
2. 建立中文 MVP 验收矩阵并执行全量回归。

## 2026-08-10｜课程时长 2–4 小时边界

### 已完成

- 课程设置页面的 Duration 输入由最少 1 小时改为最少 2、最多 4 小时，浏览器会在提交前阻止范围外输入。
- 课程设置 API 独立执行相同的 2–4 整数小时校验，即使绕过网页直接请求也不能保存 1 小时或 5 小时课程。
- 数据库共享函数再加入一层相同保护，未来维护脚本直接调用业务函数时也不会绕过已确认的课程时长规则。
- 超出范围时返回明确的英文修正提示，同时保留每周次数、年级和教室容量的原有验证。

### 本次验证

- `npm run lint`、`npm run build` 与 `npx prisma validate` 通过。
- 真实 API 对临时课程提交 1 小时时返回 HTTP 400；提交 2 小时时返回 HTTP 200。
- 同一临时课程提交 4 小时时返回 HTTP 200；提交 5 小时时返回 HTTP 400。
- 临时课程及其生成班次、管理员、登录会话和 cookie 已完整清除；正式数据保持 52 门课程、374 班次、8 条排课，`foreign_key_check` 为 0。

### 下一步

1. 建立中文 MVP 验收矩阵，逐条映射确认需求、界面、API 与证据。
2. 执行认证、基础资料、排课、冲突规则、多人刷新和新周期恢复的最终全量回归。

## 2026-08-10｜零分配行保留教师资料

### 已完成

- 修正 Teaching Members 导入：`# of grps teaching = 0` 仍不会建立课程—教师分配或班次，但该行的教师姓名与 FT／PT 类型会进入教师清单。
- 零值行现在与正数行一样校验 Mod、Lecturer、Staff Type 和非负整数分配数，格式错误不会因为数值为 0 而被静默跳过。
- 只有至少一条正数分配时才允许完成导入，防止只有教师零值行的文件创建一个没有课程班次的新周期。
- 数据库导入函数明确把教师主数据收集与正数课程分配拆成两个步骤，后续规则可以直接理解“保留教师、跳过分配”的业务含义。
- 新增可选 `TIMETABLING_DATABASE_PATH`，只用于隔离回归或未来受控运行环境；正常本机运行仍使用 `web/data/timetabling.db`。
- 根据原始 Excel 补回正式本机数据库旧导入遗漏的 `DONOVAN TAN WEI WEN (PT)`，其当前分配班数为 0。

### 本次验证

- 使用电子表格只读分析和项目实际 SheetJS 解析器交叉核对原文件：213 行、29 个零值行、87 位全部教师、86 位有正数分配；唯一全零教师为 `DONOVAN TAN WEI WEN (PT)`。
- `npm run lint`、`npm run build` 与 `npx prisma validate` 通过。
- 在独立临时 SQLite 的生产实例真实上传原 Excel，返回 52 门课程、87 位教师、184 条正数分配、374 个班次、29 个忽略的零分配行。
- 隔离数据库确认 DONOVAN 存在且分配数为 0；全部分配表中没有 0 值记录，`foreign_key_check` 为 0。
- 隔离服务器和数据库已删除；正式数据库现为 87 位教师、52 门课程、374 个班次、8 条排课，用户与会话均为 0，`foreign_key_check` 为 0。

### 下一步

1. 修正集中问题严重程度与 PRD 颜色标准的偏差。
2. 建立中文 MVP 验收矩阵并执行全量回归。

## 2026-08-10｜问题严重程度与课表颜色统一

### 已完成

- 集中问题清单按 PRD 重新分类：时间冲突、禁排、容量设施、午餐、连续超过 4 小时和同班次每周两次排在同日均为红色 High。
- 学生每日超过 6 小时和教师每日超过 7 小时保留为黄色 Warning；08:00、跨 Block 连堂和教师／班级／教室待补为蓝色 Advisory。
- 年级总表和教师／学生班级／教室个人课表 API 为每条课次返回 `warningSeverity`，取该课次全部问题中的最高级别。
- 年级总表卡片、个人课表卡片和个人课表状态摘要均使用与集中问题清单相同的红／黄／蓝标准，不再把所有问题统一显示为黄色。
- 卡片仍显示完整问题文字，颜色只帮助快速扫描，不改变“允许保存但必须明确提示”的既定行为。

### 本次验证

- `npm run lint`、`npm run build` 与 `npx prisma validate` 通过。
- 真实问题 API 当前 46 条问题分类为 30 条 High、8 条 Warning、8 条 Advisory。
- 4 条 `not assigned` 全部为 Advisory；6 条午餐、6 条连续超过 4 小时、5 条同日两次全部为 High；8 条每日 6／7 小时上限全部为 Warning。
- Year 1 API 中同时包含 High、Warning 和无问题三种课次；例如 `LEAD_01 · Session 1` 为 High，只有每日上限问题的 `LEAD_01 · Session 2` 为 Warning，互不重叠教学周的 BED／3DP 为无问题。
- 临时管理员、会话、cookie 和 API 结果文件已清除；正式数据保持 87 位教师、52 门课程、374 班次、8 条排课，`foreign_key_check` 为 0。

### 下一步

1. 建立中文 MVP 验收矩阵，记录已通过项与尚未完成的公网部署项。
2. 执行最终全量回归并确认本地核心是否还有功能缺口。

## 2026-08-10｜中文 MVP 验收矩阵与统一回归

### 已完成

- 新增 `docs/MVP_ACCEPTANCE.md`，逐条映射 PRD 的 8 项验收标准、当前界面/API 实现、验证证据和状态。
- 根据用户已确认“课程属于某年级就进入该年级总表”的决定，把 PRD 中容易误解的“每个班次选主年级”修正为“每门课程选主年级”。
- 验收结论明确区分本地功能和正式线上环境，没有把尚未部署的公网访问、共享 PostgreSQL 或跨设备协作写成已完成。
- 矩阵识别并公开记录任意教学周区间仍是本地功能缺口；当前 All、1–4、5–8 只覆盖已知换课案例。

### 本次验证

- 统一只读 API 回归覆盖未登录保护、管理员设置、4 类基础资料、禁排、问题、规则、新周期、账号、3 张年级已排/未排课表、教师/班级/教室个人课表、班次、分配偏差和候选位置；未登录为 401，其余 23 项全部为 HTTP 200/201。
- 首轮候选位置测试因测试脚本未按真实界面拆分组合 id 而返回 400；复核确认前端拖放与候选功能均已拆分，修正测试参数后返回 200，产品代码无需修改。
- SQLite `integrity_check` 为 `ok`，`foreign_key_check` 为 0；孤儿、非法 2–4 小时时长、越界课次、重复 occurrence、Smart/Multi 关系错误和非法警告 JSON 均为 0。
- 正式数据为 87 位教师、52 门课程、374 班次、6 个班次-学生关联、8 个已排课次；临时管理员、会话、cookie 和回归脚本已清除。

### 下一步

1. 把教学周从三个固定选项扩展为任意起止周区间，并验证边界重叠。
2. 更新验收矩阵后进入公网托管与生产数据库的用户决策点。

## 2026-08-10｜任意教学周起止范围

### 已完成

- 课程配置把固定的 All weeks／Weeks 1–4／Weeks 5–8 下拉框改为可选的 Start week 与 End week；两项均留空代表全部教学周。
- API 要求起止周必须同时填写、均为正整数且开始周不晚于结束周；不完整或倒序的范围会返回清楚的校验错误。
- 数据库新增 `week_start` 与 `week_end`，启动时自动迁移旧数据；原 W1–4 与 W5–8 记录会分别回填为 1/4 与 5/8。
- 年级总表、未排清单、个人课表、候选位置和集中问题清单统一显示任意 `W起–止` 标签。
- 教师、教室、学生班级、午餐、连续时数、每日时数和跨 Block 检查统一使用闭区间教学周重叠判断；全部周仍与任何范围重叠。
- 新周期应急备份新增教学周起止字段；恢复旧格式备份时仍会从旧固定模式推导范围，避免升级后无法恢复。
- 更新中文 MVP 验收矩阵：本地核心功能由“有条件通过”调整为“通过”，正式线上验收仍等待公网托管和共享数据库。

### 本次验证

- `npm run lint`、`npm run build` 与 `npx prisma validate` 通过。
- 真实受保护 API 对只填开始周和 W7–6 分别返回 HTTP 400；保存 BED W3–6 返回 HTTP 200。
- BED W3–6 与 3DP W5–8 共用教师、学生班级、时间和教室时，正确产生教师、教室和学生班级冲突；恢复 BED W1–4 后两条安排重新为零冲突。
- 隔离生产数据库执行“开始新一轮”后从 52/374/8 清空为 0/0/0，再恢复为 52/374/8；BED 1–4 与 3DP 5–8 完整保留，`foreign_key_check` 为 0。
- 测试管理员、会话、cookie、隔离数据库和服务器均已删除；正式数据库仍为 87 位教师、52 门课程、374 个班次、8 条排课，用户、会话和应急备份均为 0。

### 下一步

1. 由项目负责人选择公网托管与生产 PostgreSQL 方案。
2. 完成数据访问层迁移，并在共享环境执行双账号、跨浏览器协作验收。

## 2026-08-10｜部署前 Prisma 数据模型对齐

### 已完成

- 对照 `database.ts` 中实际运行的 SQLite 建表语句，审计早期 Prisma 规划稿与真实数据库的偏差。
- 为全部模型补齐 snake_case 表名与字段映射，使生成的迁移使用现有的 `app_users`、`course_sections`、`scheduled_lessons` 等真实名称。
- 把 Catalog 归回 Course，把课程时长改为可空以支持“导入后待配置”，并移除 CourseSection 上已经废弃的主年级字段。
- ScheduledLesson 改为真实的必填日期/时间字段，补回 `warnings_json`，并移除实际保存在 Course 上的教学周字段。
- TeachingAllocation、禁排、规则和应急备份模型删除实际表中不存在的时间戳或多余字段，并对齐外键删除策略。
- 更新技术架构说明，明确当前 API 仍使用同步 SQLite 查询；未来 PostgreSQL 迁移需要改写数据访问层，不能只切换 Prisma provider。

### 本次验证

- `npx prisma format`、`npx prisma validate`、`prisma generate` 与 `npm run db:check` 全部通过。
- 从空数据库生成的迁移 SQL 使用真实表名与字段名，包括 `duration_hours`、`warnings_json`、`week_start` 和 `week_end`。
- `npm run lint` 与 `npm run build` 通过，全部页面和 28 个 API Route Handler 正常生成。
- 此调整只修正迁移模型，不改变当前 SQLite 运行时或正式本机数据。

### 下一步

1. 根据当前系统规模比较“单实例 + SQLite 持久卷”和“托管 PostgreSQL”两条公网部署路线。
2. 由项目负责人确认路线后，补齐部署配置、备份和共享环境验收。

## 2026-08-10｜公网部署路线比较

### 已完成

- 新增 `docs/DEPLOYMENT_DECISION.md`，按当前少量账号、低频排课和现有同步 SQLite 数据访问评估两条公网路线。
- 推荐首轮采用 Railway 单个 Next.js 服务 + SQLite 持久卷：老师仍可从任何地点登录同一公网系统，同时避免立即重写全部查询。
- 记录持久卷单实例、发布短暂停机和卷删除风险，并把每日/每周备份、外部副本及实际恢复演练列为上线条件。
- 记录 PostgreSQL 路线必须改写全部同步查询和 SQLite 专有语法，不能只更改连接字符串或 Prisma provider。
- 明确 Vercel 的临时文件系统不能持久保存 SQLite；如果选择 Vercel，必须先接入 Neon、Prisma Postgres 等外部数据库。
- 没有创建任何云资源、上传本机数据或产生费用，等待项目负责人确认路线。

### 本次验证

- 对照 Railway 官方 Next.js、Volumes 与 Backups 文档，确认其卷适用于 SQLite，并支持手动和每日/每周/月度备份。
- 对照 Railway 官方限制，确认挂载卷的服务不能使用副本，重新部署会有短暂停机。
- 对照 Vercel 官方说明，确认 SQLite 不能依赖其 serverless 本地文件系统持久化。
- 对照 Prisma 官方 PostgreSQL 文档，确认 serverless PostgreSQL 需要连接池/适配器，并需要独立的运行时与迁移连接配置。

### 下一步

1. 由项目负责人确认 Railway + SQLite（推荐）或直接 PostgreSQL。
2. 确认后立即完成对应部署配置、备份策略和共享环境验收。

## 2026-08-10｜干净生产启动与健康检查

### 已完成

- 修正数据库启动流程：演示教师、学生班级和教室只在开发模式生成；生产环境第一次启动只建立空表，不再混入假资料。
- 新增公开 `GET /api/health`，真实执行一次数据库查询后只返回 `status: ok` 或 HTTP 503，不泄露文件路径、资料数量或错误详情。
- 中央认证代理只豁免精确的 `/api/health` 路径；其他业务 API 仍必须登录，避免用相似路径绕过保护。
- 更新技术架构、MVP 验收矩阵和部署决策文档，说明干净生产启动与健康检查行为。

### 本次验证

- `npm run lint`、`npm run build` 与 `npx prisma validate` 通过；构建成功生成 29 个 API Route Handler。
- 在独立空 SQLite 上启动真实生产实例，`GET /api/health` 返回 HTTP 200 与 `{"status":"ok"}`。
- 同一实例未登录访问教师 API 返回 HTTP 401，确认健康检查豁免没有扩大认证范围。
- 隔离生产数据库中的教师、学生班级、教室、课程和账号数量全部为 0，`foreign_key_check` 为 0。
- 隔离服务器、数据库和响应文件已删除；正式本机数据库没有被测试触碰。

### 下一步

1. 由项目负责人确认 Railway + SQLite（推荐）或直接 PostgreSQL。
2. 选择 Railway 后补充 standalone 配置、持久卷路径说明与部署后恢复验收。

## 2026-08-10｜自包含 standalone 生产构建

### 已完成

- 启用 Next.js `output: standalone`，生成可由长期运行 Node 托管平台直接启动的自包含服务器。
- 新增 `scripts/prepare-standalone.mjs`；生产构建后自动复制 `public/` 与 `.next/static/`，避免只部署 standalone 服务器时丢失图片、CSS 或浏览器脚本。
- 新增 `npm run start:standalone`，保留原 `npm start` 供现有本机检查使用；托管环境可通过 `PORT`、`HOSTNAME` 和 `TIMETABLING_DATABASE_PATH` 注入运行配置。
- 脚本对不存在的 public 目录安全跳过，并只忽略明确的 `ENOENT`；其他文件系统错误会中止构建，避免生成看似成功但不完整的部署包。
- 更新架构与部署决策文档，记录 standalone 产物的构建和启动方式。

### 本次验证

- `npm run lint`、启用资源复制后的 `npm run build` 与 `npx prisma validate` 全部通过。
- 构建产物包含 `server.js`、`better-sqlite3` 的 macOS/Linux 原生模块、`public/window.svg` 和 `.next/static`。
- 用独立空数据库真实启动 `npm run start:standalone`：健康检查、首页、public SVG 和编译 CSS 均返回 HTTP 200。
- 同一 standalone 实例未登录访问教师 API 返回 HTTP 401；教师、教室和账号数量均为 0，`foreign_key_check` 为 0。
- 隔离服务器、数据库与响应文件已删除；正式本机数据库未被测试触碰。

### 下一步

1. 由项目负责人确认 Railway + SQLite（推荐）或直接 PostgreSQL。
2. 确认 Railway 后增加平台配置清单并执行真实持久卷备份恢复验收。

## 2026-08-10｜公网登录与上传安全加固

### 已完成

- 会话 Cookie 从 SameSite=Lax 收紧为 SameSite=Strict，并增加 High priority；生产环境继续要求 Secure 和 HttpOnly。
- 新增进程级登录失败限流：同一来源地址与用户名组合 15 分钟内失败 5 次后暂停 15 分钟；同一来源轮换用户名累计 25 次也会暂停。
- 限流同时避免共享办公室误锁：一个用户名达到 5 次不会立即阻止同一地址尝试另一个用户名；成功登录会清除相关失败记录。
- 登录 API 对非法 JSON 返回可控的 HTTP 400；限流返回 HTTP 429、`Retry-After` 和统一错误，不暴露账号是否存在。
- 全站新增 HSTS、`nosniff`、禁止 iframe、无 Referrer 和浏览器权限限制；所有 API 禁止浏览器或共享代理缓存。
- Teaching Members 上传增加 20 MB 文件上限和 5,000 行上限，在建立额外内存副本或进入数据库导入前拒绝异常输入。
- 更新技术架构文档，记录密码、会话、限流、响应头、上传和 HTTPS 的上线安全基线。

### 本次验证

- `npm run lint`、包含 standalone 资源整理的 `npm run build` 与 `npx prisma validate` 全部通过。
- 隔离生产实例实际响应包含 Secure、HttpOnly、SameSite=Strict、Priority=High Cookie，以及 HSTS、DENY、nosniff、no-referrer、Permissions-Policy 和 API `no-store`。
- 同一来源/用户名的错误登录前 5 次返回 HTTP 401，第 6 次返回 HTTP 429 与 `Retry-After: 900`；非法 JSON 返回 HTTP 400。
- 同一来源改用另一个用户名返回正常的 HTTP 401 而非 429；有效会话访问教师 API 返回 HTTP 200。
- 20 MB + 1 字节临时 `.xlsx` 返回 HTTP 413；5,001 行有效格式工作簿返回 HTTP 400，均未建立课程或教师。
- 隔离账号、会话、工作簿、数据库、响应文件和服务器已删除；正式本机数据库未被测试触碰。

### 下一步

1. 由项目负责人确认 Railway + SQLite（推荐）或直接 PostgreSQL。
2. 确认后把安全头、健康检查、持久卷和备份要求写入实际托管配置并做公网验收。

## 2026-08-10｜课表课程按时长跨小时显示

### 已完成

- 年级总表和教师／学生班级／教室个人课表共用新的周课表网格；课程卡片会按课程时长覆盖对应的小时轨道，不再只出现在开始时间的一个格子里。
- 课程卡片明确显示开始和结束时间；例如 3 小时的 `LEAD_01` 从 09:00 覆盖至 12:00，并显示 `09:00–12:00`。
- 同一天存在允许保存的时间冲突时，重叠课程会分到不同的横向通道；该天栏位按需要扩宽并允许横向滚动，避免课程卡片互相遮挡。
- 保留原有拖放排课、拖动已排课程和点击课程打开编辑器的操作；空白小时格仍是独立的放置目标。
- 新增的布局、重叠判断和交互区块均补充了面向业务的易读注释。

### 本次验证

- `npm run lint`、`npm run build` 与 `npx prisma validate` 通过。
- 浏览器实测年级总表中的 `LEAD_01 · Session 1`：开始时间 09:00、时长 3 小时、卡片高度 210px，正确覆盖 3 个 72px 小时轨道并在 12:00 结束。
- 同一时段的冲突课程被分成独立通道，卡片宽度保持可读且互不覆盖；点击 `LEAD_01` 仍能打开课程编辑器。
- 教师个人课表中同一条 `LEAD_01` 也以 3 小时高度显示，确认两个课表视图使用一致的时长布局。
- 浏览器验证所用临时管理员和会话已清除；正式课程、教师、班次和排课资料未作修改。

### 下一步

1. 继续处理公网部署路线确认；如先完善本地体验，则优先补充课表横向滚动提示和更多屏幕尺寸的可用性检查。
2. 在后续每个排课界面功能中继续复用统一周课表网格，避免年级表与个人表出现不同显示逻辑。

## 2026-08-10｜年级总表卡片补齐学生班级

### 已完成

- 已排课次 API 现在随课程、教师和教室一并返回该班次关联的全部学生班级编号，年级总表不再需要额外请求才能展示完整资源。
- 年级总表课程卡片在教师与教室之间显示一个或多个学生班级；尚未关联时明确显示 `Student group pending`。
- 新增排课和编辑排课的响应也使用相同资料结构，因此拖入、保存或移动后不会短暂丢失学生班级，随后自动同步的结果也保持一致。
- 教师、学生班级和教室个人课表继续共用同一个排课资料类型，为后续各视图复用班级信息保留一致数据来源。
- 更新中文 MVP 验收矩阵，在第 4 项补充总表卡片完整显示教师、学生班级与教室的浏览器证据。

### 本次验证

- `npm run lint`、`npm run build` 与 `npx prisma validate` 通过。
- 浏览器实测 `LEAD_01 · Session 1` 卡片同时显示 `WAN XIN`、`AAA_01` 和 `31-05-10`，满足 PRD 对总表默认资料的要求。
- 同一张卡仍正确显示 `09:00–12:00` 并保持 210px 的 3 小时跨格高度；点击后可正常打开原课程编辑器。
- 临时管理员和会话已清除；正式数据库保持 87 位教师、52 门课程、374 个班次和 8 条排课，`foreign_key_check` 为 0。

### 下一步

1. 改善冲突较多时的横向滚动可发现性，并检查常见电脑窗口宽度下的课表可读性。
2. 完成本地 UI 可用性修正后，回到公网托管路线的项目负责人决策点。

## 2026-08-10｜课表横向浏览与固定时间列

### 已完成

- 年级总表和个人课表顶部新增持续可见的横向浏览说明，以及 `Left`／`Right` 按钮；用户不需要滚到 18:00 才能发现底部滚动条。
- 左右按钮每次移动约四分之三个可见宽度并保留部分上下文，仍可使用触控板或原生横向滚动条精细移动。
- 08:00–17:00 时间标签列改为横向滚动时固定显示，浏览到较后面的星期仍能直接判断课程所在小时。
- 修正年级总表栏位的最小宽度约束：冲突课程导致网格变宽时，由课表自身滚动，不再把整个网页撑出屏幕。
- 新增的滚动控制、宽度约束和固定列均保留原有课程拖放、跨小时显示和点击编辑结构，并补充易读的业务注释。

### 本次验证

- `npm run lint`、`npm run build` 与 `npx prisma validate` 通过。
- 在 1280px 浏览器窗口中，网页宽度与视口均为 1280px、页面横向位置保持 0；课表容器为 698px 可见宽度和 1454px 内容宽度，证明溢出被正确限制在课表内部。
- 点击 `Right` 后课表内部横向移动 523.5px；09:00 时间标签移动前后横坐标均为 541px，计算样式为 `sticky`。
- 横向移动后 `LEAD_01 · Session 2` 仍保持 210px 的 3 小时高度，点击后成功打开编辑器。
- 临时管理员和会话已清除；正式数据库保持 87 位教师、52 门课程、374 个班次和 8 条排课，`foreign_key_check` 为 0。

### 下一步

1. 本地 MVP 核心与当前已发现的课表 UI 缺口均已补齐；回到公网托管路线选择。
2. 项目负责人确认 Railway + SQLite 或 PostgreSQL 后，完成实际平台配置、备份恢复和双账号跨浏览器验收。

## 2026-08-10｜Railway + SQLite 持久卷部署配置

### 已完成

- 项目负责人已确认 Railway + SQLite 路线；更新部署决策与架构文档，不再把公网数据库方案标记为待定。
- 新增 `web/railway.json`，固定使用 Railpack 执行生产构建、standalone 服务器启动、`/api/health` 健康检查和失败重启策略。
- 数据库路径优先读取明确配置的 `TIMETABLING_DATABASE_PATH`，否则自动使用 Railway 提供的 `RAILWAY_VOLUME_MOUNT_PATH/timetabling.db`，本机开发仍回退到 `web/data/timetabling.db`。
- 新增生产启动前存储检查：Railway 环境没有持久卷时直接拒绝启动；有卷时先确认目录可建立且可写，避免正式资料误存到部署容器的临时文件系统。
- `npm start` 和 `npm run start:standalone` 统一经过存储检查后启动 standalone 服务器，并声明当前 Next.js 构建所需的 Node.js 最低版本。
- 新增中文 `docs/RAILWAY_DEPLOYMENT.md`，记录仓库根目录、单实例、`/data` 挂载点、首次上线、平台备份、恢复演练与双账号验收步骤。
- 本阶段只使用独立临时数据库验证，没有上传或修改本机正式排课数据库，也尚未创建会产生费用的 Railway 云资源。

### 本次验证

- `npm run lint`、`npm run build` 与 `npx prisma validate` 全部通过。
- 模拟 Railway 且没有卷时，启动检查按预期以清晰错误中止；提供自动卷路径或明确数据库路径时均确认目录可写。
- 使用隔离持久卷启动真实 production standalone 服务，健康检查返回 HTTP 200，并成功建立临时管理员和学生班级 `PERSIST_01`。
- 停止服务后使用同一个卷重新启动；原会话仍可认证，`PERSIST_01` 仍可读取，确认 SQLite 数据与会话跨应用重启保留。
- 隔离数据库的外键检查为 0；临时生产服务、临时卷和测试 Cookie 均已删除，正式数据库未被触碰。

### 下一步

1. 在 Railway 连接 GitHub 仓库，设置服务 Root Directory 为 `/web`，挂载 `/data` 持久卷并生成公网域名。
2. 完成公网 HTTPS、安全 Cookie、干净首次启动和真实 Teaching Members 导入验收。
3. 启用 Daily、Weekly 和手工备份，实际完成一次替代卷恢复演练。
4. 建立两个验收账号，用两个浏览器验证 5 秒同步与 revision 防覆盖，再清除测试资料。

## 2026-08-10｜可重复执行的公网部署验收

### 已完成

- 新增 `npm run verify:deployment -- https://部署域名`，把每次上线都必须执行的公网基础检查收敛为一个命令。
- 默认只读检查应用与 SQLite 健康状态、HSTS 等安全响应头、API 禁止缓存、公开认证状态，以及未登录访问教师资料必须返回 HTTP 401。
- 非 localhost 地址严格要求 HTTPS，防止把未加密的临时网址误当成可交付环境；每个网络请求有 15 秒超时，部署或 DNS 故障时不会无限等待。
- 可通过 `TIMETABLING_SMOKE_USERNAME` 与 `TIMETABLING_SMOKE_PASSWORD` 临时增加真实账号检查；脚本验证 Secure、HttpOnly、SameSite=Strict Cookie 和认证后的业务 API，并自动退出该次会话。
- 账号与密码必须同时提供，脚本不会输出或保存密码；即使 Cookie 或认证 API 检查失败，也会先尝试注销已建立的短期会话。
- 更新中文 Railway 部署清单和技术架构，记录无账号与有账号两种命令，并明确自动脚本不能替代双浏览器协作和平台备份恢复演练。

### 本次验证

- `node --check scripts/verify-deployment.mjs`、`npm run lint`、`npm run build` 与 `npx prisma validate` 全部通过。
- 在独立空生产数据库上，无账号模式确认健康检查、安全响应头、`setupRequired=true` 和未登录 HTTP 401 后通过。
- 建立临时管理员后，有账号模式确认 `setupRequired=false`、安全 Cookie、认证教师 API 和注销均通过；脚本结束后 `auth_sessions` 数量为 0，外键检查为 0。
- 非 localhost 的 HTTP 地址按预期在发出请求前拒绝；只提供一个账号环境变量时也按预期拒绝，避免不完整配置。
- 首次运行暴露出认证状态真实契约为 `user: null` 而不是推测的 `authenticated` 字段；脚本已经按真实 API 修正并重新完成全部验证。
- 隔离服务、临时管理员、Cookie、响应文件与数据库均已删除，正式本机数据库未被触碰。

### 下一步

1. 获得 Railway 登录授权后创建真实项目，连接 GitHub 分支、设置 `/web` 根目录并挂载 `/data` 持久卷。
2. 对公网域名执行本次新增的两种验收命令，再进行 Teaching Members 导入、平台备份恢复和双账号协作验收。

## 2026-08-10｜管理员完整数据库备份下载

### 已完成

- 管理员账号页新增 `Full system backup` 区块，可下载包含教师、学生班级、教室、课程、班次、排课、规则和账号资料的完整 SQLite 文件；普通排课账号看不到账号页，直接调用下载接口也会被拒绝。
- 备份使用 SQLite 在线备份机制，不直接复制正在使用的数据库文件及写入日志，避免下载到时点不一致的副本。
- 系统在生成前检查在线数据库结构和外键，在清理后再次检查最终下载文件；任何一项失败都会停止下载并返回固定安全提示。
- 登录会话属于短期凭证而不是业务资料；系统从备份副本中清除所有会话并执行 `VACUUM`，不会影响在线登录状态，账号及密码哈希仍保留以供后续完整恢复。
- 下载响应使用带 UTC 日期的文件名、SQLite 内容类型、准确长度及 `no-store`，管理员页面同时提醒该文件含敏感账号和业务资料，必须保存在限制访问的部门位置。
- Railway 部署清单已加入系统内完整备份步骤，并明确它与平台 Daily／Weekly／手工 Volume backup 共同组成双重保护。
- 新增数据库、接口和前端操作区块均补充了面向业务的易读注释。

### 本次验证

- `npm run lint`、`npm run build` 与 `npx prisma validate` 通过，生产构建正确识别 `/api/system-backup` 动态路由。
- 使用隔离生产数据库验证权限：未登录请求返回 HTTP 401，普通 scheduler 返回 HTTP 403，管理员返回 HTTP 200、`application/vnd.sqlite3`、`no-store` 和带日期的附件文件名。
- 隔离库建立 2 个账号、1 位教师、1 个学生班级、1 间教室、1 门课程和 2 个班次后，下载副本的业务数量逐项一致；副本 `integrity_check` 为 `ok`、`foreign_key_check` 为 0。
- 在线隔离库保留 2 条有效会话，下载副本为 0 条，证明会话只从副本清除且不影响正在使用的系统。
- 浏览器实测管理员页能看见完整备份内容和敏感资料警告；点击按钮后成功显示带日期的下载完成提示，页面布局在常见电脑宽度下可读。
- 临时生产服务、虚构账号、测试业务资料、Cookie、隔离数据库及浏览器下载均已删除；正式本机数据库未被读取或修改。

### 下一步

1. 实现管理员上传完整 SQLite 备份并安全恢复：校验文件格式、结构、外键和必需表，恢复前再次备份当前数据库，并要求多重确认。
2. 恢复成功后撤销全部旧会话，要求所有账号重新登录；使用隔离数据库验证失败文件不会改变在线资料，成功文件能完整恢复。

## 2026-08-10｜完整数据库安全恢复

### 已完成

- 管理员账号页新增 `Restore full system backup` 表单；只接受本系统下载的 `.sqlite`，并要求选择文件、勾选“替换全部资料和账号”、勾选“全部浏览器退出”，再准确输入 `RESTORE FULL BACKUP`。
- 上传接口仅限管理员；未登录由统一代理返回 401，普通 scheduler 即使直接提交完整表单也返回 403。
- 文件先经过扩展名、16 字节至 20 MB 大小、SQLite 3 文件头、数据库完整性、外键、全部应用表与字段顺序，以及可用管理员账号和密码哈希格式检查；验证阶段不会修改在线数据库。
- 验证通过后，系统先使用在线备份机制保存当前状态到数据库同目录的 `<数据库名>-restore-safety/`，副本清除会话、再次验证并以仅拥有者可读写的 `0600` 权限保存。
- 全部应用表在一个同步 SQLite 事务中替换；提交前再次检查外键，复制或约束失败会整体回滚。上传临时文件无论成功或失败都会删除，安全副本保留。
- 恢复成功后再次执行幂等建表／规则默认值和完整性检查，返回教师、课程、班次、课次与账号数量；在线库中的全部会话清零，当前页面回到登录画面。
- 管理员页面的备份、恢复、创建账号、重置密码和账号清单区块改为多行可读结构，并为每个业务操作区块补充易读注释。
- 技术架构与 Railway 部署清单已记录完整备份／恢复 API、文件限制、事务策略、安全副本位置和恢复后验收步骤。
- 默认 `web/data/*-restore-safety/` 已加入 Git 忽略规则，防止包含账号和业务资料的本地安全副本被误提交到 GitHub。

### 本次验证

- `npm run lint`、`npm run build` 与 `npx prisma validate` 通过，GET／POST 共享的 `/api/system-backup` 生产路由构建成功。
- 在隔离生产数据库建立 2 个账号、“旧状态”1 位教师／1 门课程／2 个班次，下载已知正确备份后，再加入“新状态”成为 2 位教师／2 门课程／5 个班次。
- 错误固定短语、非 SQLite 文件、缺少必需表的 SQLite、没有可用管理员的完整数据库、普通账号和未登录请求分别返回 400／403／401；每次拒绝后在线库仍为 2／2／5、“新状态”教师仍存在，完整性为 `ok` 且外键错误为 0。
- 正确恢复返回 HTTP 200，并把在线库完整恢复为备份中的 1 位教师／1 门课程／2 个班次；“旧状态”存在、“新状态”消失，完整性为 `ok`、外键错误为 0。
- 恢复前自动安全副本保留 2 位教师／2 门课程／5 个班次和“新状态”，会话为 0、完整性为 `ok`、外键错误为 0，文件权限为 `-rw-------`。
- 管理员和 scheduler 的两个旧 Cookie 在恢复后访问业务 API 均返回 401；使用备份内管理员密码重新登录返回 200，并只读到恢复后的“旧状态”教师。
- 浏览器在常见电脑宽度下确认恢复危险区、文件选择、两次确认、固定短语和红色提交按钮完整可读，且与蓝色下载备份区明显区分。
- 临时生产服务、虚构账号、测试新旧资料、Cookie、上传文件、隔离数据库和自动安全副本均已删除；正式本机数据库未被读取或修改。

### 下一步

1. 对照 PRD 和 MVP 验收矩阵重新进行本地完成度审计，确认季节性上线前仍有哪些可在本地解决的真实流程缺口。
2. 优先使用现有真实 Teaching Members 数据走完“导入／修正课程／建立班级和教室／三张总表排课／问题复核／完整备份恢复”演练，再根据实际操作问题继续拆分 UI 小任务。

## 2026-08-10｜保存后定位课程与明确成功反馈

### 已完成

- 修正成功提示的颜色判断顺序；`placed ... with no warnings` 会先识别为成功，不再因为句子中包含 `warnings` 而错误显示成黄色警告。
- 候选时段排课、无拖动排课、拖动排课、拖动已排课程和编辑保存成功后，系统都会记录刚保存课程的编号。
- 年级总表收到保存结果后，会自动把对应课程卡片滚动到可见区域，并使用绿色边框高亮 6 秒，让排课老师能直接对应右下角成功提示与总表中的实际结果。
- 同一门课快速连续保存时使用递增请求编号；每次保存都会重新触发滚动与完整的 6 秒高亮，不会因为课程编号没有变化而忽略第二次反馈。
- 桌面端年级排课工作区改为视口内三栏布局；待排区、总表和问题检查区各自滚动，避免整个页面过长后把关键操作和反馈推到屏幕外。
- 新增状态、定位、自动清除和桌面布局代码均补充了面向非开发人员的易读注释。
- 本次只修改 `web/src/app/page.tsx`；原有未跟踪的 UX 测试文档、结果文件和验证脚本保持不变，没有纳入本次提交。

### 本次验证

- `git diff --check`、`npm run lint`、`npm run build` 与 `npx prisma validate` 全部通过。
- 使用正式数据的只读 SQLite 备份建立隔离数据库；隔离副本包含 87 位教师、52 门课程、374 个班次、8 条既有排课和 37 个 Year 1 待排课次。
- 浏览器把 `LEAD_04 · Session 1` 从待排区通过 `Clear slots` 安排到星期三 09:00–12:00、教室 `27-03-08`；页面自动显示星期三课程卡片，卡片出现绿色高亮，成功提示为绿色并明确显示 `no warnings`。
- 浏览器随后把同一课程编辑到星期四并保存；课程卡片再次高亮，提示显示 `updated successfully`，证明候选排课和编辑保存两条核心路径都能获得可见反馈。
- 1280×720 浏览器画面中，导航、待排区、总表和问题检查区同时保持可见，课程卡片完整覆盖三小时高度；本次属于开发预检，不替代 1440×900 下由 5 位真实排课老师完成的正式 UX 测试。
- 临时课程最后已返回待排区；临时服务器、隔离数据库和测试 Cookie 均已删除。正式数据库仍为 87 位教师、52 门课程、374 个班次和 8 条排课，`integrity_check` 为 `ok`、外键错误为 0。

### 下一步

1. 保持正式数据库不动，建立一次可重复重置的真实数据验收副本，完整演练“修正课程／建立班级和教室／三张年级总表排课／问题复核／完整备份恢复”。
2. 根据 `docs/UX_TASK_TEST.md`，由 5 位真实排课老师在 1440×900 环境完成核心任务测试；未收集真实结果前不把 UX 目标标记为通过。
3. 修复本地验收或真实用户测试发现的问题；上线仍留到实际使用前约 1–2 周再执行。

## 2026-08-10｜可重复重置的本地真实数据验收副本

### 已完成

- 新增 `web/scripts/prepare-local-acceptance.mjs`；默认把正式 `data/timetabling.db` 作为只读来源，生成独立的 `data/timetabling-acceptance.db`。
- 来源数据库同时使用 SQLite 只读连接和 `query_only`，并在复制前检查文件完整性与外键；脚本使用 SQLite 在线备份，而不是直接复制可能仍有 WAL 写入的普通文件。
- 验收副本自动清除旧浏览器会话，但保留教师、课程、班次、排课和账号；压缩并再次检查副本后，逐项比较关键业务数量，只有全部一致才进入最终路径。
- 最终副本权限设为 `0600`，只允许当前电脑账号读写，降低真实教师、课程和账号资料在本机被其他用户读取的风险。
- 已有验收库默认绝不覆盖；只有显式提供 `--replace` 才能重置。来源与输出相同、来源不存在，或输出旁存在 `-wal`、`-shm`、`-journal` 时都会在修改前停止。
- 临时副本使用随机文件名并与最终文件位于同一磁盘；校验全部通过后才原子重命名。任何错误都会关闭连接并删除未完成临时文件，不会删除此前有效的验收库。
- 新增中文 `docs/LOCAL_ACCEPTANCE.md`，记录首次建立、明确重置、指定其他路径、使用环境变量启动，以及覆盖导入、课程配置、三张总表、问题复核、个人课表和完整备份恢复的验收清单。
- 脚本每个安全和数据处理区块均补充了面向非开发人员的易读注释；没有修改或提交现有未跟踪 UX 测试文件及其 `package.json` 命令。

### 本次验证

- `node --check scripts/prepare-local-acceptance.mjs`、`npm run lint`、`npm run build`、`npx prisma validate` 与 `git diff --check` 全部通过。
- 使用正式数据库生成 `/private/tmp` 隔离副本，成功复制 87 位教师、52 门课程、374 个班次、8 条排课和 0 个账号；副本会话为 0、`integrity_check` 为 `ok`、外键错误为 0、权限为 `-rw-------`。
- 第一次测试发现账号表应以当前实际结构 `app_users` 为准，而不是推测的 `user_accounts`；脚本修正后重新执行全部场景并通过。
- 已有输出且没有 `--replace` 时返回失败且不覆盖；带 `--replace` 时成功重建。人为建立 `-wal` 后替换被拒绝，来源与输出设为同一路径时也在复制前被拒绝。
- 所有临时测试数据库和 sidecar 均已删除；正式数据库仍为 87 位教师、52 门课程、374 个班次和 8 条排课，`integrity_check` 为 `ok`、外键错误为 0。

### 下一步

1. 使用本次脚本建立真实数据验收库并启动独立服务器，按 `docs/LOCAL_ACCEPTANCE.md` 走完完整流程；每一步记录实际 API、浏览器和数据库结果。
2. 把完整流程发现的问题拆成独立小任务修复、验证和提交；通过后更新 `docs/MVP_ACCEPTANCE.md` 的本地最终统一回归证据。
3. 最后由 5 位真实排课老师完成 `docs/UX_TASK_TEST.md`，公网部署仍留到实际使用前约 1–2 周。

## 2026-08-10｜真实数据端到端完整流程验收

### 已完成

- 使用 `prepare-local-acceptance.mjs` 从正式数据库建立独立验收库；创建临时管理员并下载含 87 位教师、52 门课程、374 个班次和 8 条排课的完整基线备份。
- 先验证有既有排课时重复导入会被安全拒绝，再执行开始新周期；系统自动保存应急备份并把课程／班次／排课清为 0／0／0，教师、班级、教室、规则和账号保留。
- 在空周期连续两次导入真实 Teaching Members Excel；两次结果均为 52 门课程、87 位教师、184 条正数分配、374 个班次和 29 条零值行，证明更新操作不会重复生成资料。
- 建立 Year 1–3 三个验收班级和测试教室；配置 `LEAD`、`BED`、`3DP`，覆盖每周两次、2／3 小时时长、三个主年级、第 1–4／5–8 周，以及 Lab、Multi Projector、Smart Classroom 多重要求。
- 为三门课关联教师和学生班级；`LEAD_04` 同时关联 Year 1 与 Year 2 班级，用于验证跨年级课程只进入主年级总表但仍统一校验冲突。
- 候选 API 为三门课返回 80／50／35 个完全无问题时段；保存 Year 1 两条、Year 2 一条、Year 3 一条排课后，三张总表和集中问题清单均无问题。
- 暂时把两次 `LEAD_04` 放在同一天同一时段，系统允许保存并生成 10 条 High 问题；内容覆盖每周两次同日、同课程同日、教师、教室和跨年级学生班级冲突。移回候选日期后问题恢复为 0。
- 三类个人课表通过：Year 2 的跨年级班级汇总 `BED_01` 与两次 `LEAD_04` 共 3 条；`ONG HOON JIN` 教师课表和 `27-03-08` 教室课表各为 2 条。
- 浏览器逐张确认 Year 1、Year 2、Year 3 总表课程卡片及无问题状态；跨年级学生班级课表显示来自两个主年级的 3 条课，教师和教室切换数量与 API 一致。
- 下载四条验收排课的完整系统备份，增加 `RESTORE_MARKER` 后成功恢复，标记资料消失、四条排课恢复且全部旧会话失效；随后恢复最初八条排课基线。
- 基线恢复后，已打开的浏览器在下一次同步时自动进入登录页并显示 `Your session expired. Please sign in again.`，验证会话撤销能反馈到实际页面。
- `docs/MVP_ACCEPTANCE.md` 已加入本次完整流程证据，并修正过时的部署路线说明；Railway + SQLite 已选定，但实际公网配置按项目负责人决定留到使用前约 1–2 周。

### 本次验证

- 新周期前完整备份为 `integrity_check=ok`、外键错误 0、会话 0、排课 8、账号 1；开始新周期返回 52／374／8 的应急备份元数据。
- 空周期重复导入两次均返回 HTTP 200 和相同统计；有既有排课时返回 HTTP 400 且资料未改变。
- Year 1–3 已排 API 分别为 2／1／1；四条候选排课 warnings 均为空。跨年级学生班级、教师、教室个人 API 分别返回 3／2／2。
- 冲突状态集中问题为 10 High，修正后为 0；三张年级总表与跨年级个人课表均完成浏览器截图和结构检查。
- 完整恢复后 `RESTORE_MARKER=0`、课次为 4、会话为 0、数据库完整性为 `ok`；再次恢复基线后为 87／52／374／8。
- 验收服务器、验收数据库、两个下载备份、Cookie 和四个恢复安全副本均已删除；正式数据库最终仍为 87 位教师、52 门课程、374 个班次、8 条排课、0 个账号和 0 个会话，`integrity_check` 为 `ok`、外键错误为 0。
- `npm run lint`、`npm run build`、`npx prisma validate` 与 `git diff --check` 在提交前再次执行；本次没有修改应用代码，也没有把未完成的 5 人 UX 测试写成通过。

### 下一步

1. 由项目负责人安排 5 位真实排课老师，在 1440×900 环境按 `docs/UX_TASK_TEST.md` 完成核心任务测试并记录真实结果；这一步不能由开发者模拟。
2. 如果真实用户测试失败，按“查找课程／选择时段／保存／确认结果”的实际卡点拆分 UI 修复任务并重新测试。
3. 真实用户测试通过后，本地 MVP 可冻结为上线候选版本；实际公网部署、双账号跨浏览器和平台备份恢复留到使用前约 1–2 周。

## 2026-08-10｜页面与服务端小区块注释补全

### 已完成

- 对照项目负责人“每个小 block 都要有易读 comment”的要求，重新审计 `web/src` 全部 TypeScript／TSX 命名函数与 React 副作用入口。
- 在年级／个人课表共享网格、问题颜色判断、重叠课程定位、无拖动排课、5 秒协作同步，以及账号清单、停用和重置密码入口补充业务目的注释。
- 在数据库认证与会话、新周期备份／恢复、手工课程与班次数量、教师分配偏差、跨年级个人课表、候选时段、排课新增／修改和 Excel 导入入口补充说明。
- 在登录限速区块说明滚动时间窗、IP／用户名双重限制与失败记录行为，方便非安全专业开发者理解保护目的。
- 为此前只有实现代码、没有入口说明的账号、健康检查、新周期、问题、规则、候选位置、年级总表、个人课表和禁排 API 增加请求职责注释。
- 注释统一解释“这个区块解决什么业务问题、保护什么数据、完成后发生什么”，没有复述变量名，也没有修改任何运行逻辑、接口或数据库结构。
- 审计脚本再次检查后，`web/src` 中所有命名函数和 `useEffect` 均在入口或首个业务语句处具有注释；多行参数的共享网格也已人工确认入口说明。
- 原有未跟踪 UX 文件和 `web/package.json` 修改保持不变，没有纳入本次注释任务。

### 本次验证

- `git diff --check`、`npm run lint`、`npm run build` 与 `npx prisma validate` 全部通过，生产构建仍生成全部页面和 API。
- 使用空白临时 SQLite 在 3111 端口启动真实 standalone 生产服务；`npm run verify:deployment -- http://127.0.0.1:3111` 通过应用／数据库健康、安全响应头、公开认证状态和未登录业务资料 HTTP 401 检查。
- 临时生产服务和临时数据库已删除；正式数据库仍为 87 位教师、52 门课程、374 个班次、8 条排课、0 个账号和 0 个会话，`integrity_check` 为 `ok`、外键错误为 0。

### 下一步

1. 本地代码、真实数据完整流程、运行保护和代码可读性均已完成；剩余本地门槛仍是由 5 位真实排课老师执行 `docs/UX_TASK_TEST.md`。
2. 收到五位参与者的真实结果后，运行结果门槛、修复实际观察到的卡点，并决定是否冻结本地 MVP。
3. 公网部署继续按项目负责人决定留到正式使用前约 1–2 周。

## 2026-08-10｜修复成功提交后表单误报错误

### 已完成

- 根据真实排课老师反馈，复现 Teaching allocation 上传和学生班级新增后出现的页面错误；开发日志明确记录 `TypeError: Cannot read properties of null (reading 'reset')`，Excel 导入、学生班级保存和开始新周期都命中了同一问题。
- 确认服务端操作其实已经成功：正式本地库已有 `AAA_02`、`AAA_03`，并已有 52 门课程、87 位教师和 374 个预生成班次；报错发生在成功响应后的表单清理阶段，不是 Excel 或 SQLite 写入失败。
- 根因是异步 React 表单处理器在 `await` 返回后再次读取 `event.currentTarget`；此时事件的 `currentTarget` 已清空为 `null`，调用 `reset()` 会抛出异常并阻止成功提示和页面刷新流程完成。
- 所有存在相同风险的表单统一在第一次 `await` 前保存真实表单元素，再由稳定引用完成重置；覆盖不可用时段、创建账号、重置账号密码、开始／恢复周期、教师／学生班级／教室维护、Teaching Members 导入和手工课程新增。
- 每个修复区块均补充面向非开发人员的注释，解释为什么必须在网络请求前保存表单元素；没有修改接口、数据库结构、现有业务资料或 Excel 文件。
- 当前 3000 端口仍连接正式本地数据库；诊断和写入复现全部在 `/private/tmp` 独立验收数据库与 3112 端口完成，没有为测试新增、删除或覆盖正式资料。

### 本次验证

- 只读检查原 Excel：文件为 16,460,764 bytes，包含 `Teaching Members` 和 `Sheet1`；导入页存在 `Mod`、`Catalog`、`Lecturer`、`Staff Type`、`# of grps teaching` 五个正确表头，因此不是 Sheet1 或表头缺失问题。
- 从当前正式库创建独立副本后，同一 Excel 的真实导入接口返回 HTTP 200：52 门课程、87 位教师、184 条正数分配、374 个班次和 29 条零值行；`AAA_2` 的学生班级接口返回 HTTP 201。
- 在修复后的 production standalone 页面登录临时账号，实际新增 `AAA_FORM_04`；页面显示 `Student group saved to the local database.`、学生班级数量由 7 变为 8、表单正常关闭，服务端日志没有异常。
- `rg` 已确认 `web/src` 不再存在 `event.currentTarget.reset()`；`git diff --check`、`npm run lint`、`npm run build` 与 `npx prisma validate` 全部通过。
- 临时账号密码只写入验收副本；验收数据库、Cookie、浏览器测试资料和 3112 服务将在本次任务结束时清理。正式资料保持为用户本次操作后的状态。

### 下一步

1. 请真实排课老师刷新 3000 页面；确认 `AAA_02`／`AAA_03` 是否就是刚才要建立的班级，避免再建立只有补零格式不同的重复编号。
2. 由这一位真实排课老师在可重复重置的验收副本完成三轮形成性测试；三轮属于同一参与者的学习过程，不伪装成三位独立用户。
3. 根据真实操作继续修复发现的流程问题；本地 MVP 稳定后，再在正式使用前约 1–2 周处理公网部署。

## 2026-08-10｜高密度总表与已有课程上的精确拖放

### 已完成

- 根据真实排课老师反馈重做年级总表密度：同一时段存在多张课程时，不再让每条冲突 lane 固定占 124px；普通日期保持可读宽度，高密度日期按每张 54px 的窄卡并排展开。
- 三张及以上同时段课程自动进入窄卡模式，只在表内保留课程编号、时长、教室和问题数量；教师、完整时间和教室仍保存在鼠标提示中，点击卡片后由 Inspector 显示全部资料。
- 每小时时间轨由至少 52px 缩至 44px，总表最小高度由 554px 缩至 470px；主导航、待排区和 Inspector 同时缩窄，页面最大工作宽度扩大至 1920px。
- Inspector 默认收起，把右侧 220px 返回给总表；顶部保留 High／Warning 数量和 `Show inspector` 按钮。点击已排课程、`Schedule`、`Clear slots` 或从问题清单打开课程时会自动展开，完成查看后可再次收起。
- 拖动时不再显示遮住表格的整张大卡，而是显示小型课程编号标签；当前目标小时会在课程卡上方显示绿色 `Drop 09:00`／`Drop 10:00` 提示。
- 修正已有课程遮挡背景格时的命中逻辑：系统按照鼠标在多小时卡片中的垂直位置换算实际小时；拖到 09:00–12:00 卡的第一、第二、第三段会分别提交 09:00、10:00、11:00。
- 拖动在表外取消时自动清除绿色目标；保存仍沿用原有 API 和全部冲突／规则检查，没有放宽任何排课限制。
- 新增拖动预览、目标小时换算、目标清理、密度切换和 Inspector 开关区块均补充了面向非开发人员的业务注释。

### 本次验证

- 从正式数据库建立 `/private/tmp` 独立验收副本，加入七张星期一 09:00–12:00 的三小时课程，仅用于制造极端并发场景；正式数据库没有新增测试课程、教室或排课。
- 修复前的 1280×720 画面虽然能看到七张 80px 课程卡，但星期四、星期五被推入横向滚动区；进一步压缩后，七张窄卡、星期一至星期五和 08:00–17:00 小时轴能在同一总表视野中呈现。
- 浏览器确认 Inspector 初始收起且 `Show inspector` 可见；点击 `BED_01` 后自动切换为 `Hide inspector`，编辑表单显示该课程的时长、教师、教室和六条测试冲突。
- 页面结构确认每张同时段课程都是独立可点击卡片，课程编号、3h、教室和问题数量均未因窄卡模式消失；完整资料可通过标题和 Inspector 取得。
- 自动化浏览器当前不提供原生 drag-and-drop 手势，因此没有把鼠标拖动写成已完成人工验收；精确小时换算、绿色目标行和保存参数已通过代码检查、TypeScript 生产构建验证，保留给真实排课老师刷新 3000 页面后的鼠标确认。
- `git diff --check`、`npm run lint`、`npm run build` 与 `npx prisma validate` 全部通过；临时验收服务、账号密码和高密度资料将在提交前清理。

### 下一步

1. 真实排课老师刷新 3000 页面，把一张待排课程分别拖到已有三小时课程的第一、第二、第三小时，确认绿色目标和最终开始时间一致。
2. 确认窄卡上保留的“课程编号、时长、教室、问题数量”足够用于总表扫视；如果仍太拥挤，再讨论按专业／学生班级筛选总表，而不是继续无限缩小字体。
3. 把本次真实鼠标结果纳入同一位排课老师的形成性测试记录，再继续处理下一项实际使用问题。

## 2026-08-10｜解释空待排区并建立拖放测试副本

### 已完成

- 确认正式数据库已成功导入 52 门课程和 374 个班次，但 52 门课程都还没有设置 `duration_hours` 与 `primary_year`；系统无法知道班次长度和应进入 Year 1／2／3 哪张总表，因此三个待排区为空是资料尚未配置，不是导入丢失。
- Year timetable 的空待排区不再只显示模糊的 `No configured sessions`；现在明确说明 allocation 已导入，但每门课仍需要 Duration 和 Primary year，并提供 `Configure courses` 按钮直接进入课程设置页。
- 新提示旁的代码注释从本任务开始使用详细中文，说明 Excel 能提供和不能提供的资料，以及为什么系统不能自动猜测年级和课时。
- 使用 `prepare-local-acceptance.mjs` 从正式库建立独立的 `web/data/timetabling-drag-test.db`；该文件受 Git 忽略规则保护，不会推送教师、账号或测试资料。
- 测试副本仅把 `BED` 与 `LEAD` 设置为 Year 1、每节 3 小时，并把所有相关班次关联到 `AAA_01`；预先把 `BED_01` 放在星期一 09:00–12:00，另外保留 25 个待排班次，用于真实鼠标拖到空白格和已有卡片。
- 测试副本新增单独的本机临时账号，不修改正式管理员密码；正式 3000 端口继续使用 `timetabling.db`，测试将通过 3112 端口进行。

### 本次验证

- 正式库只读检查：52 门课程、374 个班次、0 条排课，已完整设置时长与主年级的课程为 0；数据库完整性为 `ok`，外键错误为 0。
- 测试副本包含 87 位教师、52 门课程、374 个班次、2 个账号；`BED` 与 `LEAD` 的时长和主年级均为 3 小时／Year 1，预放排课 1 条，预期待排 25 条，外键错误为 0。
- `git diff --check`、`npm run lint`、`npm run build` 与 `npx prisma validate` 全部通过，生产构建仍包含完整排课与资料维护 API。
- 本任务只提交空状态 UI 和开发日志；本机测试数据库继续保留到真实老师完成拖放检查后再删除。

### 下一步

1. 在 3112 测试副本登录后，把 `LEAD_01` 拖到 `BED_01` 的第一、第二、第三小时，核对绿色目标时间和保存后的开始时间。
2. 真实资料开始配置时，在 Courses 页面逐门确认 Duration、Primary year、周次数、教学周和教室要求；不能把测试副本中的 Year 1 假设复制到正式数据。
3. 按项目负责人要求，把所有已跟踪程序代码中的英文注释翻译为详细中文，并在独立 commit 中完成扫描和验证。

## 2026-08-10｜全部已跟踪程序注释改为详细中文

### 已完成

- 按项目负责人要求，把当前已纳入 Git 版本管理的程序注释统一改为中文；覆盖年级总表主页面、数据库业务层、全部 API 路由、登录与限流、Excel 导入、备份恢复、独立部署脚本、Next.js 配置及 Prisma 数据模型，共 44 个程序文件。
- 注释不做生硬逐字翻译，而是尽量用基础程序员能理解的方式说明：这个代码区块解决什么业务问题、为什么要采用当前做法、需要保护哪些资料，以及失败时如何避免产生半完成状态。
- 数据库层详细解释连接复用、结构升级、备份校验、事务回滚、账号会话、基础资料稳定 ID、课程与班次、跨年级关联、警告引擎、候选位置及 Teaching Members 导入流程。
- 前端层详细解释 React 状态、表单处理、拖放命中、高密度卡片、多小时课程渲染、5 秒协作刷新、旧请求作废与问题检查流程；API 层则逐一说明请求职责和服务器端验证边界。
- 界面文字继续保持此前确认的英文产品要求；仅程序员阅读的代码注释改为中文。SQLite、React、API、Cookie、UUID 等必要技术名称保留英文，但其解释句均为中文。
- 使用只扫描 Git 已跟踪文件的检查命令复核所有行注释和块注释，未发现不含中文说明的纯英文程序注释。自动生成文件中的安装命令也补充了中文用途说明。
- 本次只修改注释，没有改变任何可执行语句、接口、数据库结构或正式排课资料；用户尚未完成的 UX 草稿文件和原有 `web/package.json` 修改仍保持原状，没有纳入本任务。

### 本次验证

- `git diff --check` 通过，没有空白字符错误或破损补丁。
- `npm run lint` 通过，全部 TypeScript／TSX／JavaScript 文件符合代码规范。
- `npm run build` 通过，TypeScript 编译、24 个页面生成、全部排课 API 和 standalone 构建均成功。
- `npx prisma validate` 通过，中文注释没有影响 Prisma 数据模型语法。
- 四个已跟踪 `.mjs` 维护／部署脚本分别通过 `node --check`，中文注释没有影响脚本解析。

### 下一步

1. 使用保留在本机、不会提交 Git 的 `web/data/timetabling-drag-test.db` 启动 3112 测试服务，让真实排课老师看到 25 个待排班次。
2. 真实老师把 `LEAD_01` 拖到 `BED_01` 三小时卡片的不同小时位置，确认绿色目标时间、卡片密度和最终开始时间。
3. 测试完成后删除独立测试数据库和临时账号；正式 3000 数据库继续保持不变。

## 2026-08-10｜修复警告提示遮挡 Inspector 操作按钮

### 已完成

- 根据真实排课老师截图复现：课程保存后，黄色 warning 提示固定在右下角；在较矮窗口或 Inspector 内容较长时，会覆盖 Room、Return to tray 和 Save changes 等操作区域。
- 将浮动提示从右下角移到右上角标题空白区，避免覆盖 Inspector 底部表单；窄窗口下提示仍保留左右安全间距，较宽窗口下最大宽度保持 384px。
- 提示内部加入明确的 `×` 关闭按钮，老师不必等待即可移除；同时设置八秒自动隐藏，防止操作结果长期挡住任何页面内容。
- 自动隐藏只关闭浮动层，不删除原有 `notice` 操作结果，因此其他资料页底部的 System status 仍可显示完整状态。
- 提示内容限制最大高度并允许自身滚动；即使未来一门课产生很多 warning，也不会形成覆盖大面积页面的超高浮层。
- 新增代码区块使用详细中文注释，说明提示计时器、React Effect 和保留 System status 的原因；没有修改排课、warning 判断、数据库或正式资料。

### 本次验证

- `git diff --check`、`npm run lint`、`npm run build` 和 `npx prisma validate` 全部通过；生产构建仍生成 24 个页面及全部排课 API。
- 在 1280×720 的 3112 独立测试环境保存 `BED_01`，确认 warning 提示显示在右上角，不覆盖右侧 Inspector。
- 提示显示期间重新打开 Inspector，DOM 同时存在 `Dismiss notification` 和 `Save changes`；实际点击 `Save changes` 成功，证明提示没有拦截底部操作。
- 实际点击 `×` 后提示立即消失；再次触发提示并等待八秒，关闭按钮数量自动变为 0，证明自动隐藏生效。
- 测试只对 `timetabling-drag-test.db` 中 `BED_01` 原值重复保存，没有改动正式 3000 数据库，也没有重置用户当前测试副本。

### 下一步

1. 真实排课老师刷新 3112 页面，继续拖放课程；保存 warning 后可直接关闭右上角提示，也可等待八秒自动消失。
2. 继续观察 Inspector 在真实资料量下的滚动和按钮可达性；若仍有表单过长问题，再单独优化 Inspector 的固定操作栏。

## 2026-08-10｜压缩 Master timetable 并优先显示教师姓名

### 已完成

- 根据真实排课老师的大屏截图确认，旧版会按同一天的重叠通道数量扩大日期栏；星期二有七门同时上课时几乎占满整张表，导致星期三至星期五被推离屏幕。
- Master timetable 卡片删除重复的 `2h`／`3h`／`4h` 文字；卡片跨越多少小时格已经直接表达课程时长，鼠标提示和 Inspector 仍保留准确开始、结束时间及总时长。
- 所有密度的 Master timetable 卡片都直接显示教师姓名，不再像旧版高密度卡片那样只保留课程编号、教室和问题数；显示顺序改为课程编号、教师、教室、问题数。
- 每小时时间格由 44px 压缩到 36px，总表最低高度由 470px 降为 388px；08:00–18:00 更容易和页面工具栏一起进入普通显示器画面。
- 日期宽度不再按重叠课程数无限增加：普通日期最低 104px，繁忙日期按每条通道约 36px 增长，但最低宽度最多 252px，剩余弹性权重最多为普通日期的 2.5 倍。
- 三门以上同时上课时，卡片之间的横向间隔由 4px 减到 2px，内边距和课程编号字体同步缩小；课程编号保持单行，教师姓名允许按单词换行，避免逐字竖排。
- 大屏概览继续支持隐藏 Inspector，把右侧宽度还给总表；窄窗口或 Inspector 打开后如仍低于固定可读宽度，可用已有 Left／Right 按钮进行短距离浏览。
- 新增与调整的布局区块均使用详细中文注释，说明宽度上限、通道间距和不重复显示时长的业务原因；没有修改排课位置、拖放命中、warning 规则、接口或数据库结构。

### 本次验证

- 建立独立 `timetabling-density-test.db`，在星期二 09:00–12:00 放入七门三小时 LEAD 课程；该副本来自原 3112 测试库，不修改正式 3000 数据库或用户当前拖放测试进度。
- 1280×720 浏览器中隐藏 Inspector 后，星期一至星期五和 08:00–18:00 全部在同一画面；七张星期二课程卡均显示完整课程编号，并直接显示对应教师姓名。
- 打开 Inspector 后仍可点击每张高密度课程并编辑；可读宽度不足时只出现有限横向滚动，不再让繁忙日期按七倍比例挤走其余日期。
- 普通单通道 `BED_01` 卡片确认不显示 `3h`，但仍严格跨越 09:00–12:00 三个小时格；课程高度表达时长的规则没有改变。
- `git diff --check`、`npm run lint`、`npm run build` 和 `npx prisma validate` 全部通过；TypeScript 生产构建继续生成 24 个页面及全部排课 API。

### 下一步

1. 真实排课老师刷新 3112 页面，在实际大屏继续排入更多课程，确认“课程编号、教师姓名、教室、问题数”的信息优先级满足扫视需要。
2. 如果真实满表后仍希望进一步压缩，可增加用户可切换的 Overview／Detail 密度模式；在取得真实反馈前不再牺牲教师姓名可读性。

## 2026-08-10｜PRD 增加全宽 Master timetable 与按需面板要求

### 已完成

- 根据项目负责人最新决定，把年级排课页的信息优先级写入 `docs/PRD.md`，文档版本由 v1.0 更新为 v1.1。
- 明确 `Master timetable` 默认占满全局导航以外的全部可用宽度，待排区和 Inspector 不得永久预留左右栏。
- 明确待排区使用带数量的顶部开关和左侧抽屉：关闭时完全不占宽度，打开后仍保留搜索、筛选和拖放能力，并允许临时缩小总表。
- 明确 Inspector 使用右侧浮动面板：打开时覆盖在总表工作区上方，不重新计算或压缩总表宽度；点击课程或问题可自动打开，并必须提供清楚的关闭入口。
- 新增大屏完整周视野、只在小窗口内部横向滚动、鼠标和键盘可操作，以及打开待排抽屉后仍能拖到所有可见时段的验收要求。
- 在首版验收标准中新增第 9 项，要求验证全宽默认状态和两个按需面板的拖放、编辑及关闭行为。

### 本次验证

- `git diff --check` 通过；本次仅更新中文需求文档和开发日志，没有修改应用代码、数据库或用户测试资料。

### 下一步

1. 按 PRD v1.1 实现待排区开关、左侧抽屉和右侧浮动 Inspector。
2. 使用多门同时上课的独立测试副本，在抽屉关闭、待排抽屉打开和 Inspector 打开三种状态下检查总表宽度与按钮可达性。

## 2026-08-10｜实现全宽 Master timetable 与按需工具面板

### 已完成

- 年级排课页移除原有 1920px 最大宽度和长期保留的 `190px + 总表 + 220px` 三栏布局；总表现在是页面中唯一永久占位的排课主栏，默认占满全局导航以外的全部可用宽度。
- `Unscheduled sessions (N)` 改为顶部按需开关。关闭时组件不渲染，不留下空白列；打开时在 1024px 以上电脑屏幕临时加入 220px 左栏，在 1280px 以上使用 240px，继续保留搜索、教师类型、专业、学生班级筛选及原生拖放功能。
- 待排抽屉仍留在正常页面布局中，没有增加会拦截鼠标事件的全屏遮罩，因此课程可以从左侧直接拖到星期一至星期五的可见时间格；较窄页面下抽屉最高 420px，避免把总表推到过远位置。
- Inspector 改为总表内部的右侧浮动面板，桌面宽度固定为 320px；打开时覆盖右侧内容但不参与 CSS Grid 宽度计算，总表的五天栏宽度不会重新计算，关闭后也不会留下任何右侧空白。
- 待排区和 Inspector 保持互斥。点击 `Schedule`、`Clear slots`、已排课程或问题清单时，系统会收起待排区并自动打开 Inspector，避免两个工具面板同时夹窄或遮挡总表。
- 两个面板都有明确的关闭按钮、`aria-expanded`、`aria-controls` 与标题关联；打开后焦点进入面板的关闭按钮，按 `Esc` 可关闭并把焦点返回顶部开关，键盘操作不会因原按钮卸载而丢失位置。
- 总表顶部按钮文字与 PRD v1.1 统一为 `Unscheduled sessions (N)`；相关 React 状态、面板职责、互斥原因、焦点管理和响应式宽度均补充了基础程序员可读的详细中文注释。
- 只读代码审查确认没有 P0／P1 阻断问题；审查提出的 1024–1279px 左右布局、键盘焦点管理和按钮文字一致性三项问题均已在本次提交前修正。

### 本次验证

- 1280×720 浏览器默认状态下，页面宽度为 1280px，总表从横坐标 164px 延伸到 1268px，实际宽度 1104px；待排区和 Inspector 都不存在，页面横向总宽仍为 1280px，星期一至星期五全部可见。
- 打开待排抽屉后，左栏准确为 240px，总表临时变为 856px；关闭后总表立即恢复为 1104px，没有残留空列或页面级横向滚动。
- 打开 Inspector 后，浮层准确为 320px，而底层总表仍保持 1104px；点击 `BED_01` 后，`Return to tray` 与 `Save changes` 均位于可视区域，没有被状态提示或浮层边界遮挡。
- 自动化浏览器实际验证两个面板互斥；点击待排课程的 `Schedule` 后，待排抽屉被卸载、Inspector 自动打开，并显示对应课程的点击式排课表单。
- 自动化浏览器实际验证焦点流程：打开待排区后焦点位于 `Close unscheduled sessions`，按 `Esc` 后回到 `Unscheduled sessions (25)`；Inspector 同样从 `Close inspector` 返回 `Inspector` 开关。
- 从 3112 测试库建立一次性临时副本，在星期二 09:00–12:00 放入七门三小时 LEAD 课程。默认全宽状态下五个工作日、完整 08:00–18:00 时间轴和七张课程卡同时可见；七张卡都显示课程编号、教师姓名与教室状态，界面文字中没有重复的 `3h`。
- 高密度副本打开 240px 待排抽屉后，星期一至星期五仍全部位于 1280px 视口内，Inspector 保持关闭，页面没有横向溢出。临时服务随后停止，一次性数据库和临时目录均已删除；正式数据库和长期保留的 3112 拖放测试库没有被修改。
- `git diff --check`、`npm run lint`、`npx prisma validate` 与 `npm run build` 全部通过；TypeScript、24 个页面、全部排课 API 和 standalone 生产构建均成功。

### 下一步

1. 真实排课老师在保留的 3112 页面打开 `Unscheduled sessions`，把课程拖到空白时间格和已有多小时课程的不同小时位置，完成原生鼠标拖放的人工验收。
2. 用真实满表资料确认 220px／240px 待排抽屉宽度是否适合连续排课；如果老师需要更大的卡片，再增加可调节抽屉宽度，而不是永久压缩总表。

## 2026-08-10｜PRD 调整为顶部 Workspace 菜单与至少三天同屏

### 已完成

- 根据项目负责人对上一版全宽界面的最新反馈，把中文需求文档从 v1.1 更新为 v1.2。
- 明确所有 Workspace 功能入口从永久左侧栏移动到页面顶部菜单栏；当前页面仍须有明显选中状态，管理员账号入口继续只对管理员显示，窗口较窄时由菜单栏自身横向滚动。
- Master timetable 的“全宽”定义同步改为占满页面安全边距以外的宽度，因为顶部菜单不再消耗任何水平空间。
- 将大屏“周一至周五必须全部同屏”的要求放宽为“至少 3 个连续工作日和完整 08:00–18:00 同屏”；繁忙日期可以获得更宽的课程栏，其余日期使用总表已有的左右按钮或横向滚动查看。
- 更新首版验收标准第 9 项，把顶部 Workspace 菜单、全宽总表和至少三天同屏列为同一条正式验收要求。

### 本次验证

- `git diff --check` 通过；本子任务只更新中文 PRD 和开发日志，没有修改应用代码、数据库或本地测试资料。

### 下一步

1. 把现有左侧 Workspace 导航移动到顶部菜单栏，并让年级总表取得释放出的全部水平宽度。
2. 调整日期栏最低宽度和繁忙日期上限，使 1024px 以上电脑至少完整显示三个连续工作日，同时提升多课程并排卡片的可读性。

## 2026-08-10｜实现顶部 Workspace 菜单与至少三天同屏

### 已完成

- 新增共用的 `WorkspaceMenuButton` 组件，把 `Year timetables`、`Personal timetables`、`Courses`、`Data management`、`Rules & issues`、`New cycle & recovery` 和管理员可见的 `Accounts` 全部移入顶部身份栏。
- 桌面端品牌、Workspace 菜单和账号保持同一行，不额外占用课表垂直空间；菜单使用独立横向滚动容器和不压缩按钮，窄屏时才换到下一行，账号与 `Sign out` 始终留在画面中。
- 当前菜单入口增加明显的蓝色选中状态与 `aria-current="page"`；原有页面打开函数、Data management 子页面选中逻辑和管理员权限没有改变。
- 移除所有页面永久左侧导航和原有 140px／220px Grid 列，年级页与资料页都变成真正的单栏内容；年级 Master timetable 现在只保留左右 12px 页面安全边距。
- 原侧栏的 Current cycle 数量合并进超宽屏的 `Draft · N sections` 状态标签；一般电脑优先把顶栏宽度交给业务导航。
- 总表提示改为“至少三天同屏”；普通日期最低宽度为 136px，按每条重叠通道约 44px 增长，最繁忙日期上限为 288px。三个最宽日期加时间轴和间距约为 924px，给 1024px 页面边距、滚动条和像素舍入保留余量。
- 1024px 以上的年级页面、内容容器、section 和排课 Grid 统一使用同一条有界 `flex / min-h-0` 高度链；待排抽屉即使有数百张卡也只在自身内部滚动，不会把总表拉成长页面。
- 操作提示因 Workspace 移入顶栏而改到顶栏下方中央，只覆盖没有操作按钮的页面标题；不会挡住菜单、账号、总表工具栏或 Inspector，并继续支持手工关闭与八秒自动隐藏。
- 所有新增或调整区块均加入详细中文注释，说明菜单溢出、权限、单栏宽度、三天宽度数学、响应式高度和提示位置的业务原因。

### 本次验证

- 1280×720 普通测试资料中，Master timetable 左缘从旧版约 164px 移到 12px，右缘为 1268px，实际宽度 1256px；08:00–18:00 完整显示，页面总宽保持 1280px，没有页面级横向溢出。
- 顶部非管理员菜单在 867px 可用区内完整显示；实际点击 `Courses` 后页面切换成功且 `aria-current` 移到 Courses，再点击 `Year timetables` 后正确返回年级总表。当前入口始终只有一个。
- 浏览器确认新的居中操作提示位于页面标题上方，没有覆盖顶部 Workspace、账号、`Unscheduled sessions`、Inspector、年级切换或左右滚动按钮。
- 建立一次性高密度副本，在周一、周二、周三 09:00–12:00 各放入七门三小时课程，共 21 张同时段课程卡；最终 288px 日期上限下三天课程编号、教师姓名和教室状态均可见，Master 卡片仍不重复显示 `3h`。
- 高密度副本打开 240px 待排抽屉后，Master 宽度为 1008px、内部可见宽度为 982px；Mon、Tue、Wed 三个 288px 日期栏全部位于可视边界内，Thu、Fri 由 `Left`／`Right` 查看，页面总宽仍为 1280px。
- 浏览器实际点击 `Right` 后横向位置发生改变，Thu 与 Fri 进入完整可视范围；顶部菜单和总表分别只滚动自身，不会带动整个页面。
- 两轮只读代码审查先发现 1024–1279px 抽屉高度链和三天宽度余量问题；统一到 `lg` 有界高度并把日期上限降到 288px 后复核通过，没有 P0、P1 或 P2 阻断事项。
- `git diff --check`、`npm run lint`、`npx prisma validate` 与 `npm run build` 全部通过；TypeScript、24 个页面、全部排课 API 和 standalone 生产构建均成功。
- 两次高密度验收均只使用一次性临时数据库；相关 3113 服务、数据库和临时目录已经停止并删除，正式数据库与长期保留的 3112 拖放测试库没有被修改。

### 下一步

1. 真实排课老师在 3112 页面确认顶部 Workspace 的入口顺序是否符合日常使用频率；如需调整，只改变菜单顺序，不恢复永久左侧栏。
2. 使用真实鼠标把待排课程拖入当前可见三天，并通过 `Right` 浏览到星期四／星期五继续排课，完成原生拖放与横向浏览的联合人工验收。

## 2026-08-11｜PRD 明确课程卡文字层级、默认待排区与 Inspector 学生班级

### 已完成

- 根据项目负责人最新决定，将中文需求文档从 v1.2 更新为 v1.3；本次只确认需求，不修改排课数据或执行逻辑。
- 明确保留同一小时课程横向并列的现有布局，不在小时格内自动换行，避免卡片纵向位置与真实开始、结束时间产生歧义。
- 明确课表卡片字号不随同时段课程数量缩小；课程编号与班次为最大加粗文字，教师姓名和教室使用较小文字，空间不足时截断并由鼠标提示和 Inspector 提供完整资料。
- 明确首次进入年级总表时默认打开 `Unscheduled sessions`，老师可以直接开始拖放课程，并仍可随时关闭以恢复全宽总表。
- 明确 Inspector 可分配或更改一个或多个学生班级；修改作用于同一课程班次的全部每周课次，并须重新计算所有相关冲突和警告。

### 本次验证

- `git diff --check` 通过；本子任务只修改中文 PRD 和开发日志，没有修改应用代码、数据库或本地测试资料。

### 下一步

1. 固定 Master timetable 卡片的字号和课程、教师、教室信息层级。
2. 将待排抽屉改为首次进入年级总表时默认打开，同时避免初次载入时抢走键盘焦点。
3. 在 Inspector 增加学生班级选择，并让后端安全更新班次关联与所有相关警告。

## 2026-08-11｜固定 Master timetable 课程卡文字层级

### 已完成

- Master timetable 课程卡不再根据同一时段横向通道数量切换字号；普通日期和繁忙日期现在使用完全相同的固定字体大小。
- 课程编号与班次使用 11px 最大加粗文字，教师姓名和教室使用 9px 较小文字，问题数量也固定为 9px，信息层级在满表中保持一致。
- 当卡片横向空间不足时，只截断过长文字，不把字体继续缩小；鼠标提示和 Inspector 仍保留完整课程、教师、教室及时间资料。
- 保留现有课程横向并列与多小时高度逻辑，没有改变排课位置、冲突检查、拖放命中或数据库资料。

### 本次验证

- `git diff --check`、`npm run lint` 与 `npm run build` 全部通过。

### 下一步

1. 将 `Unscheduled sessions` 改为首次进入年级总表时默认打开，并避免初次载入抢走键盘焦点。
2. 在 Inspector 增加学生班级选择及后端同步更新。

## 2026-08-11｜首次进入年级总表默认打开待排区

### 已完成

- 年级总表的 `Unscheduled sessions` 左侧待排区改为默认打开，老师登录并进入排课页后可以直接搜索、筛选和拖放尚未安排的课程。
- 顶部开关、关闭按钮、`Esc` 键和关闭后恢复全宽总表的原有行为全部保留。
- 新增前一状态记录：页面首次载入时不会因为默认打开待排区而抢走键盘焦点；只有老师主动从关闭状态重新打开时，焦点才进入待排区的关闭按钮。
- Inspector 与待排区继续保持互斥，打开 Inspector 时会收起待排区，不会同时遮挡或压缩总表。

### 本次验证

- `git diff --check`、`npm run lint` 与 `npm run build` 全部通过。

### 下一步

1. 在 Inspector 增加一个或多个学生班级的分配与修改。
2. 保存学生班级后同步该班次的全部每周课次，并重新计算相关冲突和警告。

## 2026-08-11｜Inspector 支持分配和修改学生班级

### 已完成

- Inspector 新增可滚动的 `Student groups` 复选清单，可为已排课程班次选择零个、一个或多个学生班级；每项同时显示班级编号、年级和专业，支持跨年级课程。
- 界面明确提示学生班级修改会应用到该课程班次的全部每周课次；复选框通过原生 form 关联与星期、时间、教师和教室一起由同一个 `Save changes` 保存。
- 排课查询现在同时返回学生班级稳定 ID 和显示编号，使 Inspector 能准确恢复当前勾选状态；新放置、年级总表和个人课表使用一致的返回结构。
- 服务器先去重并验证全部学生班级 ID，再在同一 SQLite 事务中整体替换班次关联、保存当前课次位置并提高修订号；任一资料无效时整体回滚。
- 同一班次若每周上两次，修改一课次的学生班级会同步应用到另一课次，并提高另一课次的 revision，阻止仍打开的旧 Inspector 静默覆盖新资料。
- 班级关联写入后统一重新计算全部排课警告，因此当前课次、同班次其他课次，以及与新班级产生冲突的其他课程都会取得最新问题状态。
- 所有新增逻辑均补充详细中文注释，说明跨周次同步、FormData、多选验证、事务回滚、revision 与 warning 刷新的业务原因。

### 本次验证

- `git diff --check`、`npm run lint` 与 `npm run build` 全部通过；TypeScript、24 个页面及全部排课 API 构建成功。
- 使用一次性数据库副本调用业务层，把 `BED_01` 从 `AAA_01` 改为 `AAA_02`；返回资料和重新查询结果均包含正确的学生班级 ID、编号、最新 revision 与 warning。
- 在临时副本人为建立 `BED_01` 每周两次的测试场景；从 occurrence 1 修改班级后，occurrence 1 和 occurrence 2 都同步改为 `AAA_01`，两条 revision 分别从 5→6 和 1→2，相关 warning 同步刷新。
- 使用不存在的学生班级 ID 保存时得到 `Choose valid student groups.`，保存前后的班级和 revision 完全相同，证明无效资料不会产生半完成更新。
- 在 localhost:3113 的生产构建浏览器验收中，页面首次进入已默认显示展开的待排区且没有抢走焦点；课程卡计算样式为课程编号 11px／900、教师 9px／500、教室 9px／600。
- 浏览器实际打开 `BED_01 · Session 1`，Inspector 正确勾选 `AAA_01`；改勾 `AAA_02` 并保存成功，再打开 `Session 2` 时也正确显示 `AAA_02`，确认界面、API 和多课次同步完整贯通。
- 隔离测试早期曾误用环境变量名 `TIMETABLING_DB_PATH`，导致本地默认库的 `CM_12` 短暂从未分配改为 `AAA_01`；发现后立即备份并恢复为空班级、revision 1，再次读取确认 `Student group not assigned` 警告恢复。后续全部测试改用正确的 `TIMETABLING_DATABASE_PATH` 和一次性副本。

### 下一步

1. 让真实排课老师在日常测试副本中从 Inspector 分配跨年级的多个学生班级，确认清单高度和班级排序符合实际工作习惯。
2. 继续开发其余尚未完成的课程配置、规则维护和协作验收功能。

## 2026-08-11｜修复已排课程拖动时遗漏学生班级

### 已完成

- 发布前接口审查发现：Inspector 增加学生班级后，已排课程的拖动请求仍沿用旧格式，没有发送 `studentGroupIds`；新 PATCH 接口会把这种请求判定为不完整并返回 400，导致课程无法移动。
- 拖动已排课程时现在会把当前课程的全部学生班级 ID 与教师、教室、revision 一起原样提交，只改变老师实际拖动的星期和开始时间。
- 新增中文注释说明 PATCH 是完整保存操作，为什么移动时也必须保留学生班级，避免以后新增字段时再次出现同类回归。
- 未排课程首次放置使用独立 POST 流程，不受本次修改影响。

### 本次验证

- `git diff --check`、`npm run lint` 与 `npm run build` 全部通过。
- 使用隔离数据库和真实登录会话实际移动 `BED_01`：星期从周一改为周二、开始时间从 09:00 改为 13:00；教师 `NG WEI PENG`、教室 `31-05-10` 与学生班级 `AAA_01` 均完整保留，revision 从 4 增加到 5。
- 以上验证只修改 `/private/tmp` 下的发布测试副本，没有改动老师正在查看的本地数据库。

### 下一步

1. 继续 UI、CRUD、数据关联、可靠性与中文注释覆盖的完整发布审查。

## 2026-08-11｜防止已有排课因清空主年级而消失

### 已完成

- 课程清单 API 新增 `scheduledLessons` 数量，界面可以准确知道一门课程是否已经出现在总表中，而不是根据班次数或配置状态猜测。
- 已有排课的课程设置会把 `Choose later` 选项设为不可选，并明确说明 Primary year 是必填项；尚未排课的课程仍可稍后选择年级。
- 数据库共用写入函数增加第二层保护：任何调用者只要尝试把已有排课课程的主要年级清空，都会在写入前被拒绝。
- 课程设置 API 将这种当前资料状态冲突返回为 409，并提供清楚的英文提示；一般字段格式错误仍保持 400。
- 把课程设置第一行的超长 JSX 拆成易读的小区块，并补充中文注释说明为何已有排课不能取消所属总表。

### 本次验证

- `git diff --check`、`npm run lint` 与 `npm run build` 全部通过；TypeScript 和 24 个页面、API 均成功构建。
- 在隔离生产构建中读取 `BED`，API 正确返回 `scheduledLessons: 1`；实际尝试把 Primary year 改为 `null` 时得到 409 与 `Choose a primary year before saving a course that already has scheduled lessons.`。
- 拒绝请求后直接读取隔离 SQLite，`BED.primary_year` 仍为 1，`BED_01` 仍在周二 13:00 且 revision 仍为 5，证明没有产生半完成写入或隐藏排课。
- 浏览器实际打开 `Configure BED`，`Choose later` 已禁用、`Year 1` 保持选中，并显示已有排课必须选择年级的说明。

### 下一步

1. 修复重复导入 Teaching allocation 时可能覆盖尚未排课班次的手工教师与学生班级关联。
2. 继续补齐并发 revision、网络错误处理、CRUD 自动化回归和发布安全检查。

## 2026-08-11｜重复导入保留班次 ID 与手工关联

### 已完成

- Teaching allocation 重复导入不再先删除整门课程的全部班次；改为按照 `course + sequence` 差异化更新，保留已有 `LEAD_01` 等班次的稳定 ID 和学生班级关联。
- `course_sections` 新增可空的 `allocation_teacher_id`，只标记由新版 Excel 导入器自动维护的教师；老师从 Sections 或 Inspector 真正更改教师后会清空来源标记，后续导入不会覆盖手工决定。
- 纯粹拖动课程、改教室或只改学生班级不会误清来源标记；代码会先比较教师 ID，只有教师真的改变才标记为手工覆盖。
- 旧 SQLite 自动补列时不会猜测历史教师来源，既有班次默认按受保护资料处理；新导入增加的尾部班次才记录明确的 Excel 来源教师。
- 数量增加时只建立缺失的尾部班次；由导入器新增且仍未手工修改的班次可以随着下次工作簿自动更新教师。
- 数量减少时只删除目标范围外、没有受保护教师和学生班级的尾部班次；否则整次事务回滚并返回可操作的 409 提示。
- 已有排课课程仍明确拒绝重复导入并返回 409，避免工作簿在老师不知情时改变已排总表；手工课程配置和工作簿未提及的课程继续保留。
- 紧急周期快照与 Prisma 模型同步保存 Excel 来源教师；旧版紧急快照恢复时安全回退为受保护资料。

### 本次验证

- `git diff --check`、`npm run lint`、`npm exec prisma validate` 与 `npm run build` 全部通过；24 个页面和 API 成功构建。
- 使用一次性 SQLite 和真实 multipart 上传 API 重导 8 班 `BED`：返回 200，`BED_01` ID、教师和 `AAA_01` 关联全部不变，数据库完整性为 `ok` 且无外键错误。
- 把工作簿从 8 班增加到 9 班时只新增 `BED_09`；再次改变 Excel 教师后，`BED_09` 保持同一个 ID并从 `WAN XIN` 自动更新为 `TAN CHOON PENG`，证明新版来源追踪生效。
- 把数量安全降回 8 班时只删除无手工资料的 `BED_09`，原 8 个班次与全部学生班级保持不变。
- 尝试继续缩减并删除受保护的 `BED_08` 时得到 409；事务回滚后仍为 8 个班次、分配总数仍为 8、全部班级关联仍存在。
- 临时加入一条已排课程后重导得到清楚的 409；课次星期、时间和 revision 完全不变。所有测试只使用 `/private/tmp` 下的一次性数据库。

### 下一步

1. 给 Course Sections 教师与学生班级编辑增加独立 revision，防止两位老师同时保存时后提交者静默覆盖。
2. 修复待排课程首次放置的并发 409、停用教师规则和发布级 Excel 解析依赖。

## 2026-08-11｜Course Sections 增加多人编辑 revision

### 已完成

- `course_sections` 新增独立 `revision`，专门保护一个班次共享的教师和学生班级；旧 SQLite 启动时自动补为 revision 1，新数据库和 Prisma 模型保持一致。
- Course Sections API 现在要求客户端提交 revision，并用 `UPDATE ... WHERE id = ? AND revision = ?` 做数据库级 compare-and-swap；旧表单保存返回明确 409，不会覆盖另一位老师已经完成的修改。
- 教师、班级关联、section revision、该班次全部课次 revision 与重新计算的 warnings 现在放在同一个外层事务中；任何一步失败都会整体回滚。
- Inspector 会先比较教师和标准化后的班级集合：只有共享分配真的改变才增加 section 与其他 occurrence 的 revision；单纯移动时间或更换教室只增加当前课次 revision。
- Teaching allocation 自动更新自己维护的教师时也会增加 section revision，使已经打开的 Sections 表单及时失效；教师没有变化时不制造无意义版本号。
- 前端 CourseSection 类型和 PATCH body 增加 revision；409 时重新读取最新 Sections，表单使用 `section.id + revision` 作为 key，确保非受控下拉框与复选框真实重建为最新资料。
- Save 增加即时 ref 锁与 Saving 状态，快速双击不会发出两个相同 revision 请求；网络中断通过 `catch/finally` 恢复按钮并显示可重试说明。
- 把原本整行压缩的班次表单拆为易读 JSX 小区块，并补充中文业务注释、教师下拉可访问名称和包含班次编号的保存按钮名称。

### 本次验证

- `git diff --check`、`npm run lint`、`npm exec prisma validate` 与 `npm run build` 全部通过；24 个页面和 API 成功构建。
- 在一次性旧数据库启动生产构建后，`course_sections` 自动出现 `allocation_teacher_id` 与 `revision` 两列；GET Sections 的全部旧班次均返回 revision 1。
- 模拟两个账号同时读取 `BED_01` revision 1：第一位把学生班级改为 `CICTP_02` 后成功得到 revision 2，两条每周课次 revision 同步增加；第二位仍用 revision 1 保存时得到 409，数据库继续保持第一位的班级。
- 缺少 revision 的请求得到受控 400，资料零变化。
- 纯移动 occurrence 1 从周一 09:00 到周二 10:00 后，当前课次 revision 5→6；section 仍为 2、occurrence 2 仍为 2，证明没有错误影响共享资料或另一课次。
- 从 Inspector 把学生班级改为 `CSF_03` 后，section revision 2→3、当前课次 6→7、另一课次 2→3；旧 Sections revision 2 随后得到 409。
- 浏览器实际保留旧版 `BED_01` 表单，第二个会话先改回 `CICTP_02`；旧表单按 Save 后显示“changed by another scheduler”，并自动重载为 `CICTP_02` 已勾选，未覆盖最新资料。
- 在一次性数据库执行 Start new cycle 后立即 Restore：`BED_01` section revision 4、两条课次 revision 8／4、`CICTP_02` 关联和 Excel 来源状态全部按快照恢复。
- 最终 `PRAGMA integrity_check` 为 `ok`、`foreign_key_check` 为空；全部测试只修改 `/private/tmp` 下的一次性数据库。

### 下一步

1. 把待排课次首次放置的唯一键错误转换为清楚的 409，并让界面重新载入最新总表。
2. 修复教师停用后的 warning、候选位置与 Inspector 显示语义。
3. 升级存在漏洞的 Excel 解析依赖，并加入受控工作表和行数解析限制。

## 2026-08-11｜首次放置课次增加跨账号并发保护

### 已完成

- 新增首次排课专用的输入错误与多人冲突错误类型；同一个 `section + occurrence` 已被其他账号先放置时，接口稳定返回 409 和 `LESSON_ALREADY_SCHEDULED`，不再把 SQLite 的唯一键原文当成普通 400 泄露给浏览器。
- 把班次读取、输入检查、启用教室检查、警告计算、课程插入、全表警告刷新和响应资料查询放进同一个 `IMMEDIATE` 事务；警告重算失败时新课程会完整回滚，不会出现“界面显示失败但数据库其实已经保存”的半完成状态。
- 事务开始时先检查同一课次是否存在，同时保留数据库唯一键作为最终防线；SQLite 判断只使用稳定错误代码，不依赖可能随版本或语言改变的英文错误文字。
- 损坏 JSON 和已知排课输入继续得到受控 400；未知数据库故障只在服务器日志保留细节，浏览器只收到通用 500。
- 拖放、Clear slots 候选按钮和 Inspector 表单三个首次排课入口现在共用同一个保存函数，三条路径不会再出现各自不同的错误处理或刷新行为。
- 前端增加同步 `ref` 锁和可见的 pending state：快速双击、重复 drop 或同时点击另一候选不会发出第二个首次排课请求；相关待排卡、按钮、选择器和关闭动作在保存及随后刷新期间会显示等待状态并暂时停用。
- 收到 409 后自动重新读取当前年级的总表、待排清单和问题；重载成功会要求老师检查另一账号保存的位置，重载失败则明确要求手动刷新，绝不会错误宣称已取得最新资料。
- 年级总表加载函数现在能捕获断网异常并返回明确成功／失败结果；首次排课请求超过 30 秒会主动解除界面锁，并提示老师先刷新确认结果，再决定是否重试，避免因结果未知而重复建立课程。
- 已排课程、待排清单和问题清单的三个响应会先全部解析成功，再一起更新画面；网络在中途断开时保留完整上一版，避免出现新旧资料混合的短暂状态。
- 多人冲突响应除了检查 HTTP 409，还会核对稳定的 `LESSON_ALREADY_SCHEDULED` code；未来其他业务 409 不会被错误当成“已被另一位老师排课”。Inspector 内容因刷新卸载后，键盘焦点会回到仍存在的关闭按钮。
- 把本次改动涉及的 Inspector 排课表单和候选结果从压缩单行 JSX 拆成分区结构，并补充初学者可读的中文注释，说明共享资料、选择器锁定和候选状态的业务原因。

### 本次验证

- `git diff --check`、`npm run lint` 与隔离副本中的 `npm run build` 全部通过；TypeScript、24 个页面和 API 均成功构建。
- 两个真实登录会话同时把同一课次放到不同位置，结果严格为一个 201 和一个 409；409 只包含业务 code 和清楚提示，不含 `SQLITE`、`UNIQUE constraint`、表名或列名。第三次旧请求仍稳定得到相同 409。
- 同一班次的 occurrence 1 与 occurrence 2 同时保存时两者都得到 201；两个不同班次放进同一时间也都能保存并产生正常提醒，证明并发保护没有误改“允许冲突但要警告”的产品规则。
- 使用 SQLite trigger 人为让 warning 更新失败，接口返回通用 500，目标课次行数仍为 0；证明 INSERT 和警告刷新位于同一个可回滚事务中。
- 启动两个独立 production standalone 进程并让它们共用同一隔离 SQLite 文件，同时提交同一课次，仍只得到一个 201 和一个 409，没有出现 `SQLITE_BUSY` 或 500；数据库最终只有赢家的一行。
- 浏览器先保留旧版 `ZZ_ROLLBACK_01` Inspector，第二个账号再从 API 先行保存；旧页面点击 Place 后显示“already been placed by another scheduler”，自动显示赢家的星期与时间、清除旧 Inspector，并从待排区移除该课次。
- 浏览器对 `ZZ_UI_DOUBLE_01` 的 Place session 执行快速双击，服务器日志只有一个 POST，数据库也只有一条课次记录。
- 最终 production 构建中从 Inspector 正常放置 `ZZ_FINAL_UI_01`：锁保持到总表、待排区和问题清单全部刷新完成，待排数量变成 0、课程卡出现在 08:00、旧排课表单清除，并显示完整 warning 结果。
- 最终 `PRAGMA integrity_check` 为 `ok`、`foreign_key_check` 为空；以上账号、课程、故障 trigger 和数据库均只存在于 `/private/tmp/timetabling-placement.3L57t1`，没有修改老师正在使用的本地资料。

### 下一步

1. 修复教师停用后已有课程 warning、候选位置和 Inspector 下拉仍可能使用旧教师的关联语义。
2. 升级存在已知漏洞的 Excel 解析依赖，并限制只解析 Teaching Members 工作表和允许的最大行数。
3. 继续补齐 CRUD 自动化回归、主数据跨账号同步、规则性能和发布安全检查。

## 2026-08-11｜保留停用教师关联并实时显示严重警告

### 已完成

- 明确统一教师状态语义：`Inactive` 只表示不能建立新的教师分配，不会删除或清空既有 Teaching allocation、课程班次、已排课程、个人课表或不可用时段关联。
- 教师停用／启用和全部课程 warning 重算现在位于同一个 `IMMEDIATE` SQLite 事务；重算中途失败时，教师状态、更新时间和已经开始更新的 warning 会全部回滚。
- 统一警告引擎新增 `Teacher is inactive`，问题清单明确归类为 `Availability / High`；已有停用教师的课程仍可移动、换教室、改学生班级或手工排入总表，但会持续显示红色严重警告。
- Course Sections 和已排课程 PATCH 只在教师 ID 真正改变时要求目标教师为 Active；保留原停用教师并修改其他字段可以正常保存，尝试把停用教师改派到另一班次会得到受控 400。
- 教师变更验证和数据库写入改用 `IMMEDIATE` 次序，并在持有写锁后再次检查 Active 状态，避免另一服务进程恰好在验证与保存之间停用教师。
- 新增共用 `TeacherSelect`：当前教师始终使用稳定 option；停用时显示 `Inactive — current assignment` 和说明，Active 教师仍可改选。切换课程或跨账号启用／停用时都不会落到 `Teacher pending`。
- 教师下拉改为受控草稿：若老师刚选择的新教师在保存前被另一账号停用，选项会保留为 `Inactive — choose another teacher` 并显示明确说明，不会静默跳回原教师或空值；继续保存会由服务端拒绝，原关联保持不变。
- Inspector 编辑表单加入 `lesson id + revision` key；不关闭 Inspector 连续点击两门课程时，星期、时间、教师和教室会按新课程完整重建，不会把上一门课的默认值误存到下一门。
- 待排接口增加 `teacherIsActive`；停用教师卡片显示红色 `Inactive teacher`，排在 Active PT／FT 之后且不再错误标成 PT priority。手工排课表单明确说明仍可保存但会产生严重警告。
- Clear slots 在教师缺失或停用时返回清楚说明，并把 Inspector 恢复到手工排课表单；网络错误也会解除 loading，不再用普通“零候选”掩盖资料问题。
- 个人课表重新列出停用教师并标记 `Inactive`，历史课程不会因教师离开 Active 名单而从界面消失。
- 五秒同步在年级总表和个人课表中同时刷新教师清单；年级总表还会按稳定 ID 更新已经打开的课程编辑器、手工排课和候选对象，使另一账号改变教师状态后，卡片、警告、下拉和说明在同一轮同步中更新。
- Teaching allocation 重导不再把既有教师自动重新启用；同一停用教师的原有自动分配可以保留稳定 ID，新增班次或把其他自动班次改派给停用教师会返回 409 并回滚整次导入。
- 教师状态接口补齐损坏 JSON、`null` 等非对象输入的受控 400；warning 重算异常只向浏览器返回通用 500，不暴露 SQLite 或 trigger 细节。
- 把 Inspector 原本压缩成一行的编辑表单拆成标题、问题、时间、教师、教室和操作区块，并补充初学者可读的中文业务注释。

### 本次验证

- `git diff --check`、`npm run lint`、`npm exec prisma validate` 与隔离副本中的 `npm run build` 全部通过；TypeScript 和 24 个页面／API 成功构建。
- 在一次性 SQLite 建立两位教师、三个班次、三组学生和两条已排课程；停用前 Clear slots 有 33 个候选，重新启用后仍有 29 个符合当时其他课程占用情况的候选。
- 使用 SQLite trigger 让 warning 重算在第二条课程中途失败：停用请求返回通用 500，教师仍为 Active，第一条已执行的 warning 更新也回滚；反向启用失败时教师仍为 Inactive，原 `Teacher is inactive` 仍完整保留。
- 正常停用后，年级课程保留相同 teacher ID／姓名与 revision，warning 精确包含 `Teacher is inactive`、最高等级为 High；问题清单为 Availability／High，待排接口返回 `teacherIsActive: false`。
- 已排课程保持原停用教师并只改开始时间成功，revision 正常增加且 warning 保留；Course Sections 保留同一停用教师并改学生班级成功；把另一 Active 教师班次改派给停用教师得到 400 且资料不变。
- 停用教师的待排班次使用普通手工 POST 得到 201 和 High warning，证明 Clear slots 限制没有错误变成禁止保存；退回待排区后关联仍完整。
- 个人课表 API 和真实界面都能选择停用教师并查看其历史课程；重新启用后关联不变、inactive warning 清除、候选时段恢复。
- Teaching Members 最小工作簿首次建立一班后，停用教师再导入相同工作簿得到 200，教师保持 Inactive、班次 ID／revision／来源教师和分配数量不变；把数量从 1 增为 2 得到明确 409，数据库仍只有原班次和原分配。
- 另一门自动维护课程尝试从 Active 教师改派到 Inactive 教师同样得到 409；班次教师、来源教师、revision 和 teaching allocation 全部保持原值。
- 浏览器实际验证待排卡红色状态、Inspector 当前停用教师选项、Course Sections 三个班次下拉、Clear slots 回到手工表单、个人课表停用教师选项，以及只改时间后教师未被清空。
- 不关闭 Inspector 从停用教师课程切到 Active 教师课程再切回，教师与开始时间两次都恢复正确默认值。
- 双会话最终验收：第二会话停用教师后，打开中的 Inspector 在五秒内自动出现 High warning 且仍选中原教师；再次启用后 warning 与卡片标记自动消失，教师仍选中且 `Teacher pending` 没有被选中。
- 未保存草稿竞态验收：课程原教师为 `TEACHER A`，界面先选择 `TEACHER B` 但不保存；第二会话停用 B 后，五秒同步仍选中并显示 `TEACHER B (Inactive — choose another teacher)`，没有回退到 A 或 pending。点击 Save 得到 `Choose an active teacher.`，数据库 section 仍为 A、section revision 与 lesson revision 均未变化。
- 最终 `PRAGMA integrity_check` 为 `ok`、`foreign_key_check` 为空；全部账号、工作簿、trigger、教师和课程仅存在于 `/private/tmp/timetabling-inactive.fnaMNT` 隔离环境，没有修改老师正在使用的本地数据库。

### 下一步

1. 升级存在已知漏洞的 Excel 解析依赖，并限制解析器只读取 Teaching Members 工作表和允许的最大行数。
2. 建立可重复执行的 API／SQLite CRUD 与外键行为自动化回归，不再只依赖开发日志中的一次性验收记录。
3. 减少问题清单轮询造成的全库 warning 重算与写入，并补齐数据库索引和并发性能基准。

## 2026-08-11｜升级 Excel 解析器并收紧上传边界

### 已完成

- 把存在已知安全漏洞的 npm `xlsx 0.18.5` 替换为 SheetJS 官方 Community Edition 0.20.3；官方安装包保存在 `web/vendor`，部署不需要临时访问第三方 CDN，也不会因公共 npm 仓库版本落后而退回旧版。
- 本地供应说明记录了官方下载地址、Apache-2.0 许可证和 SHA-256 校验值；`package-lock.json` 固定到同一份 tarball，干净安装可重复得到完全相同的解析器。
- Teaching Members 导入现在在解析阶段只建立目标工作表对象，不再建立其他工作表对象；同时把读取上限提前到解析器，最多读取表头、5,000 条业务资料和 1 条超限检测行。
- 增加 SheetJS `!fullref` 截断检查：即使前 5,002 个物理行含大量空行，只要后面仍有资料，也会拒绝整份文件，绝不会把尾部资料静默遗漏后继续导入。
- 损坏的 multipart 上传现在得到清楚的 400；文件格式或工作表解析失败保持安全的 400；数据库保存失败改为通用 500，三类问题不再被混成同一个“文件无法读取”提示。
- 解析和数据库写入分成两个明确阶段；只有全部表头、行数和字段验证通过后，才调用现有 SQLite 事务一次性更新教师、课程、分配和班次。
- 保留 20 MB 文件上限，并修正中文注释，明确该限制发生在 multipart 已读取之后、复制到 Excel 解析缓冲区之前，避免夸大它对请求体内存的保护范围。
- 保留了工作树中老师尚未提交的 UX 验收脚本和文档，本次依赖修改只会局部暂存自己的 `xlsx` 行。

### 本次验证

- 官方 tarball 的 SHA-256 为 `8dc73fc3b00203e72d176e85b50938627c7b086e607c682e8d3c22c02bb99fe8`；包内版本为 0.20.3、许可证为 Apache-2.0，lockfile 的 SHA-512 integrity 与文件一致。
- 在完全独立的临时目录执行 `npm ci --ignore-scripts`，成功安装 530 个包；`npm ls xlsx` 和运行时版本都只显示 0.20.3，随后 production build 的 24 个页面／API 全部成功构建。
- `npm audit --omit=dev` 返回 0 个已报告生产依赖漏洞；同时直接检查最终服务器代码包含 0.20.3 且不含 0.18.5，避免只依赖对本地 file package 覆盖有限的在线审计结果。
- `git diff --check`、`npm run lint`、独立 TypeScript 检查、Prisma schema／migration 检查与 production build 全部通过。
- 使用真实登录会话上传老师提供的 16 MB Teaching Allocation：返回 200，导入 52 门课程、87 位教师、184 条分配和 374 个班次，并正确忽略 29 条零分配记录。
- 目标工作表隔离测试中，工作簿另含 10,000 行 Decoy 表；解析结果只建立 `Teaching Members`，API 只导入目标课程，数据库没有 Decoy 资料。
- 5,000 条资料得到 200，并正确统计 4,999 条零分配行；5,001 条资料得到 400。第 5,003 物理行才出现第二条资料的稀疏工作簿也得到 400，证明 `!fullref` 防止静默截断。
- 缺少目标工作表得到受控 400；损坏 multipart 得到 JSON 400；20 MB 加 1 字节文件得到 413；含 `__proto__`、`constructor` 与 `prototype` 表头的烟测没有修改全局对象原型。
- 使用 SQLite trigger 在第二个班次写入时人为失败，接口只返回安全的通用 500；教师、课程和班次数量与测试前完全相同，失败课程为 0 条，`integrity_check` 为 `ok`、`foreign_key_check` 为空。
- 全部测试工作簿、账号、数据库、故障 trigger 和干净安装目录只建立在 `/private/tmp/timetabling-xlsx-security.kre5vQ`，没有修改老师正在使用的本地数据库；验收结束后临时目录已删除。

### 下一步

1. 建立可重复执行的 API／SQLite CRUD、外键、revision 和 Excel 边界自动化测试脚本，把本次一次性验收变成后续每次提交都能运行的回归门槛。
2. 为上传解析增加更严格的 ZIP 条目数／总解压大小或隔离子进程限制；当前 20 MB、目标工作表和行数上限属于纵深保护，不能完全阻止所有压缩炸弹。
3. 减少问题清单轮询造成的全库 warning 重算与写入，并补齐数据库索引和并发性能基准。

## 2026-08-11｜保护班次、教室与退回待排区的资料关联

### 已完成

- 修正课程班次数量缩减的资料保护：准备删除的尾部班次只要已有排课、教师或学生班级中的任何一项，接口都会返回 409，并要求老师先明确清除分配；不再静默删除“已分配教师但尚未排课”的班次。
- 缩减班次的检查和删除改到同一个 `IMMEDIATE` SQLite 事务中；另一服务进程不能在检查通过后、真正删除前又为该班次加入教师或学生班级。
- 已排课程修改现在会验证教室 ID：不存在的教室得到安全的 400，新改派到 Inactive 教室同样被拒绝；课程原本已经引用的 Inactive 教室可以在只调整时间、教师或学生班级时继续保留，避免隐藏的既有资料被意外清空。
- Inspector 新增受控教室选择器：当前 Inactive 教室会稳定显示为 `Inactive — current assignment`，不会因为 Active 清单中没有对应 option 而跳到 `Room pending`；未保存的新教室若被另一账号停用，也会保留草稿并明确要求重选。
- 年级总表的五秒同步现在同时刷新教室清单；另一账号停用／启用教室后，课程 warning、教室标签和选择器说明会一起更新，同时不会覆盖老师尚未保存的选择。
- 教师、教室、学生班级、revision、位置更新和 warning 重算全部放入同一个 `IMMEDIATE` 事务。任何验证或重算失败都会一起回滚课程位置、共享班次分配和全部 revision。
- 为已排课程编辑新增明确的 revision 冲突与输入错误类型：旧页面修改返回 409，可修正资料返回 400；未知 SQLite 或 warning 故障只在服务器记录细节，浏览器只收到通用 500，不再暴露外键或表名。
- 退回待排区的 DELETE 与 warning 重算改成同一个事务；如果重算失败，课程仍留在原位置，老师不会遇到“画面说失败但资料其实已经删除”的假失败。
- DELETE revision 参数现在必须是正整数；缺少、空值、0 或损坏文字都得到受控 400，不会把缺少参数误当成 revision 0。
- 把本次修改涉及的路由验证和事务区块补充为初学者可读的中文注释，并将复杂条件拆成具名判断，方便后续老师或基础开发人员维护。

### 本次验证

- `node --check scripts/verify-api-crud.mjs`、`git diff --check`、`npm run lint`、独立 TypeScript 检查和完整 `npm test` 全部通过；production build 的 24 个页面／API 成功生成。
- 隔离数据库把第三个尾班分配教师但不分配学生班级、也不排入总表；尝试把班次从 3 减到 2 得到 409，尾班 ID、教师和 revision 保持不变。清除教师后再次缩减才成功。
- 使用随机教室 ID 修改课程得到受控 400，响应不含 `SQLite`、`FOREIGN KEY`、表名或列名；把课程新改派到 Inactive 教室也得到 400，原教室和 revision 均未改变。
- 停用课程当前教室后，只把开始时间从 10:00 改到 11:00 能正常保存；返回资料保留相同 room ID／code、revision 正常增加并继续包含 `Room is unavailable`，证明既有引用与新分配使用不同规则。
- 浏览器在独立 production 服务中实际打开 `ROOM_QA_01`：Inspector 的 Room 明确选中 `99-09-09 (Inactive — current assignment)`，没有落到 `Room pending`。把开始时间从 09:00 改到 10:00 保存并重新打开后，时间、相同 room ID 和停用标签全部保留，`Room is unavailable` 仍可见，控制台没有 error／warning。
- 使用 SQLite trigger 人为让退回待排区后的 warning 更新失败：DELETE 返回通用 500，目标课程仍以相同 ID、位置和 revision 留在总表。删除 trigger 后，用同一 revision 重试即可正常退回待排区。
- 两个并发首次排课请求仍严格得到一个 201 和一个带 `LESSON_ALREADY_SCHEDULED` 的 409；总表只存在赢家的一条记录，冲突响应不含 SQLite 或唯一键细节。
- 最终 `PRAGMA integrity_check` 为 `ok`、`foreign_key_check` 为空；测试只使用操作系统临时目录下的一次性数据库，正式 `web/data/timetabling.db` 修改时间保持 `2026-08-11 01:06:13`，测试结束后临时目录为空。

### 下一步

1. 把本次使用的 API／SQLite 隔离回归脚本正式加入项目，并接入统一 `npm test` 命令。
2. 自动化 Teaching allocation 重导后的稳定 ID、手工分配保留、受保护缩减和 Inactive 教师继承语义。
3. 把学生班级、教室、规则与不可用时段等其余“写入后重算 warning”路径逐步改成同一事务，并减少问题清单轮询造成的全库写入。

## 2026-08-11｜加入 production API、SQLite 与 Excel 自动化集成回归

### 已完成

- 新增统一 `npm test`／`npm run test:integration`：每次先生成最新 production standalone build，再运行 API、SQLite、Excel、revision 和资料关系回归，避免误测旧构建或只验证开发模式。
- 测试脚本每次使用 `os.tmpdir()` 下的全新 SQLite 文件和操作系统分配的随机本机端口；启动前清除继承的 Railway、正式数据库和端口环境变量，绝不会打开 `web/data/timetabling.db`。
- 直接启动 standalone Node 进程，不经过额外 npm／shell 子进程；启动失败、端口竞争、请求超时、断言失败、Ctrl-C 和 CI `SIGTERM` 都共用幂等清理，保证子服务与临时目录不会留在后台。
- 自动创建一次性管理员并验证 401、首次 setup、安全 Cookie、注销和旧 Cookie 失效；测试不会输出密码、完整 Cookie 或请求头。
- 抽出 production API 与测试共用的 Teaching Members 工作表解析器：目标表名称、5,000 行上限、额外超限检测行和 `!fullref` 截断保护只有一份实现。测试直接读取同一次解析建立的 Worksheet 名称，不再用复制的 SheetJS 选项或脆弱源码正则产生假阳性。
- Excel 回归覆盖固定 SheetJS 0.20.3、目标表前置 Decoy、恰好 5,000／5,001 行、稀疏尾行、损坏 multipart、20 MB 上限和原型字段烟测；被拒绝的 5,001 行文件使用完整业务表快照证明零写入。
- Excel 回归同时覆盖缺少 `Teaching Members`、缺少必要表头和扩展名正确但 ZIP 内容截断；三类请求都得到安全 400，响应不泄露 SheetJS／ZIP／XML 技术文字，完整业务表快照保持不变。
- Teaching allocation 重导自动覆盖：课程／班次稳定 ID、相同工作簿不提高无变化 revision、人工教师与学生班级保留、受保护尾班缩减 409、Inactive 原教师 grandfather、新增 Inactive 班次 409，以及课程排入总表后整份重导 409；三类拒绝都验证事务零变化。
- CRUD／关系回归覆盖教师、班级、教室、课程、班次分配、课程位置、共享 revision、旧 revision 409、无效／停用教室、Inactive 教师／教室 warning、首次排课重复 409、并发请求唯一赢家和退回待排区。
- 使用 SQLite trigger 故障注入验证 DELETE 与 warning 重算原子回滚；同时验证受保护尾班的 ID／教师／revision，以及失败删除后的星期、开始时间、教室和 revision 全部不变。
- 服务停止后直接执行 SQLite `RESTRICT`、`SET NULL`、`CASCADE`、`integrity_check` 和 `foreign_key_check`，确认教师、班级、教室、课程、班次和已排课程没有悬空关联。
- 测试脚本各小区块均加入面向基础开发人员的中文注释；正常输出按业务保证分组，失败时只输出有上限的服务器日志尾部，便于定位且避免终端被大量资料淹没。
- 保留老师工作树中尚未提交的 UX 测试命令、脚本和文档；本次只会局部暂存 `test`／`test:integration` 两条 package 命令和自己的集成测试文件。

### 本次验证

- `node --check scripts/verify-api-crud.mjs`、`git diff --check`、`npm run lint`、独立 TypeScript 检查与完整 `npm test` 全部通过；production build 的 24 个页面／API 成功生成。
- 完整测试依次通过：身份保护与管理员、Excel 安全边界、Teaching allocation 五类重导关系、教师／班级／教室／课程／班次／排课 revision、注销、SQLite 外键与完整性。
- 5,001 行工作簿拒绝前后的教师、班级、教室、课程、allocation、section、section-group 和 lesson 全表快照完全相同；Decoy 工作表没有建立 Worksheet 对象，数据库也没有 Decoy 课程。
- 相同 Teaching allocation 重导后，课程 ID、两个班次 ID、第一班 revision 和人工维护第二班 revision 全部保持；人工教师和学生班级没有被 Excel 覆盖。缩减、Inactive 新增和已排课程重导的 409 前后完整业务快照相同；已排课程冲突工作簿还刻意修改目录并加入一门 companion 课程／教师，证明 409 前没有部分提交其他课程。
- 两条并发 HTTP 首次排课请求严格得到一个 201 和一个带 `LESSON_ALREADY_SCHEDULED` 的 409；总表只有赢家一行，响应不含 SQLite／UNIQUE／constraint 文字。
- warning trigger 故障时退回待排区得到安全 500，课程的星期、时间、教室和 revision 全部保持；移除 trigger 后用同一 revision 正常删除。
- 手工发送 Ctrl-C 后进程使用标准退出码 130 结束，`os.tmpdir()` 下没有残留 `timetabling-api-crud-*`；完整正常测试退出码为 0，正式数据库修改时间仍为 `2026-08-11 01:06:13`。

### 下一步

1. 把同一课次首次排课的并发回归扩展到两个独立登录会话和两个共享 SQLite 的 standalone 进程；当前自动化已覆盖同进程并发请求，跨进程路径仍只有之前的一次性验收记录。
2. 为学生班级、教室、规则与不可用时段的写入／warning 重算补齐 trigger 故障回滚，并统一未知数据库错误的 500 响应。
3. 减少 `/api/issues` 每五秒触发的全库 warning 写入，补齐 runtime 数据库索引和满负载性能基准。

## 2026-08-11｜主资料与 warning 重算改为原子事务

### 已完成

- 学生班级资料修改改为 `IMMEDIATE` SQLite 事务：编号、年级、专业与全部课程 warning 在同一次提交中保存；warning 重算中途失败时，班级资料和已经开始更新的 warning 会一起回滚。
- 教室资料修改与 Active／Inactive 状态切换采用同样的原子事务；容量、设施、Block、状态和 `Room is unavailable` 等警告始终来自同一个数据库版本，不再出现接口说失败但教室其实已经改变的半完成状态。
- 教师及年级不可用时段的新增／删除与 warning 重算合并为一个事务；删除失败时原时段仍存在，新增失败时不会留下没有对应警告的时段。
- 新增 Teacher 不可用时段时，数据库在取得写锁后再次确认教师 ID 存在；错误 ID 得到安全的 400，避免把外键错误或表结构发送到浏览器。
- 可修改规则的开关与全库 warning 重算改为原子事务；开关更新失败时规则值和所有课程警告均保持原状。
- 建立 `MasterDataUniqueConflictError` 与 `MasterDataInputError`：只有稳定的 `SQLITE_CONSTRAINT_UNIQUE` 会成为学生班级／教室编号重复的 409；trigger、磁盘或其他未知数据库故障只写服务器日志，并向界面返回通用 JSON 500。
- 学生班级、教室、规则和不可用时段的 JSON 路由统一处理损坏 JSON、`null`、数组与无效字段，全部返回可修正的 400；未知故障不会穿过 Next.js 形成 HTML 错误页。
- 教室设施字段改为严格布尔值验证；字符串 `"false"` 不会再被 JavaScript 的 `Boolean("false")` 错误保存成 `true`。Smart Classroom 仍会按照院系规则自动同时标记为 Multi Projector。
- 自动化完整业务快照新增教师不可用时段、年级禁排时段和规则开关，后续任何原子回归都会同时检查这些关系，而不只比较课程数量。
- 保留老师工作树中尚未提交的 UX 测试命令、脚本和文档；本次没有暂存或修改这些文件。

### 本次验证

- `node --check scripts/verify-api-crud.mjs`、`git diff --check`、`npm run lint`、独立 TypeScript 检查和完整 `npm test` 全部通过；production build 的 24 个页面／API 成功生成。
- 在一次性 SQLite 中建立真实课程后，用 `BEFORE UPDATE OF warnings_json` trigger 强制 warning 重算失败；随后依次修改学生班级、教室资料、教室状态、`lunch_break` 规则、新增 Teacher 不可用时段和删除与现有课程重叠的 Year 不可用时段，六个请求均得到安全 500。
- 六次故障中的每一次都独立比较完整业务快照：教师、学生班级、教室、课程、Teaching allocation、班次、班级关联、已排课程、教师不可用时段、年级禁排时段和规则开关逐字段完全相同，证明不存在主资料先提交的半更新。
- 另为学生班级和教室新增分别建立 `BEFORE INSERT` trigger；非唯一键数据库故障得到通用 500，不会被误报成“编号已存在”，响应不含 `SQLite`、trigger、constraint、表名或列名，失败后完整业务快照不变。
- 六个 JSON 写入入口分别使用 `null` 和损坏 JSON 测试，全部得到 JSON 400；教室新增／修改传入字符串布尔值也得到 400，没有建立或修改资料。
- 删除故障 trigger 后，规则可以正常切换并恢复原值，Year 不可用时段可以正常删除；随机不存在的教师 ID 新增不可用时段得到明确 400 且数据库零变化。
- 原有身份、Excel 边界、Teaching allocation 重导、CRUD／revision、多人首次排课、注销、SQLite `RESTRICT`／`SET NULL`／`CASCADE`、`integrity_check` 与 `foreign_key_check` 回归仍全部通过。
- 测试只使用 `os.tmpdir()` 下自动清理的一次性数据库和随机 localhost 端口；正式 `web/data/timetabling.db` 修改时间仍为 `2026-08-11 01:06:13`。

### 下一步

1. 把新周期建立和上次周期恢复中的资料写入、快照与 warning 重算合并为同一个可回滚事务。
2. 把课程配置与相关课次 revision／warning 重算合并为同一个原子事务，并增加 trigger 故障回归。
3. 把 `/api/issues` 改为纯读取已保存 warning，避免每个账号五秒一次全库写入；随后补齐运行时索引和满负载性能基准。

## 2026-08-11｜新周期快照与恢复改为原子且防旧页面覆盖

### 已完成

- `Start new cycle` 现在先取得 SQLite `IMMEDIATE` 写锁，再从课程、Teaching allocation、班次、班级关联和已排课程五张表读取同一个版本的快照；其他账号不能在快照的多次查询之间写入，也不能在快照完成后、清空前加入一门不在备份中的课程。
- 空周期检查、删除旧应急备份、写入新快照和级联清空课程全部位于同一个事务；任何 trigger、磁盘或数据库错误都会同时恢复原课程与原备份。
- `Restore emergency backup` 在取得写锁后才读取并验证最新备份；当前活动周期删除、课程／分配／班次／排课恢复和当前规则下的全部 warning 重算位于同一个外层事务，重算失败时整次恢复完整回滚。
- 周期写事务直接返回自己刚建立的状态，不再提交后重新调用状态查询；避免操作已成功提交、但第二次读取失败后界面却收到失败的假失败。
- Cycle GET 把五张当前周期表和最新备份放入同一个只读事务快照，课程数量、班次数量、课次数量和备份不会再来自两个并发版本。
- Cycle 状态新增不可逆的 `currentToken`（稳定周期快照 SHA-256）。Start 和 Restore 都必须提交老师打开页面时看到的 token；另一个账号保存课程后，旧页面得到 409 并要求刷新复核，不会清空或覆盖老师未见过的新工作。
- Restore 额外提交页面显示的 `backupId`，并在写锁内核对当前最新备份；若另一账号已经开始了新周期并替换应急快照，旧页面不能悄悄恢复一份从未确认过的新备份。
- Cycle 页面已把 `currentToken` 和 `backupId` 自动加入现有三重确认请求；界面仍只显示计数、时间和确认短语，不显示完整备份内容或 hash 技术细节。
- 损坏、`null`、数组 JSON、错误 action、错误确认短语和缺少绑定字段统一得到 JSON 400；空周期、无备份、备份改变、当前周期改变和无效快照使用明确 409。
- 未知 SQLite／trigger 故障只写服务器日志并返回固定通用 500；稳定识别到 `SQLITE_BUSY`／`SQLITE_LOCKED` 时返回安全 503，提示另一位老师正在更新周期，且连接初始化阶段的锁错误也不会漏成 500。
- 无效备份解析新增“至少一门课程”不变量。合法 Start 永远不会为零课程建立备份，因此五个空数组不能被当成有效恢复并静默清空当前周期。
- 自动化完整快照新增 `schedule_backups` 和账号资料，并把多表读取本身放入同一个只读事务；测试断言不会因为跨两个数据库版本而产生假阳性。

### 本次验证

- `node --check scripts/verify-api-crud.mjs`、`git diff --check`、`npm run lint`、独立 TypeScript 检查和完整 `npm test` 全部通过；production build 的 24 个页面／API 成功生成。
- 管理员建立一个普通 scheduler 并取得独立 Cookie；未登录 GET／POST Cycle 得到 401，普通账号完整执行 GET、Start、Restore 后主动注销，证明“所有账号可排课、仅账号创建受管理员限制”的权限保持不变。
- `null`、数组、损坏 JSON、未知 action、两种错误确认短语、缺少 `currentToken` 和缺少 `backupId` 均得到 400；每次请求后的完整业务快照与请求前逐字段相同。
- 页面取得 token T0 后，经 production API 新增 `CYCLE_TOKEN_MARKER`；使用旧 T0 Start 得到明确 409，marker 完整保留。重新 GET 的 T1 为不同的 64 位 SHA-256，连续两次无修改 GET 返回相同 token。
- Start 故障测试先直接加入一份合法旧备份 sentinel，再用 `BEFORE DELETE ON courses` trigger 强制清空中途失败；接口只返回固定通用 500，当前五张周期表、全部基础资料和旧备份逐字段完全不变。
- 移除 trigger 后用同一 T1 正常 Start：当前五张周期表全部为空，新备份原子替换 sentinel；解析后的 snapshot JSON 与清空前课程、分配、班次、学生班级关联、排课位置、warning 和 revision 完全相同。
- 空周期再次 Start 得到 409，已建立的新备份没有被替换；使用随机错误 `backupId` Restore 也得到 409，当前周期与备份均零变化。
- 临时把备份内容改成五个空数组后 Restore 得到 `The emergency backup is not valid.`，没有清空资料；测试随后原样还原合法 snapshot JSON。
- 空活动区建立 `RESTORE_CURRENT`，取得旧 token 后再建立 `RESTORE_STALE_MARKER`；旧页面 Restore 得到明确 409，两门新课程和全部关系都完整保留。
- Restore 故障测试在备份排序最后一条课程的 warning UPDATE 上建立 trigger；接口返回固定通用 500，恢复前的两门活动课程、备份 JSON、全部 ID／revision／warning 和保留资料逐字段完全相同，证明前面已执行的恢复写入也全部回滚。
- 删除 trigger 后使用相同 token 正常 Restore：两门临时活动课程消失，备份中的五张周期表逐字段恢复，原 backup ID／JSON 保持；POST 响应和后续 GET 的数量、token 与备份一致。
- 完整测试末尾原有 SQLite `RESTRICT`／`SET NULL`／`CASCADE`、`integrity_check` 与 `foreign_key_check` 继续通过，两个登录会话均已注销；正式数据库修改时间仍为 `2026-08-11 01:06:13`。

### 下一步

1. 把课程配置、相关课次 revision 和 warning 重算合并为一个 `IMMEDIATE` 事务，并增加中途故障回滚测试。
2. 把 `/api/issues` 改为纯读取已保存 warning，避免每个账号五秒一次全库写入；同时确认所有资料变更入口都主动刷新必要 warning。
3. 补齐运行时数据库索引，并用接近实际 374 个班次、多账号轮询的资料量建立性能基准。

## 2026-08-11｜课程 Setup 改为原子保存并防止旧表单覆盖

### 已完成

- 为每门课程新增独立 `revision`，并让 Courses 清单、Configure 表单、PATCH API、Prisma 模型和紧急周期快照／恢复共同读写同一个版本号；旧数据库会自动为既有课程补上 revision 1。
- Course Configure 保存会提交页面打开时读到的 revision；数据库使用 `UPDATE ... WHERE revision = ?` 比较后更新。另一账号已经先保存时，旧表单得到带 `COURSE_SETUP_CHANGED` 的 409，不再以最后提交者覆盖赢家。
- 浏览器收到课程版本冲突后会关闭已经过期的表单、重新加载 Courses 清单，并明确要求老师重新打开 Configure 复核最新设置；若刷新失败则要求整页刷新，不会暗示旧表单仍可安全重试。
- 课程设置的状态检查、课程字段／revision 更新、该课程全部已排课次的冗余时长／revision 更新和全库 warning 重算，全部合并到一个 `IMMEDIATE` SQLite 事务。
- “已有排课不能清空主年级”“occurrence 2 已排时不能降为每周一次”“晚课会超过 18:00 时不能增加时长”三项检查改到写锁内执行，另一服务进程不能在检查通过后插入一条让新设置失效的课程。
- 所有课程设置都完全相同时返回成功 no-op，但不修改 `updated_at`、课程 revision、课次 revision 或 warning；老师重复按 Save 不会无意义地让其他打开中的 Inspector 过期。
- 首次排课入口在自己的 `IMMEDIATE` 事务中反向确认课程仍有主年级；即使旧页面握有班次 ID，也不能在另一老师清空年级后建立一条从 Year 1–3 三张总表全部消失的隐藏课程。
- Course Setup API 统一处理损坏 JSON、`null`、数组、布尔／数组伪装数字和字段范围；可修正输入为 400、当前排课状态冲突与旧 revision 为 409、SQLite 锁竞争为 503、未知 trigger／数据库故障为固定安全 500。
- 任何真实课程设置变化都会把该课程全部 occurrence 的 lesson revision 一起增加；课程规则改变前已经打开的 Inspector 随后保存会得到 409，不能在新规则上继续提交旧资料。
- 完整系统备份恢复不再依赖物理列顺序：结构校验会按列名排序后比较名称、类型、必填、默认值和主键，复制时为每张表显式列出目标／来源列名，不再使用顺序敏感的 `INSERT ... SELECT *`。因此经历多次 `ALTER TABLE` 的旧库备份可以恢复到同版本全新库，同时仍拒绝真正缺列或类型不兼容的文件。
- 全新数据库仍把课程 revision 建在表尾，与本次 `ALTER TABLE` 追加位置一致；Prisma 模型也使用相同顺序，减少以后人工检查 schema 时的困惑。
- 紧急周期快照解析会核对 lesson → section → course 的跨数组关系；只要任何已排课程的 `primary_year` 为空，就把快照视为无效并在删除当前周期前返回 409，旧快照不能绕过新的总表可见性规则。
- `/api/issues` 改为只读取原子写事务已经保存的 `warnings_json`；五秒轮询不再读取旧课时后写回 warning，既防止覆盖刚提交的 Course Setup 结果，也移除每个账号反复进行的全库 SQLite 写入。
- Configure PATCH 已经成功但随后 Courses 清单刷新失败时，界面会准确提示“设置已保存、最新清单未载入”并关闭旧 revision 表单，不再把刷新失败误报为保存失败。
- 新增的数据库、API、页面和测试区块均写入面向基础开发人员的中文注释；老师工作树里尚未提交的 UX 脚本、文档和两条 package 命令继续保持不动。

### 本次验证

- `node --check scripts/verify-api-crud.mjs`、`git diff --check`、`npm run lint`、独立 TypeScript 检查、Prisma schema 验证和完整 `npm test` 全部通过；production build 的 24 个页面／API 成功生成。
- 使用一次性 production 服务和隔离 SQLite，先让 Course Setup 的 warning 重算在最后一条课程上由 trigger 强制失败；接口精确返回 `{error: "The course setup could not be saved. Try again."}`，不含 SQLite、trigger、表名、列名或 stack。
- 故障请求前后完整业务快照逐字段相同：课程全部设置、课程 revision、两个每周课次的时长／revision／warning、班次 revision、基础资料和其他课程均没有半完成变化；删除 trigger 后用同一个 revision 重试成功。
- 正常把目标课程从 3 小时改为 4 小时并把最低容量从 20 改为 51：课程 revision 精确加 1，两条 occurrence 的时长同步为 4、lesson revision 各加 1并出现容量 warning；班次 revision 与其他课程 lesson revision 保持不变。
- 使用修改前的旧 Course Configure revision 再保存得到带稳定 code 的 409；使用课程规则改变前的旧 lesson revision 保存 Inspector 同样得到 409，两次失败后的完整业务快照与赢家版本相同。
- 两个同时发出的 production HTTP Configure 请求使用同一课程 revision，严格得到一个 200 和一个带 `COURSE_SETUP_CHANGED` 的 409；赢家 revision 只增加 1且容量值来自两个赢家候选之一。此项验证 API 的 CAS 契约，不冒充尚未自动化的跨进程 SQLite 锁测试。
- 恢复原 3 小时配置后课程和两个课次 revision 再各加 1，容量 warning 消失；后续原有移动、删除、多人首次放置、Cycle Start／Restore 与外键回归继续全部通过。
- 相同课程设置重复保存返回 `{changed:false}`，课程、课次、warning、`updated_at` 和所有 revision 完全不变；把每周次数从 2 降到 1时因 occurrence 2 已排得到 409且快照零变化。
- `null`、损坏 JSON、`durationHours:[3]` 和 `sessionsPerWeek:true` 都得到 JSON 400且完整业务快照零变化，证明 API 不会再依赖 JavaScript 宽松的数字强制转换。
- 另建立已设置 2 小时时长但主年级为空的课程；尝试首次排课得到明确 400，数据库中 `primary_year IS NULL` 的已排课记录数量保持 0。
- warning 故障 trigger 仍启用时读取 `/api/issues` 继续返回 200 且完整快照零变化；如果问题轮询恢复成写入 warning，这条回归会立即触发 500。
- 把合法紧急快照中一条已排课程的主年级人工改为 null 后，Restore 得到精确 `The emergency backup is not valid.` 409；当前活动周期和损坏备份本身逐字段不变，随后还原合法 JSON 才允许正常恢复。
- 通过管理员 production API 下载完整 SQLite 后，只在临时副本中把 `courses` 重建为真实历史升级库的不同物理列顺序；该文件通过完整恢复接口成功覆盖 fresh 测试库，恢复前后全部业务表逐字段相同，旧 Cookie 失效后原管理员能用原密码重新登录。
- 临时“历史顺序”备份在上传前通过 `integrity_check=ok` 与空 `foreign_key_check`；测试明确确认其列顺序不同于 fresh 库，因而不会用两个实际相同的 schema 产生假阳性。
- 集成测试仍只使用 `os.tmpdir()` 下自动清理的临时数据库和随机 localhost 端口。正在运行的本地开发应用已为正式数据库 52 门既有课程安全补上 revision 列，初始值全部为 1；`PRAGMA integrity_check` 为 `ok`、`foreign_key_check` 为空。

### 下一步

1. 把 `/api/issues` 改成纯读取已保存 warning，避免每个账号五秒一次全库写入，并确认所有 warning 依赖资料的修改入口都主动刷新。
2. 补齐 Prisma 中已经声明但 runtime SQLite 尚未建立的索引，并以接近实际 374 个班次、多账号轮询的资料量建立性能基准。
3. 把 Course Setup 的浏览器级双会话冲突加入可重复 UI 验收；当前 production API 已覆盖旧 Configure／旧 Inspector 两类 revision 保护。

## 2026-08-11｜补齐运行时索引并建立 374 班次性能回归

### 已完成

- 在 SQLite 实际初始化路径建立 12 个高频索引，覆盖登录会话、Active 教室、课程主年级、教师反向关联、学生班级反向关联、全局／教室／班次时段、教师不可用时段和年级禁排时段；普通主键与唯一键已经自动拥有的索引不重复建立。
- 索引集合按真实 SQL 查询计划收敛：没有保留教师类型排序、学生班级排序、Block 筛选和 Excel 来源教师等当前查询不会使用的低收益索引，减少每次拖放保存时不必要的索引写入。
- 学生班级、教师不可用和年级禁排的复合索引使用包含完整列名的新名称；旧开发版本即使曾建立同名前缀短索引，`IF NOT EXISTS` 也不会静默跳过新结构。
- 新增 `scheduled_lessons(section_id, day_of_week, start_hour)`；否则加入全局星期／时间索引后，SQLite 会在“同一班次是否同日”检查中错误优先扫描当天全部课程。
- Prisma schema 与 runtime SQLite 的 12 个索引名称、列和顺序完全一致；`prisma migrate diff` 生成的 SQL 已逐项核对，设计模型不再声明 runtime 没有部署的低收益索引。
- 数据库连接新增明确的 runtime schema 版本。每个 production 进程只初始化一次表、兼容列和索引；开发热重载发现版本变化时仍会自动补迁移，不再让每个五秒轮询请求重复解析 DDL 和兼容 UPDATE。
- 学生班级个人时间表从每节课执行 correlated `EXISTS` 改为直接 JOIN `section_student_groups(student_group_id, section_id)`；教师、教室分支仍不连接该表，跨年级课程会在两个参与班级中出现一次且只出现一次。
- 登录会话验证改为纯 SELECT；过去每个已登录 GET 都会先执行一次可能零匹配的过期会话 DELETE，即使没有删到资料也会尝试取得 SQLite 写锁。过期记录现在只在低频的成功登录／建立新会话事务中清理。
- 登录完成阶段新增 `IMMEDIATE` 复核：Scrypt 密码验证仍在写锁外执行，但真正建立会话前会重新确认账号仍 Active、密码哈希仍是刚才验证的版本，管理员并发停用或重置密码后旧请求不能补回新会话。
- 新增独立 `verify-api-performance.mjs`：production standalone、随机 localhost 端口和一次性 SQLite 固定生成 52 门课程、374 个班次、360 条已排课、184 条 Teaching allocation、87 位教师、54 个学生班级、30 间教室和 6 个独立 scheduler 会话。
- 性能夹具精确锁定三个年级已排数量 `123／122／115`、待排数量 `7／0／7`、270 条初始问题和所有主资料数量；错误返回空数组不能以“更快”的结果让性能门槛假通过。
- 测试为候选班次保留第 87 位教师、第 54 个学生班级和第 30 间教室，仍遍历完整满载资料，但确定存在真实可用候选；同时验证该保留教室确实出现在结果中。
- 纯读阶段建立 warning UPDATE trigger、session DELETE trigger，并由辅助连接持有 `BEGIN IMMEDIATE`；Issues、总表、待排区、候选、个人时间表和六账号轮询只要执行任何写语句（包括零行 DELETE）就会失败。
- 真实 SHA-256 过期 Cookie 得到 401，持锁期间过期记录保持且不影响读取；释放锁后的下一次成功登录会删除它，证明减少读锁竞争没有破坏会话过期和清理语义。
- Warning 写入性能先通过审计 trigger 记录 360 个不同 lesson ID，证明计时包含真实全量重算；之后才测五次 PATCH 和“一个写入 + 十个读取”，不会把漏刷新误判成性能提升。
- 新增 `test:performance` 和 `test:release`。日常 `npm test` 保持确定性的功能回归；发布门槛在同一次 production build 后依次运行完整 CRUD／Excel／Cycle 回归和规模性能回归，杜绝直接执行脚本误测旧 `.next`。
- 所有新数据库、查询、会话和性能测试区块都加入面向基础开发人员的中文注释；老师尚未提交的 UX 命令、脚本和文档保持在工作树中，本次提交只会选择性暂存自己的两条测试命令。

### 本次验证

- `node --check scripts/verify-api-performance.mjs`、`git diff --check`、`npm run lint`、独立 TypeScript 检查、Prisma validate／migration diff 和最终 `npm run test:release` 全部通过；production build 的 24 个页面／API 成功生成。
- 12 个 runtime 索引的名称和列顺序逐项与预期相同；另对课程年级、教师班次、学生班级真实 JOIN、教室时段、全局时段、同班次同日、教师不可用和年级禁排八条热点查询执行 `EXPLAIN QUERY PLAN`，均命中对应索引。
- 教师、学生班级和教室三类 Personal timetable 逐项对照 SQLite 精确 lesson ID，没有重复或遗漏；`PERF_C001_01` 同时关联 Year 1 与 Year 2 的两个班级，两个班级个人表及教师／教室视图都保留完整标签。
- 满载纯读最终基线：Issues p95 中位 `17.8 ms`，Year timetable p95 `14.8 ms`；六账号同时发出 30 个页面轮询请求的整轮中位 `51.2 ms`、单请求 p95 `52.8 ms`。
- 30 间教室的完整候选位置搜索中位 `553.8 ms`；360 条课次 warning 全量重算中位 `194.3 ms`；一个 warning 写入加十个并发读取的整轮中位 `232.8 ms`。门槛分别保留 1.5／6／10／15 秒级宽松余量，只拦数量级退化，不把普通电脑抖动误报成失败。
- 六个 scheduler 使用不同用户名和六个随机 Cookie；真实轮询、候选、Personal timetable 和写读混合请求全部得到预期数量，没有 500、503、超时或资料缺失。
- 性能与功能测试全部使用 `os.tmpdir()` 下的独立数据库，结束后没有残留 `timetabling-api-crud-*` 或 `timetabling-api-performance-*` 目录，测试没有连接正式数据库。
- 正在运行的本地开发服务在本轮热重载时确实为 `web/data/timetabling.db` 建立了索引，文件当前修改时间为 `2026-08-11 04:45:51`、大小 544,768 bytes；不能再声称正式数据库文件未修改。
- 正式库只读后验为 87 位教师、7 个学生班级、3 间教室、52 门课程、184 条 allocation、374 个班次、20 条已排课、各 1 条教师／年级不可用时段、1 个会话和 1 个应急备份；`integrity_check=ok`、`foreign_key_check` 为空。审查期间观察到索引迁移前后这些行数保持，但没有迁移前逐字段 hash，因此不夸大为正式业务记录逐字段不变。
- 正式库还保留本轮早期草案曾建立的 8 个冗余旧索引，共 20 个显式索引；它们不影响资料、查询正确性或备份恢复，只带来很小写入开销。为避免对老师数据库做不必要的 DROP，本轮不主动删除；全新数据库只建立最终 12 个索引。

### 下一步

1. 把登录密码重置／停用与登录完成的跨进程竞态加入双 standalone 可重复测试；当前事务顺序已由代码审查和单进程 production 回归确认。
2. 复用 `calculatePlacementWarnings` 的 prepared statements／批量上下文，减少候选搜索和满载 warning 重算仍会重复编译 SQL 的成本；当前 374 班次基线充足，但索引不是最终算法优化。
3. 建立双进程共享 SQLite 的首次排课、Course Setup、Cycle 和登录并发回归，并明确部署始终保持单应用实例。

## 2026-08-11｜建立双 production 进程共享 SQLite 的确定性并发回归

### 已完成

- 新增独立 `verify-cross-process-concurrency.mjs`，在操作系统临时目录建立唯一 SQLite，并启动 A、B 两个真正的 Next.js production standalone；两个进程使用不同随机端口、不同普通排课账号和不同 Cookie，但共同读取同一数据库。
- A 先完成空库初始化和管理员 setup，B 才顺序启动；业务竞态不会被第一次建表／建索引的冷启动竞争污染。两位普通排课账号分别通过 A、B 登录，再把 Cookie 交叉发送到另一进程，证明账号和 session 来自共享 SQLite，而不是 Node 进程内存。
- 四组业务写入都使用第三条 SQLite 连接先取得 `BEGIN IMMEDIATE` 写锁；A、B 请求发出后必须分别提供已经到达真实 `.immediate()` 调用的 ready 证据，主测试看到两份完整证据且两个请求都仍未结束后才释放锁。
- Ready 证据由仅测试使用的 CommonJS preload 生成。它拦截 standalone 实际加载的 `better-sqlite3`，完整保留 transaction 的 callable、`default`／`deferred`／`immediate`／`exclusive`、调用者 `this` 和 `database` API，只在命中本轮随机 arm 时于真实 `immediate` 之前原子改名。
- 每个进程拥有独立 writer arm，内容同时绑定随机 run token、A／B 标签和随机 nonce；每轮结束都会删除 arm、临时 arm 与 ready。不存在 arm 时 preload 完全透明，不能把后台无关事务误认为目标请求。
- 首次排课竞态让 A、B 对同一 `section + occurrence` 选择不同星期／时间，严格得到一个 201 和一个带 `LESSON_ALREADY_SCHEDULED` 的 409；数据库只有赢家一行，ID／位置等于赢家，时长、revision、warning JSON 正确，败方顺序重试仍为 409且完整行不变。
- Course Setup 竞态让两个旧表单用同一 course revision 保存两组完全不同的时长、每周次数、主年级、容量、设施、分日和教学周设置，严格得到一个 `{ok:true, changed:true}` 200 和一个带 `COURSE_SETUP_CHANGED` 的 409。
- Course Setup 赢家 revision 只增加 1，最终全部字段整组来自同一赢家；已排 lesson 的冗余时长与 winner 相同、lesson revision 只增加 1、section revision 不变。败方旧 revision 顺序重试仍为 409，五张周期表逐字段零变化。
- Cycle Start 竞态让两个账号使用同一个非空 `currentToken`，严格得到一个 200 和一个“current cycle changed”409；赢家响应为 0 门课／0 个班次／0 条课次，数据库五张周期表全部为空且只有一份 backup。
- Start 建立的 backup JSON 与竞争前课程、Teaching allocation、班次、班级关联和课次五表逐字段相同；教师、学生班级、教室、不可用时段、规则、账号和全部 session 在竞争前后保持不变，赢家 token 与两个进程随后读取结果一致。
- Cycle Restore 竞态使用同一个空周期 token 与同一 backup ID，仍严格得到一个 200 和一个 409；恢复后五张周期表逐字段回到 Start 前版本，backup ID／JSON／时间完全不变，保留资料与 session 不变，恢复 token 等于两进程 GET 且回到 Start 前 token。
- 登录撤销竞态不使用随机 `Promise.all` 猜测时序。测试专用 preload 只在真实 `timingSafeEqual` 已经返回成功、两端都是 64-byte Scrypt 结果、数据库旧哈希二次指纹匹配后，才把 `arm` 原子改名成 `ready` 并同步暂停登录。
- Auth arm 不保存明文密码，并绑定 run token、目标进程 B、随机 nonce 和旧密码哈希指纹；release 文件内容必须精确等于同一 nonce。主测试还会直接读取管理员已经提交的新密码哈希／Inactive 状态和零 session，才允许旧登录继续。
- 密码重置竞态稳定证明“旧密码已验证成功 → 管理员新哈希提交并撤销 session → 登录继续 → 401、无 Set-Cookie、零 session”；之后旧密码继续 401，新密码可以正常登录。
- 账号停用竞态使用相同屏障，稳定证明 Inactive 已提交后旧登录不能补回会话；之后账号保持 Inactive、旧密码 401，重新启用后原密码恢复正常登录。
- Preload 只能由测试命令显式 `node --require` 加载；启动时必须同时满足精确测试模式、64 位随机 run token、私有 marker、A／B 标签、系统临时目录前缀、数据库直属路径和 control 直属路径，否则 fail closed。HTTP 请求无法启用这些同步点，正常 development／production 启动不会加载该文件。
- 两个服务在 spawn 后立即登记到统一清理清单；普通完成、断言失败、Ctrl-C、SIGTERM、认证暂停和端口启动失败都共用幂等 cleanup。清理会先用匹配 nonce 释放认证屏障，再 TERM／必要时 KILL 两个进程，关闭 blocker 并删除整个临时目录。
- 新增 `test:concurrency`；日常 `test:integration` 在同一次 build 后依次运行 CRUD 与跨进程回归，`test:release` 在同一次 build 后依次运行 CRUD、跨进程和 374 班次规模性能回归。老师尚未提交的两条 UX 命令继续保留在工作树，本次只会选择性暂存自己的 package 行。
- 双进程本机回归只证明同一主机、同一文件、短事务下的 SQLite 串行化和业务 CAS 正确；它不改变部署约束。Railway／其他托管环境仍必须保持单应用实例、单持久化 volume，不能把同一 SQLite 文件用于多副本或网络文件系统横向扩容。

### 本次验证

- `node --check scripts/verify-cross-process-concurrency.mjs`、`node --check scripts/support/auth-race-preload.cjs`、两文件 ESLint 和 `git diff --check` 全部通过；独立审查在 writer-ready、auth barrier、API 语义、快照和清理边界中未发现 P0／P1。
- `npm run test:concurrency` 在收尾前后各完整通过一次；每次都重新生成 24 个 production 页面／API，然后通过共享账号、首次排课、Course Setup、Cycle Start／Restore、密码重置和账号停用六组检查。
- 完整 `npm run test:release` 通过：身份、Excel 安全边界、Teaching allocation 重导关系、CRUD／revision、主资料 warning 回滚、完整备份、Cycle、SQLite 外键、双进程并发和规模性能全部成功。
- 本轮规模性能基线为 Issues p95 中位 `16.4 ms`、Year timetable p95 `13.6 ms`、30 间教室候选中位 `554.3 ms`、六账号 30 请求轮询整轮 `48.5 ms`／请求 p95 `46.2 ms`、360 条 warning 重算 `186.3 ms`、一个写入加十个读取 `222.1 ms`。
- 并发响应没有出现 500、503、SQLite、UNIQUE、constraint、表名或列名泄露；所有赢家／败家结论均根据实际 HTTP 状态反推，测试不假定 A 或 B 固定获胜。
- 两个 standalone 停止后，临时数据库 `integrity_check=ok`、`foreign_key_check` 为空；最终仍为 1 门课程、2 个班次、1 条已排课和 1 份应急备份。
- `os.tmpdir()` 中没有残留 `timetabling-api-concurrency-*`、`timetabling-api-crud-*` 或 `timetabling-api-performance-*` 目录。正式 `web/data/timetabling.db` 修改时间仍为 `2026-08-11 04:45:51`、大小 544,768 bytes，`integrity_check=ok` 且 `foreign_key_check` 为空；本轮自动化没有连接或修改它。

### 下一步

1. 复用 `calculatePlacementWarnings` 的 prepared statements 与批量上下文，降低 Clear slots 对大量教室／时段重复编译 SQL 的成本；现有 374 班次基线用于防止优化引入资料错误或数量级退化。
2. 增加两个真实浏览器会话的 Course Configure／Inspector 冲突验收，确认服务端 409 之外的重新载入、焦点和提示在老师实际操作中也清楚。
3. 统一首次排课、班次编辑等剩余写入口在超过 SQLite busy timeout 时的安全 503／Retry-After 语义，并另建旧数据库双进程冷启动迁移专项；正式部署仍维持单实例。

## 2026-08-11｜复用警告查询模板并将候选搜索提速约三倍

### 已完成

- 把 `calculatePlacementWarnings` 使用的 16 条固定 SQL 集中到 `preparePlacementWarningStatements`。一次候选搜索或一次整表警告刷新只编译一组 Statement，后续每个日期、时间、教室和课次只替换绑定参数执行，不再重复编译相同 SQL。
- 新增 `PlacementWarningInput` 与 `PlacementWarningStatements` 两个清晰类型；`calculatePlacementWarnings` 现在强制调用者显式传入 Statement 集合，未来开发者不能因为遗漏参数而悄悄退回“每次规则计算自动 prepare”的慢路径。
- 本次只缓存 SQLite 编译后的 Statement handle，不缓存启用规则、课程、教师、班级、教室、不可用时段或排课结果。每次 `.get()`／`.all()` 仍读取当前连接和当前事务的最新资料，多账号修改后的数据不会被跨请求旧缓存覆盖。
- 候选搜索在读取完整班次、Active 教师与 Active 教室后为本请求建立一组 Statement，并在全部 `day × hour × room` 组合中复用；候选排序、零 warning 筛选和所有英文提示保持不变。
- 整表 warning 刷新每次建立一组 Statement，并在按 lesson ID 排序的全部课次中复用；warning 文字、加入数组的顺序、`warnings_json`、严重程度和事务回滚语义均未改变。
- 首次排课在同一个 `IMMEDIATE` 事务中，于 INSERT 前建立 Statement、计算新课 warning、写入课次，再使用同一组 Statement 刷新整表。Statement 只保存编译 SQL而不保存查询结果，所以 INSERT 后重新执行能够看到刚写入的课次；新课与其他受影响课程仍一起提交或回滚。
- 其余教师、班级、教室、规则、不可用时段、Course Setup、Course Sections、课程移动／删除和 Cycle Restore 的 warning 刷新入口继续在原有外层 `IMMEDIATE` 事务中运行，没有扩大跨进程缓存或改变锁边界。
- 为 16 条警告 SQL 加入只供测试识别的注释 marker；SQLite 会忽略该注释，业务查询计划和返回结果不变。
- 新增测试专用 `statement-reuse-audit-preload.cjs`。它只在明确的 performance test mode、64 位随机 token、系统临时目录、指定数据库直属路径、marker 与报告直属路径全部匹配时启动；普通 development／production 不会加载，HTTP 也不能启用。
- 审计 preload 只记录带 marker 的 SQL 模板、prepare 次数和执行次数，不保存绑定参数或真实业务资料。Statement 原型包装通过 WeakMap 识别目标 Statement，并用 `Reflect.apply` 保留 better-sqlite3 原始 `this` 行为。
- 性能脚本把“真实耗时”和“结构审计”完全分开：正常 production 服务不加载 preload，完成所有计时后停止；随后分别启动一个候选搜索审计进程和一个全量刷新审计进程，因此计数器开销不会污染加速前后的墙钟数据。
- 两个审计进程各自使用新的随机 token 和报告文件。目标 HTTP 完整返回后，主测试通过 `SIGUSR2` 请求同步报告，并要求 `snapshotSequence` 增加后才读取，避免固定等待时间在慢速 CI 上误读半途或上一阶段的旧快照。
- 自动门槛要求当前全量算法至少存在一条执行 32 次以上的热点 SQL，每条热点的执行／prepare 比例至少为 8；另用源码守卫禁止把 `.prepare()` 放回 `calculatePlacementWarnings` 主体。未来若改成真正的批量或增量算法，可同时更新这项明确契约，而不会依赖易抖动的固定加速百分比。
- 所有新增生产代码、测试 preload 和性能门槛均加入面向基础开发人员的中文注释。老师尚未提交的 UX 两条 package 命令、脚本和文档保持不动。

### 本次验证

- `node --check scripts/verify-api-performance.mjs`、`node --check scripts/support/statement-reuse-audit-preload.cjs`、`git diff --check`、独立 TypeScript 检查、ESLint、production build 和最终 `npm run test:release` 全部通过。
- 完整发布回归继续覆盖身份／Cookie、Excel 安全边界、Teaching allocation 重导关系、教师／班级／教室／课程／班次／排课 CRUD、revision CAS、warning 故障原子回滚、完整系统备份、Cycle、SQLite 外键和两个 production 进程的排课／Course Setup／Cycle／登录撤销竞态。
- 在相同的 52 门课、374 个班次、360 条已排课、87 位教师、54 个学生班级和30间教室夹具中，优化前基线为候选搜索约 `554.3 ms`、整表 warning 刷新约 `186.3 ms`。
- 最终无插桩发布回归中，30 间教室候选搜索中位为 `218.8 ms`，360 条课次 warning 刷新中位为 `61.5 ms`；多次单独复跑分别得到约 `192.6–201.3 ms` 与 `57.5–61.5 ms`。候选搜索约快 2.5–2.9 倍，警告刷新约快 3 倍。
- 最终其他规模指标仍远低于宽松门槛：Issues p95 `29.8 ms`、Year timetable p95 `20.0 ms`、六账号30请求轮询整轮中位 `64.1 ms`、一个 warning 写入加十个读取中位 `100.7 ms`；没有 500、503或超时。
- 独立候选审计精确记录 `16` 次 prepare、`12,600` 次目标 SQL 执行，最高单一模板复用 `900` 倍；独立全量刷新审计记录 `16` 次 prepare、`5,288` 次执行，最高复用 `600` 倍。若恢复旧的循环内 prepare，这两项会违反至少 8 倍复用门槛。
- 固定资料数量、三个年级总表数量、14 个待排班次、270 条问题、候选保留教室、Personal timetable 跨年级关系、360 个不同 lesson 的 warning UPDATE、纯读写锁证明、最终 `integrity_check=ok` 与空 `foreign_key_check` 全部继续通过。
- 所有测试数据库、随机 marker、审计 JSON 和 standalone 进程都位于 `os.tmpdir()` 的唯一目录并在成功／失败后清理；没有残留 `timetabling-api-performance-*`、`timetabling-api-crud-*` 或 `timetabling-api-concurrency-*`。
- 正式 `web/data/timetabling.db` 修改时间仍为 `2026-08-11 04:45:51`、大小 544,768 bytes，`integrity_check=ok` 且 `foreign_key_check` 为空；本轮自动化没有连接或修改它。
- 三项独立只读审查核对了16条SQL、positional参数、warning顺序、Statement跨INSERT可见性、13个刷新事务入口、两个 standalone 的连接隔离、preload路径安全和报告新鲜度；最终未发现 P0／P1。两项非阻断 P2 是 SIGUSR2 审计仅适用于当前 macOS／Linux 环境，以及直接手工运行旧 `.next` 时源码守卫可能与旧 bundle 不同；正式测试命令总会先 build，因此当前发布门槛不受影响。

### 下一步

1. 修复 Candidate API 的既有一致性边界：在短只读快照中一次读取候选所需资料，再在内存中计算，避免另一进程恰好修改课程或 Cycle 时单个响应混合两个版本；同时把未知数据库异常改为安全通用 500，不能原样当成 400 返回。
2. 增加两个真实浏览器会话的 Course Configure／Inspector 冲突验收，确认服务端 409 之外的重新载入、焦点和提示在老师实际操作中也清楚。
3. 统一首次排课、班次编辑等剩余写入口在超过 SQLite busy timeout 时的安全 503／Retry-After 语义，并另建旧数据库双进程冷启动迁移专项；正式部署仍维持单实例。

## 2026-08-11｜Candidate API 使用一致读快照并收紧安全错误边界

### 已完成

- Candidate／Clear slots 的完整读取和警告计算现在放在同一个 SQLite `DEFERRED` 只读事务中；事务第一条查询固定已提交快照，同一响应不能再把修改前的课程／教室清单与修改后的容量、规则、教师状态或排课记录拼在一起。
- 当前实现刻意选择最低风险的完整读事务，而不是一次性重写 warning 引擎。代价是在 rollback journal 模式下约 150–220 ms 的候选计算期间，另一进程可以执行修改但 COMMIT 会短暂等待读事务结束；现有 5 秒 busy timeout 和单实例部署下有充分余量。
- 为候选业务新增明确错误类型：资料未完成为 400、班次不存在为 404、旧页面的 occurrence 已被移除或已排课为带 `CANDIDATE_REQUEST_STALE` 的 409、SQLite `BUSY／LOCKED` 为带 `Retry-After: 1` 的 503。
- 未知数据库、schema、磁盘或程序异常不再把 `error.message` 原样当作 400 发给浏览器；服务器只记录内部详情，客户端固定收到 `Candidate slots could not be calculated. Try again.` 500，避免泄露 SQLite、表名、列名或路径。
- occurrence 查询参数缺省为第一次课；显式参数只接受精确的 `1` 或 `2`，空白、0、3、科学记数法和 `+1` 都得到固定 400，不再依赖 JavaScript 宽松数字转换。
- 警告计算读取不到关联课程时新增显式内部不变量检查，不能再以 `undefined` 解引用 TypeError 偶然决定 HTTP 响应；正常外键和同一读快照下该分支不会出现。
- 扩充 production API CRUD 回归：验证缺失班次 404、五类非法 occurrence 400、未分配 Active 教师 400、有效候选非空、课次排入后候选 409，以及错误正文不含 SQLite／表列／stack 文字。
- 扩充双 standalone 跨进程回归：课程最低容量30、唯一 Active 教室容量20时旧候选精确为空；修改为40后新候选精确为35项，每天只含 09、10、11、13、14、15、16 点且全部显示容量40。
- 跨进程测试会让 A 在真实 Active rooms 查询已经物化容量20后暂停，再由 B 的正式 Room PATCH 在 IMMEDIATE 事务内真实执行容量40的 UPDATE；A 的读锁仍存在时 B 的 HTTP 必须等待 COMMIT，释放 A 后其响应只能是完整旧状态，随后 A／B 都读取完整新状态。
- 测试同步点只由显式 `node --require` 的隔离 preload 使用，并绑定随机 run token、A／B 标签、随机 nonce、系统临时目录、数据库和 control 直属路径；生产启动不加载，HTTP 无法启用。异常、Ctrl-C 和 SIGTERM 都会先写入匹配 release，再停止进程并删除整个临时目录。
- 新增及修改的数据库、API 和测试区块都写入面向基础开发人员的中文注释；老师尚未提交的 UX package 命令、脚本和文档继续保持不动。

### 本次验证

- `node --check` 两个跨进程脚本、`git diff --check`、独立 TypeScript 检查、ESLint 和 production build 全部通过。
- `npm run test:concurrency` 完整通过：候选快照、首次排课、Course Setup、Cycle Start／Restore、密码重置和账号停用六组跨进程竞态全部成功，没有 500、503或 SQLite 泄露。
- 最终 `npm run test:release` 完整通过：身份、Excel、Teaching allocation 重导、全部 CRUD／revision、warning 原子回滚、完整备份、Cycle、外键、跨进程和374班次规模性能全部成功。
- 最新满载基线为 Issues p95 中位 `15.5 ms`、Year timetable p95 `13.0 ms`、30间教室候选中位 `145.3 ms`、六账号30请求轮询整轮 `56.2 ms`／请求 p95 `51.4 ms`、360条 warning 重算 `62.8 ms`、一个写入加十个读取 `93.9 ms`。
- Prepared Statement 结构审计仍为候选 `16 prepare／12,600 execute`、全量警告刷新 `16／5,288`；新增一致读事务没有破坏上一任务的查询复用或数量级性能。
- 所有自动化只使用 `os.tmpdir()` 下的临时 SQLite 和随机 localhost 端口；测试结束没有残留 `timetabling-api-*` 目录。
- 正式 `web/data/timetabling.db` 修改时间仍为 `2026-08-11 04:45:51`、大小 544,768 bytes，`integrity_check=ok` 且 `foreign_key_check` 为空；本轮测试没有连接或修改正式数据库。

### 下一步

1. 在不复制两套规则语义的前提下，把 Candidate 改成“短事务一次性加载完整资料 + 事务外纯内存计算”，缩短 rollback journal 下对其他老师 COMMIT 的等待；当前完整读事务已先保证正确性。
2. 增加两个真实浏览器会话的 Course Configure／Inspector 冲突验收，确认服务端 409 之外的重新载入、焦点和提示在老师实际操作中也清楚。
3. 统一首次排课、班次编辑等剩余写入口在超过 SQLite busy timeout 时的安全 503／Retry-After 语义，并建立旧数据库双进程冷启动迁移专项；正式部署仍维持单实例。

## 2026-08-11｜为 Candidate 503／500 建立真实故障回归

### 已完成

- 在 Candidate 的第一条班次查询加入 SQLite 会忽略的内部 marker；正常 production 行为不变，只有双进程测试显式加载的隔离 preload 会识别它。
- Candidate BUSY 测试不伪造异常：A 已完成 Cookie 验证并进入 `DEFERRED`、但尚未执行首条 SELECT 时暂停；第三条 SQLite 连接确认 `journal_mode=delete` 后取得真实 `BEGIN EXCLUSIVE`。
- A 释放后保持 EXCLUSIVE 锁直到默认约5秒 busy timeout 完成，接口必须返回固定 503、`Retry-After: 1` 和安全业务文案；收到响应后才 ROLLBACK，证明错误来自 Candidate 的真实锁竞争而非认证代理或测试替身。
- 内部故障测试在相同 entry marker 注入普通 Error，错误刻意包含 `SECRET`、SQL、表名、列名、stack 和私有路径哨兵；浏览器只能收到固定 `{error: "Candidate slots could not be calculated. Try again."}` 500，且没有 `Retry-After`。
- 两类故障的 arm 都绑定随机 run token、A 标签、随机 nonce、目标 section ID 和故障类型；ready 文件原样保存 control，测试会逐字段核对，不能由偶然的其他 500／503产生假阳性。
- 故障前后在同一个只读事务中逐字段比较14张业务表，包括账号 session 与紧急备份；同一 observer 的 `PRAGMA data_version` 也保持不变，证明 Candidate 失败没有隐藏写入。
- 清除故障和锁后再次从 production API 请求 Candidate，响应必须与故障前基线逐字段相同；连接、事务和 prepared statements 都能继续正常使用。
- 普通完成、断言失败、Ctrl-C 和 SIGTERM 继续共用幂等清理：先写匹配 nonce 的 release，再等待请求、停止 A／B，并删除本轮唯一临时数据库和 control 目录。

### 本次验证

- `node --check`、`git diff --check`、独立 TypeScript 检查、ESLint、production build 和 `npm run test:concurrency` 全部通过。
- 最终 `npm run test:release` 完整通过；CRUD／Excel／Cycle、Candidate 快照与故障、首次排课／Course Setup／周期／登录竞态、外键和满载性能均成功。
- 最新满载基线为 Issues p95 `17.0 ms`、Year timetable p95 `13.8 ms`、30间教室候选中位 `148.5 ms`、六账号30请求轮询整轮 `57.6 ms`／请求 p95 `54.0 ms`、360条 warning 重算 `61.4 ms`、一个写入加十个读取 `95.6 ms`。
- 正式 `web/data/timetabling.db` 仍保持 `2026-08-11 04:45:51`、544,768 bytes；本任务自动化只使用 `os.tmpdir()` 临时库，结束后没有残留 `timetabling-api-*` 目录。

### 下一步

1. 修复数据库首次初始化失败时的新连接清理：只关闭尚未挂入 global 的局部连接，避免持续锁竞争累积文件描述符。
2. 在不复制两套规则语义的前提下，把 Candidate 改成“短事务加载完整快照 + 事务外纯内存计算”，进一步缩短 rollback journal 下其他老师 COMMIT 的等待。
3. 增加两个真实浏览器会话的 Course Configure／Inspector 冲突验收，并继续统一首次排课、班次编辑等剩余写入口的安全 503 语义。

## 2026-08-11｜初始化失败时主动关闭 SQLite 连接并验证干净重试

### 已完成

- 把首次打开数据库后的外键设置、表结构初始化、开发 seed 和全局发布放入同一个局部 `try` 边界。任一步失败都会主动关闭这条尚未发布的新连接，避免等待垃圾回收期间累积文件描述符或继续占用 SQLite 文件。
- 关闭失败不会覆盖真正的初始化错误：服务器会记录关闭异常，但调用者继续收到最初的 pragma、schema 或 seed 错误，排查时不会被次要清理问题误导。
- 只有表结构与开发示例资料全部成功后，才同时发布 `timetableDatabase` 和 `timetableSchemaVersion`。seed 中途失败时不会留下“版本已完成、连接却不存在”的矛盾热重载状态。
- 已经发布并由开发热重载复用的 global 连接保持原边界：迁移失败时不关闭共享连接、不提高 schema 版本；下一次请求仍用同一条打开的连接重试，避免 global 指向已关闭连接后永久失效。
- 为第一组真实 schema SQL 加入 SQLite 会忽略的内部 marker。双 standalone 测试 preload 会让 A 在这组 CREATE 已实际提交后故意失败一次，并且只有命中故障的同一 Database 实例完成真实 `close()` 后才生成 ready 证据。
- A 的第一次 health 返回失败后，启动等待器不会重启进程，而是在同一个 child 中再次请求 health。第二次必须幂等补齐7条规则和 runtime 索引、通过 integrity／FK 检查，证明没有复用关闭对象，也不是只靠 `SELECT 1` 偶然成功。
- 新增独立 `verify-database-initialization.mjs`，直接以 TypeScript `transpileModule` 加载真实 `database.ts`，因此能覆盖 production build 会裁掉的开发 seed 分支；测试只把 better-sqlite3 换成继承真实 Database 的计数子类，所有 SQL、事务和落盘行为仍由真实 SQLite 执行。
- 独立回归覆盖三组确定性故障：fresh schema 在真实 DDL 后失败；development seed 在第二位教师真实 INSERT 后失败；已发布 global 连接在强制旧 schema 版本后迁移失败。分别断言 close 次数、`.open`、构造实例数、global 指针、schema 版本、seed 整体回滚和同连接重试。
- 初始化脚本会在跨进程回归开始时先执行；现有 `test:integration`、`test:concurrency` 和 `test:release` 都会自然经过这项固定门槛，无需改动老师尚未提交的 package UX 区块。以后若有人把 schema 版本提前发布、删除主动 close、误关 global 连接或破坏 seed 事务，普通发布回归会直接失败。
- 新增生产、测试和脚本区块都写入面向基础开发人员的中文注释。老师尚未提交的 UX 两条 package 命令、UX 脚本和文档保持不动。

### 本次验证

- `node scripts/verify-database-initialization.mjs` 三组故障全部通过：fresh 失败后实例1关闭且 global/version 未发布，实例2成功发布，第三次调用不再新建；seed 失败后教师／班级／教室均为0，重试后精确为4／4／3；global 迁移失败后实例数和 close 数不变，同一 open handle 重试成功。
- `node --check` 两个相关脚本、`git diff --check`、独立 TypeScript 检查、ESLint、production build 和 `npm run test:concurrency` 全部通过。
- 最终 `npm run test:release` 完整通过：数据库初始化、身份、Excel、Teaching allocation、全部 CRUD／revision、warning 原子回滚、完整备份、Cycle、Candidate 快照与故障、跨进程排课／Setup／认证竞态、外键和374班次规模性能均成功。
- 最新满载结果为 Issues p95 `15.0 ms`、Year timetable p95 `12.7 ms`、30间教室候选中位 `144.0 ms`、六账号30请求轮询整轮 `59.6 ms`／请求 p95 `56.7 ms`、360条 warning 重算 `63.0 ms`、一个写入加十个读取 `99.0 ms`。
- Prepared Statement 审计仍为候选 `16 prepare／12,600 execute`、全量刷新 `16／5,288`；初始化故障测试没有改变业务查询或性能基线。
- 所有新测试数据库均位于 `os.tmpdir()` 的唯一目录，结束后没有残留 `timetabling-db-init-*` 或 `timetabling-api-*`。正式 `web/data/timetabling.db` 仍为 `2026-08-11 04:45:51`、544,768 bytes，`integrity_check=ok` 且 `foreign_key_check` 为空。
- 两项独立只读审查核对了 close 错误不掩盖原错、fresh/global 分支隔离、同实例 ready 证据、seed 事务回滚、global schemaVersion 时点、临时路径和 package 接线，最终未发现 P0／P1。

### 下一步

1. 在不复制两套规则语义的前提下，把 Candidate 改成“短事务加载完整快照 + 事务外纯内存计算”，进一步缩短 rollback journal 下其他老师 COMMIT 的等待。
2. 增加两个真实浏览器会话的 Course Configure／Inspector 冲突验收，确认服务端 409 之外的重新载入、焦点和提示在老师实际操作中也清楚。
3. 统一首次排课、班次编辑等剩余写入口在超过 SQLite busy timeout 时的安全 503／Retry-After 语义，并建立旧数据库双进程冷启动迁移专项。

## 2026-08-11｜Candidate 使用短 SQLite 镜像并在事务外完成规则计算

### 已完成

- Candidate／Clear slots 不再让正式数据库的 `DEFERRED` 读事务覆盖整段日期、时间和教室组合计算。事务现在只执行第一条班次 SELECT、固定当前已提交版本并调用 `db.serialize()` 复制一致 SQLite 镜像，随后立即 COMMIT／释放正式库读锁。
- 不存在的班次会在短事务的第一条查询后直接返回安全 404，不复制整库；其余课程时长、每周课次、教师状态、房间、规则和冲突资料都继续从同一个已固定快照判断。
- 镜像 Buffer 会同步构造一个独立 readonly 内存 SQLite。原 `calculatePlacementWarnings` 和16条 prepared SQL 在这个内存库上原样运行，因此周次重叠、教师／班级／教室冲突、不可用时段、午餐、连续时长、每日上限、跨楼、警告文字和加入顺序都没有复制成第二套规则。
- 完整镜像而不是手工维护“候选需要哪些表”的白名单，可避免以后新增规则依赖后忘记同步候选数据；镜像只存在于本次服务器请求内存，不写临时文件、不返回浏览器、不写日志，也不跨请求缓存。
- better-sqlite3 从 Buffer 同步复制完内存库后，系统立即用 `fill(0)` 覆写原 Buffer；候选计算无论成功或报错，readonly 内存库都在 `finally` 中关闭并释放 native 内存，尽量缩短账号摘要、会话摘要和备份等非候选资料额外副本的驻留时间。
- `candidate-entry` 故障 marker 移到正式数据库短事务的第一条 SELECT，真实 EXCLUSIVE 锁仍能稳定验证 BUSY 503；Active rooms marker 则自然命中内存库，能确定性暂停事务外计算。
- 跨进程回归现在要求：A 已复制容量20的内存快照并暂停计算时，B 的正式 Room PATCH 必须在2秒内完整返回200，数据库容量已经是40，而 A 仍未结束；释放后 A 的响应必须逐字段等于旧 baseline，随后 A／B 都返回35个容量40的新候选。
- 这条竞态会同时抓住两类回归：若候选重新把计算放回正式库长事务，B 会在2秒门槛超时；若没有一致快照而继续读取 live 数据，A 会产生“显示旧容量20、却按新容量40放行”的混合响应。
- 原 Candidate 400／404／409／503／固定500、纯读取、无 warning 写入、prepared statement 复用和所有清理路径保持不变。新增及修改区块继续写入面向基础开发人员的中文注释，老师未提交的 package UX 命令和4个 UX 文件保持不动。

### 本次验证

- `git diff --check`、两个脚本 `node --check`、独立 TypeScript 检查、ESLint、production build、`npm run test:concurrency` 和最终 `npm run test:release` 全部通过。
- 完整发布回归继续覆盖数据库初始化、身份、Excel、Teaching allocation、CRUD／revision、warning 原子回滚、完整备份、Cycle、Candidate BUSY／500、跨进程排课／Setup／认证竞态、SQLite 外键和374班次规模性能。
- 最终满载结果为 Issues p95 `16.3 ms`、Year timetable p95 `12.6 ms`、30间教室候选中位 `139.1 ms`、六账号30请求轮询整轮 `53.6 ms`／请求 p95 `51.7 ms`、360条 warning 重算 `60.5 ms`、一个写入加十个读取 `93.3 ms`。
- Candidate 的结构审计仍精确得到 `16 prepare／12,600 execute`、最高复用900倍；全量刷新仍为 `16／5,288`、最高600倍，证明内存计算没有绕开或简化统一警告引擎。
- 新竞态直接确认 B 的 Room PATCH 在 A 计算完成前已经 HTTP 200且正式库容量为40；A 返回旧 baseline，随后两个 standalone 返回完全相同的新35项，没有锁等待、混合版本或缓存残留。
- 正式数据库当前约0.55 MB，单次镜像额外内存很小；方案的峰值额外内存约为数据库大小的两份，若未来数据库长期增长到几十 MB，需要增加镜像大小／RSS监控或改成经过规则依赖审计的白名单快照。同步规则计算仍会占用单实例 Node 事件循环约139 ms，本次解决的是 SQLite COMMIT 等待而不是 CPU 并行。
- 当前正式和测试 SQLite 都是 `journal_mode=delete`；未来若迁移到 WAL，需要重新验证 serialize／反序列化一致性竞态。正式 `web/data/timetabling.db` 仍为 `2026-08-11 04:45:51`、544,768 bytes，`integrity_check=ok` 且 `foreign_key_check` 为空；临时目录没有残留。
- 三项独立只读审查核对了 SQLite pager 快照、serialize／deserialize 内存所有权、16条规则语义、marker时序、writer COMMIT、清理和prepared审计，最终未发现 P0／P1。

### 下一步

1. 增加两个真实浏览器会话的 Course Configure／Inspector 冲突验收，确认服务端 409 之外的重新载入、焦点和提示在老师实际操作中也清楚。
2. 统一首次排课、班次编辑等剩余写入口在超过 SQLite busy timeout 时的安全 503／Retry-After 语义，并建立旧数据库双进程冷启动迁移专项。
3. 在规模资料明显增长前加入 Candidate 镜像大小／RSS趋势报告；若超过当前院系规模，再评估 worker 或经过规则依赖审计的白名单内存快照。

## 2026-08-11｜保护多人编辑草稿、统一年级工作区快照并补齐真实并发验收

### 已完成

- Course Configure 与 Inspector 都以 revision 作为多人编辑令牌。服务端的旧版 Scheduled Lesson PATCH／DELETE 409 现在带稳定 `SCHEDULED_LESSON_CHANGED` code；浏览器只在状态和 code 同时匹配时进入协作冲突流程，其他 409 不会被误判。
- 新增独立 `lesson-draft-reconciliation` helper：五秒轮询看到相同 revision 时同步服务器派生资料；看到更高 revision 或课程已被退回时，保留老师当前打开的同一个草稿对象并显示 stale 提醒，不再用新 key 静默重建星期、时间、教师、教室和学生班级字段。
- Inspector 的 Save、Return 和已排课程拖动共用同步 ref 锁与可见 state。快速双击、Save／Return 交叉、保存中拖动、切换 Workspace／年级／账号／问题或再开待排表单都被阻止，完成后才恢复入口。
- 已排课程拖动在 drag-start 保存完整课程和当时的 revision；drop 不会借五秒轮询刚收到的新版 revision 覆盖另一位老师已经完成的移动。
- 冲突刷新会区分四种真实去向：原 lesson 仍在、同一个 `section + occurrence` 已用新 ID 重新排入、确实退回当前年级待排区、或已离开当前年级／周期。提示、Inspector／待排抽屉状态和焦点分别与去向一致。
- Return 200 后也不再无条件宣称“已进入待排区”。系统会重新读取一致工作区；若另一账号已经立即重排，则打开新 lesson；若课程改年级，则给出中性准确说明。
- Course Configure 保存使用同步锁，保存中停用会卸载表单或切换资料的入口。成功和 409 都按稳定 course ID 恢复到同一门课程的 Configure 按钮；刷新失败时回到课程搜索框，不会重开仍带旧 revision 的不可信表单。
- Course Configure 打开后聚焦 Duration；Inspector 从隐藏状态因冲突重开时聚焦 Day。新增一次性 `pendingInspectorFocusRef`，让唯一 opening Effect 决定 Day 或 Close，消除了两个 `requestAnimationFrame` 先后抢焦点的竞态。
- 主动换年级、保存后刷新和冲突重载拥有高于五秒轮询的 generation。主动请求开始前废弃旧轮询，主动请求在途时新 tick 不领取号码；旧成功和旧失败都不能倒灌或覆盖新提示。
- 新增 `/api/schedule/workspace`。服务端在一个 SQLite `DEFERRED` 只读事务中读取已排课程、待排课程、问题、教师和教室，五份资料共享第一条 SELECT 固定的已提交快照。`openTimetable`、年级轮询和从问题清单打开课程全部改用这一份聚合 payload。
- 聚合接口解决了独立 `/lessons` 与 `/unscheduled` 请求的既有边界：另一账号恰好 Return／重排时，同一个逻辑课次不会在单次页面刷新中同时出现于总表和待排区，也不会因为混合两个版本而两边都缺失。
- 从问题清单打开课程现在也复用主动 `openTimetable` 流程；Schedule Issue 增加稳定 `sectionId + occurrence`，可以识别 Return 后立刻重建的新 lesson ID，同时受到相同 generation 保护。
- 年级页面每个账号的一次五秒轮询从5个 HTTP 请求减少为1个聚合请求。性能门槛相应改成六账号6请求，并在写入并发阶段真实执行5个聚合 workspace 读取，不再测试旧页面请求形状。
- 所有新增／修改的生产、测试和脚本区块都加入面向基础开发人员的中文注释，说明业务原因、事务边界、竞态时序和失败清理。老师尚未提交的 package UX 两条命令、两份 UX 脚本和两份 UX 文档保持不动，未纳入本次提交。

### 真实浏览器验收

- 使用一个真实应用内浏览器和一个拥有独立 Cookie 的第二排课账号／API actor，在隔离临时 SQLite 上验证多人操作；没有连接正式数据库，也没有把单浏览器测试误写成两个浏览器 context。
- Course Configure：浏览器保留容量77草稿，第二账号先保存容量41；浏览器随后收到409、旧表单关闭、焦点回到准确的 `Configure BROWSER101`，重开后显示赢家41且 Duration 获焦。
- Inspector：浏览器保留 Thursday 14:00 草稿与当前 Start 焦点，第二账号改为 Friday 08:00；跨过一次五秒轮询后本地草稿和焦点仍保留并出现 stale 提醒，Save 409 后载入 Friday 08:00且 Day 获焦。
- 远端 Return：本地13:00草稿跨轮询保留；浏览器 Return 409 后 Inspector 关闭、待排区打开、焦点落在待排 Close，提示准确说明已退回。
- Return 后立即重排：旧 lesson ID 保存得到404后，浏览器按相同 `section + occurrence` 找到新 ID、打开 Wednesday 10:00最新版并聚焦 Day，没有误报待排。
- 课程主年级从Y1改到Y2后，旧Y1 Inspector 冲突不会打开Y1待排区；提示说明课程可能已移到其他年级。切换Y2并跨过轮询仍停在Y2，新 lesson 正常可见。
- 验收结束已关闭临时浏览器标签，并删除 `/private/tmp/timetabling-browser.CY0wdw` 中不可恢复但完全可丢弃的测试数据库与 Cookie。

### 自动回归与结果

- CRUD 回归新增 reconciliation 的对象身份断言，并锁定 Scheduled Lesson stale PATCH、stale DELETE、Course Setup 提高 lesson revision 后旧 Inspector 三条路径的稳定 code、安全正文和完整业务表零变化。
- 双 standalone 新增同一 lesson／同一 revision 的 PATCH 竞态：两个真实进程必须严格得到一个200和一个409；赢家整组字段、lesson revision和section revision只增加一次，败方顺序重试仍409且14张业务表零变化。
- 年级 workspace 新增确定性快照竞态。A 在真实旧 lessons `.all()` 后暂停；B 在真实 INSERT 后暂停。释放B后，测试用 `busy_timeout=0` 的新只读连接观察 DELETE journal 的真实 `SQLITE_BUSY／PENDING`，证明 B 已到 COMMIT且正被 A 的 SHARED snapshot 挡住，再释放A。A必须返回完整旧版，随后A／B都返回完整新版；没有固定 sleep 假阳性。
- `git diff --check`、独立 TypeScript、ESLint、production build、`npm run test:concurrency` 和最终 `npm run test:release` 全部通过。测试共构建25个页面／API路由，并覆盖身份、Excel、Teaching allocation、CRUD／revision、warning 原子回滚、完整备份、Cycle、Candidate故障、跨进程和SQLite外键。
- 最新374班次规模结果：Issues p95中位 `19.0 ms`、Year endpoint p95 `99.5 ms`、30间教室 Candidate中位 `166.8 ms`、六账号6请求一致快照轮询整轮中位 `30.6 ms`／请求p95 `33.0 ms`、360条 warning重算 `77.8 ms`、一个写入加5个一致快照读取 `118.6 ms`。
- Prepared Statement审计仍为 Candidate `16 prepare／12,600 execute`、最高复用900倍；全量 warning刷新 `16／5,288`、最高600倍。
- 正式 `web/data/timetabling.db` 最终仍为 `2026-08-11 04:45:51`、544,768 bytes，`integrity_check=ok` 且 `foreign_key_check` 为空。自动化只使用 `os.tmpdir()` 临时库，收尾未发现 `timetabling-api-*` 或 `timetabling-db-init-*` 残留。

### 下一步

1. 若之后引入 Playwright／浏览器测试框架，把本轮 Course Configure 与 Inspector 的 `document.activeElement`、stale banner 和五字段草稿保护固化成一键 UI 回归；当前已完成真实浏览器人工验收和底层纯函数门槛。
2. 为聚合 workspace 等剩余只读路由统一 SQLite `BUSY／LOCKED` 的安全503与 `Retry-After: 1`，并继续保持正式部署单应用实例。
3. 继续本地完善排课功能和易用性；所有功能稳定后再进入免费／低频上线方案，不需要为了本轮测试提前部署。

## 2026-08-11｜补齐课程卡资料、通知语义与 Inspector 键盘反馈

### 已完成

- 先在 `1440×900` 的真实应用内浏览器、隔离临时 SQLite 上走完“建立教师／班级／教室 → 新增课程 → 配置课程 → 分配教师和班级 → 默认待排区 → Inspector 放置课程”的完整 UI 路径；确认待排区默认开启、Inspector 已能更改多个 Student Groups，也现场复现了总表课程卡遗漏 Student Groups 的问题。
- Master timetable 卡片现在固定使用清楚的资料层级：课程／班次名称为最大且加粗的 `11px`，教师、Student Groups 和 Room 固定为较小但仍可读的 `10px`；卡片不显示无用的 `2h／3h` 行，完整 `title` 和可访问名称也包含全部学生班级。
- 操作通知改为显式 `info／success／warning／error`，不再靠英文句子里的关键词猜颜色。每次通知都有独立序号，因此八秒隐藏后再次产生完全相同的错误仍会重新挂载 live region、重新显示并重启计时。
- 全部明显的保存、导入、认证、备份、课程与排课成功／失败／部分成功路径都明确传入通知类型；错误使用 `alert`，其余状态使用 polite live region。Inspector 的层级高于提示，窄窗口下关键 Close／Save 不会被 Toast 抢走点击区域。
- Inspector 的问题面板沿用课程最高严重度：High 为红色、Warning 为黄色、Advisory 为蓝色，不再把所有提示一律渲染成红色 `Resolve issues`。
- Edit、手工 Place 和 Candidate 子视图的 Close 会在卸载按钮后把焦点恢复到稳定存在的 Inspector Close；键盘焦点不会掉回 `body`。已有的草稿、revision、轮询 generation 和导航锁保持不变。
- `openRules`、`openCycle` 与个人课表入口现在捕获网络中断、非成功 HTTP 和损坏 JSON；失败时保留当前完整画面并给出可重试说明，不再产生无反馈的未处理 Promise。
- Fresh production 空库且完全没有课程时，待排区明确引导先 Import／Add course；只有确实存在课程但当前年级尚未配置时才显示配置说明，不再误说“allocation was imported”。
- 页面平滑滚动和 CSS 动画尊重 `prefers-reduced-motion`。同时把原先全部超过 500 字符的 JSX 行拆成可逐段阅读的结构，并继续保留面向基础开发人员的中文区块注释。

### 本次验证

- `git diff --check`、`npm run lint` 与 `npx tsc --noEmit` 全部通过。
- TypeScript AST 检查确认 `setNotice` 的 108 个调用点全部显式提供 tone，没有错误／成功路径落回默认中性样式。
- 自动 UX 源码门槛能够识别固定卡片字号、Student Groups 与浮动 Inspector；最终新构建的多视口浏览器回归会在所有后端发布修复合并后统一执行，不能用本条源码验证代替五名真实老师的任务测试。
- 本轮浏览器和课程资料只使用 `/private/tmp/timetabling-ui-release.*` 下的隔离数据库；正式 `web/data/timetabling.db` 未连接、未修改。

### 下一步

1. 用包含本轮 UI 和后端安全修复的最终 production build，在 `1440×900`、`1024×768` 和窄屏视口重跑课程卡、三天同屏、Toast／Inspector 相交及键盘焦点验收。
2. 完成首次管理员 setup token、登录输入上限、旧 JSON 路由、Workspace BUSY 与完整备份业务不变量修复。
3. 五名真实排课老师的 `UX_TASK_TEST_RESULTS.json` 和 Railway 线上证据仍为空；这两项必须由真实外部验收完成，不能由开发者自测代替。
