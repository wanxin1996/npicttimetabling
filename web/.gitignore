# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# 默认忽略可能包含真实密钥的环境文件；只提交不含秘密值的说明模板。
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/generated/prisma
/data/*.db
# SQLite 数据库、安全副本和活动日志都可能包含真实账号与排课资料；任何扩展名形式都不得进入 Git。
/data/*.db-*
/data/*.sqlite
/data/*.sqlite-*
# 完整恢复安全副本包含账号和业务资料；和主 SQLite 一样只能保留在本地或持久卷，不能提交。
/data/*-restore-safety/
